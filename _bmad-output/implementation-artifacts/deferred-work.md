# Deferred Work

## Deferred from: repo-wide clean-gate sweep (2026-08-20)

- source_spec: none
  summary: `TestGormUserRepository_SearchByUsername` failed against any database containing a real account whose username matches a probe — CLOSED, test namespaced.
  evidence: |
    Found while making `make lint` + the full suites green with no known-failing
    carve-outs. `getTestDB` hands out a transaction that is rolled back on
    cleanup, which isolates the test from OTHER TESTS but NOT from rows already
    in the target database. Every subtest asserted an EXACT result set, so the
    seeded QA account `alice` (users.id 1223) was returned by the `"ali"` probe
    and failed `assert.ElementsMatch`.
    Fixed on the TEST side, not the data side: every fixture username now carries
    an `sbufx_` prefix and every query searches for it, so no real username can
    enter a result set and the assertions stay EXACT (still proving `_`/`%`
    escaping and ascending order). The one query that cannot be prefix-scoped is
    the bare `"%"` probe — whose entire point is that `%` must not mean
    match-everything — so it now asserts against the seeded `Bob` control
    instead of an exact set.
    Deliberately NOT fixed by deleting the row: `alice` is part of a coherent
    hand-seeded QA roster (`alice`, `bruno_qa`, `carla_qa`, `dimitar`, `elena`,
    `filip`, `nikola_new`, `zoran` — all `@beljot.test`, with hand-set
    `created_at` spread across 2025-2026), and `alice`/`bruno_qa`/`carla_qa`
    each carry 2 friendship rows wiring up the Epic 11 friends surfaces for
    manual testing. Deleting it would destroy live dev fixtures AND leave the
    test broken again the moment any account contains "ali".

## Deferred from: 9-7 honour UX review pass (2026-07-31)

- **Abandonment match-end forces every remaining player to the lobby — no "Return to Room" choice.** When a disconnect window expires and the match ends as an abandonment, the server marks the room `completed` (`handleReconnectTimeout` → `UpdateRoomStatus(roomID, "completed")`) and the client's end state is the ReconnectOverlay's single "Return to Lobby" exit (`MatchPage.tsx` renders it from `matchAbandonedData` with only `onReturnToLobby`). Surrender and natural match end instead reopen the room and offer the MatchResult dialog's Return to Room / Return to Lobby pair. Desired: abandonment should end in that same two-button choice for the three remaining players. Scope is cross-cutting, not a client patch: the server must reopen the room instead of completing it (status back to `waiting`, the abandoner's seat freed, presence / "waiting to return" seeded the way reopened rooms already do it, and the honor gate re-checked on return — the abandoner's own drop may now bar them), and the client must route the abandonment end through the MatchResult dialog instead of the overlay's lobby-only exit. Raised by the user during the 2026-07-31 honour UX pass. [server/internal/match/reconnect.go:692, client/src/features/match/MatchPage.tsx:1928-1952]

## Deferred from: 9-7-honor-score-system (2026-07-29)

- **`abandonPartialXPFactor` float truncation is still untested for non-1.0 (inherited from 9.5) — but honor's own float path IS covered.** Story 9.7's `DecayFactor` sits in the same "float feeding an integer-visible result" position that the 9.5 review flagged. It was closed out here rather than deferred: rounding is `math.Round` (half away from zero), chosen explicitly to match the Postgres `ROUND(numeric)` used by the 000017 backfill so the two paths can never disagree, and `honor_test.go` pins non-identity decay factors (0.5 / 0.25 / 0.0601) plus an exactness proof that decay-forward-then-add equals the from-scratch weighted sum. The XP-side item is untouched and remains open. [server/internal/match/xp_award.go:20]
- **Honor counters are denormalized onto `users`, so two-writer drift with `matches` is possible.** A conscious inversion of Story 7-2's "aggregate per request, never denormalize" choice; justified by D3 (admin forgiveness and 9.8's per-join gate both need mutable stored state a pure function over `matches` cannot provide). Mitigated by (a) writing honor in the same best-effort band as coins and XP, from the same two finalizers, and (b) the 000017 backfill being written to be re-runnable as a reconciliation recipe. If drift is ever observed, re-running that `UPDATE ... FROM agg` statement recomputes every column from `matches`. No reconciliation JOB is scheduled — there is no job infrastructure in this codebase (see the `refresh_tokens` reaper entry above, same blocker).
- **An AFK player who lets the per-move timer auto-play every card still scores `completed`.** Follows directly from D1: timer expiry auto-plays and continues the match, it never abandons anyone, so there is no signal to charge. Consistent with how the system actually behaves. If this becomes a farming vector it needs a new abandonment trigger (an AFK-strike threshold), which `spec-abandonment-per-player-results.md` currently freezes — that is a new story, not a patch.
- **`rage_quits` and `timeout_abandons` are not modelled at all.** Deliberate (D1) — neither has a producer in this codebase. The formula uses a single `honorAbandonPenalty` weight, so splitting the bucket later is a one-const change plus a migration, not a redesign.
- **In-app navigation away from a live match keeps the WebSocket open**, so the match limps to a natural end via auto-play and nobody is charged an abandonment. Pre-existing behaviour, out of scope; noted because it is the most likely "why wasn't I penalised?" question about honor.
- **Operator forgiveness has no surface.** `UserRepository.ResetHonor(userID)` ships and the 000017 migration header documents the equivalent SQL, but there is no endpoint, no admin role and no UI (D7) — there is no admin system in this project to hang one on. PO open question #5 asks when the operator surface is wanted.
- **The three auth-envelope-to-`User` mappers are still copy-pasted.** `axiosClient.ts` (refresh), `hooks/mutations/useAuth.ts` (login/register/SSO) and `hooks/useAuth.ts` (bootstrap refresh) each build a `User` field-by-field, so every additive field (9.5's `totalXp`/`level`, 9.7's honor trio) edits three places. Not unified here because `axiosClient.ts` declares its own inline axios response type rather than reusing `RefreshResponse`, so a shared mapper needs those types unified first. The TEST side of the same problem WAS fixed: `test-utils.tsx` now exports `makeUser()` and all ~20 `User` fixtures route through it.
- **Pre-existing `gofmt -l` failure on `server/internal/auth/profile_identity_handler_test.go`** — CLOSED 2026-08-20: `gofmt -w` applied (comment-alignment only, no behaviour change); `gofmt -l ./server` is now empty. Original text follows. (trailing-comment alignment, lines ~118-121 and ~171-174). Untouched by this story and confirmed present on the branch point. It does not block CI: `make lint` runs `golangci-lint run ./...`, which has no `gofmt` linter enabled. One `gofmt -w` on that file clears it.

## Deferred from: code review of spec-auth-long-lived-sessions (2026-07-06)

- **`refresh_tokens` grows unbounded — no pruning of rotated/revoked/expired rows** — with rotation on every refresh and a ~11-min proactive cadence, an active user inserts roughly one row per refresh (~130/day) for up to the 180-day absolute cap; nothing deletes spent rows. Correctness is unaffected (lookups are by unique `token_hash`), but the table and its indexes grow indefinitely. **Deferred per review 2026-07-06** (surfaced by both the blind and edge-case reviewers): needs a reaper (a periodic `DELETE FROM refresh_tokens WHERE revoked_at IS NOT NULL OR family_expires_at < now()` job, or opportunistic per-family cleanup on rotation). No scheduled-job infrastructure exists in the codebase yet, so this folds into a future ops/maintenance pass. [server/internal/refreshtoken, server/migrations/000013_create_refresh_tokens.up.sql]
- **Lost rotation response leaves the browser on a consumed refresh token → one spurious logout next cycle** — if `/auth/refresh` rotates server-side but its `Set-Cookie` response never reaches the browser (a rare partial network failure), the browser keeps the now-consumed token; the next refresh trips reuse detection and revokes the family. Accepted residual of heal-without-rotation (see the spec Design Notes) — the common multi-tab case is now safe, and this failure mode is rare and self-limited to a single re-login. Revisit only if telemetry shows real incidence. [server/internal/auth/handler.go Refresh]

## Deferred from: code review of 9-3-insolvency-ejection-and-room-persistence (2026-06-19)

- **Refund-on-start-failure `ApplySettlement` error is only `slog`-logged — charged coins can be destroyed on a refund-time DB failure** — in `StartMatch`, after a successful `ChargeStakes` but a failed `matchStarter.StartMatch`, the compensating `ApplySettlement(refund)` is best-effort: on error the charged humans are never refunded and the coins leave circulation with no record. **Deferred per decision 2026-06-19:** accept best-effort-with-log as the documented interim — it matches the rest of the wallet layer (no outbox/saga anywhere in the codebase). A durable fix needs a transactional outbox / reconciliation job, which is cross-cutting and beyond 9.3. Probability is low (DB failure in the narrow charge→StartMatch→refund window). Revisit with broader wallet-durability hardening. [server/internal/room/handler.go:2612]
- **`transferOwnershipOrClose` close path does not `DecrementPlayerCount`** — when the helper closes an ownerless room it removes every `room_players` row but never adjusts `PlayerCount`, so the `completed` room is broadcast with a stale non-zero `playerCount`. Pre-existing (the legacy inline owner-leave close had the same omission); benign because the room is `completed` and lobby grids drop completed rooms. [server/internal/room/handler.go:742-760]
- **`ejectInsolventReturner` `player_left` broadcast swallows the `FindPlayersByRoomID` error with no log** — if the post-commit re-read of remaining players errors, the `system:player_left` fan-out is silently skipped (no `slog`), so members miss the freed-seat update. Broadcasts are best-effort by design and the start path logs the equivalent failure; low-value consistency cleanup. [server/internal/room/handler.go:852-854]

## Deferred from: code review of 9-2-room-buy-in-and-match-coin-settlement (2026-06-19)

- **Successful stake charge followed by a failed `matchStarter.StartMatch` strands coins and the room** — in `RoomHandler.StartMatch`, `ChargeStakes` commits (handler.go:2180) before `matchStarter.StartMatch` (handler.go:2198); a start failure is only `slog`-logged with no revert and no refund, unlike the insolvency branch (2181-2190) and the auto-start path's `revertAutoStart`. Every human seat is debited, no match runs, settlement never fires (coins destroyed), and the room is stranded in `playing`. **Deferred to Story 9.3** (decision 2026-06-19): 9.3 owns StartMatch hardening + insolvency/ejection; refund-on-start-failure folds into that work. `StartMatch` failure is rare (only "session already exists") and the room-stranding half is pre-existing. [server/internal/room/handler.go:2198]
- **One insolvent (or post-join-spent) seat makes `ChargeStakes` roll back the whole table and blocks every start attempt** — the join affordability check is a non-locking read (Decision A), so a seated human can drop below the buy-in before start; at start `ChargeStakes` is all-or-nothing, so a single under-funded seat reverts the room to `waiting` and the owner can never start until that player tops up or leaves. A griefing/denial vector. 9.2's hard guarantee ("never start a match with an unpaid stake") holds; the polished per-player ejection is explicitly Story 9.3. [server/internal/room/handler.go:2180]
- **Boot-time reconcile of stale `playing` rooms sinks already-charged stakes** — a process restart between `StartMatch`'s charge (tx 1) and match-end leaves stakes debited with no surviving session to settle; `reconcileStaleRoom` deliberately persists 0 deltas / 0 buy-in and does not refund (documented inline). The charged coins are removed from circulation with no record. Crash-recovery hardening, not a 9.2 happy-path bug. [server/internal/match/reconcile.go:91]
- **`GetBalances` post-credit read is a separate unlocked transaction** — `settleMatch` credits winners in one tx (`ApplySettlement`) then reads balances for the `newBalance` field in a second, unlocked read; a concurrent wallet write (daily-login grant, another match) landing in the gap makes the `newBalance` pushed to the client not equal `previousBalance + coinDelta`. Self-correcting on the next authoritative read. Spec Task 3 explicitly offered the separate-read option. [server/internal/match/settlement.go:47]
- **Natural match-end with nil `WinnerTeam` defaults to TeamA and credits it the pot** — `handleMatchEnd` sets `winnerTeam = 0` when `finalState.WinnerTeam == nil`, and `settleMatch` trusts the passed team unconditionally (no nil/no-winner sentinel). Defensive-only today (the engine always sets a non-nil winner at every match-end transition); would mis-award the pot if a future draw/error state reaches `PhaseMatchEnd`. [server/internal/match/live_match.go handleMatchEnd]
- **No maximum on `coinBuyIn`; `pot = numHumans × buyIn` can overflow the `INTEGER` column at extreme stakes** — AC1 mandates "min 0, no maximum (owner freedom)", so the create path only rejects negatives; a near-`MAX_INT` buy-in overflows `pot` (and the 32-bit Postgres `INTEGER` match columns) in `computeSettlement`. Practically gated by the start-time affordability charge (a player can't be charged more than they hold). Revisit if a sane cap is desired despite AC1. [server/internal/match/settlement.go:113]

## Deferred from: rules-page Belote/Bela instant-win declaration (2026-06-14)

- **Implement the "Belote / Bela" instant-win rule for ANY suit (all eight cards of one non-trump suit)** — the rules content (`client/src/features/rules/content/shared.ts` `DECLARATIONS_BASE` id `bela`, 1001 pts, + the `bela` strings in `en/hr/mk/sr.ts`) carries this entry. **Correction 2026-08-21 (Story 12.9):** the original wording of this entry — "there is no engine support and the rules page intentionally describes it without it being playable" — was FALSE for the trump-suit case and stayed false through two stories. `scoring.go:228` `checkInstantWin` IS implemented and IS live from both `bidding.go:190` (post-pick) and `scoring.go:219` (post-deal): a hand holding all eight cards of the **trump** suit ends the match on the spot. Only the ANY-suit generalisation is unimplemented, and Story 12.9 rewrote the rules copy in all four locales to describe the trump-suit rule the engine actually enforces. What remains deferred is therefore narrower than this entry originally claimed. Implementation needs: (1) **detect** a hand of all 8 cards in one NON-TRUMP suit — at deal time, once the full 8-card deal completes; the trump-suit case is already detected and needs nothing; (2) **award** the holding player's team an immediate **1001-point win and stop the match at once** — a dedicated end-of-match path distinct from normal hand scoring; (3) decide the open product questions before coding: variant applicability (Bitola vs Croatian), precedence vs. other game-end/scoring paths, the WS event(s) + reveal UI to announce it, and interaction with the **501-point match mode** (instant win regardless of the configured target?). Naming note: the chosen name overlaps the existing trump-pair declaration ("Belote-Rebelote" / "Bela-Rebela" / "Бељот-Ромбељот") in every locale — confirm desired final naming with product before implementing. [server/internal/game (deal/declarations/scoring), client rules content `bela`]
  - **Status 2026-08-18 (bmad-correct-course), as written then:** REMAINS DEFERRED, and the same treatment applies to **both** variants. The Croatian rules audit confirmed Croatian also treats all eight cards of one suit as an automatic win, but since it is not implemented for Bitola either — it exists only as rules-page text — Epic 12 deliberately does NOT build it. Statistically rare enough not to spend the engine work now. Note this is DISTINCT from `checkInstantWin`, which IS implemented and fires when a player holds all eight cards of the **trump** suit. The open product questions above (variant applicability, precedence, naming collision with the K+Q trump bonus) are unchanged.
  - **Revision 2026-08-21 (Story 12.9):** the 2026-08-18 bullet above is preserved verbatim as the record of that decision, but read it with the heading's correction in mind: "it exists only as rules-page text" was true only of the ANY-suit rule. The trump-suit rule has engine support and always did. Story 12.9 rewrote the locale copy to describe the trump-suit rule, so the rules page no longer documents anything unimplemented, and it made no claim about a point award — `checkInstantWin` names a winner and never touches `TeamScores`. Still deferred: the any-suit generalisation, unchanged in substance and still carrying the same open product questions.

## Deferred from: code review of spec-auth-page-language-selector (2026-05-23)

- **Post-login PATCH `/users/:id/preferences` is awaited before `navigate("/lobby")`** — a slow or hanging preferences endpoint will block the redirect with no loading indicator or timeout. Spec leaves async semantics unspecified; UX would benefit from making the reconcile fire-and-forget so navigation never waits on it. Surfaced by adversarial + edge-case reviews. [client/src/features/auth/LoginPage.tsx handleSubmit]
- **Quadruple source-of-truth for the supported-language set grew to five sites** — adding `client/src/shared/i18n/i18n.ts` `SUPPORTED_LANGUAGES`, `client/src/shared/components/AuthLanguageSelector.tsx` `languages`, and `server/internal/auth/handler.go` `user.IsSupportedLanguage` call on top of the four existing sites (LanguageSelector, SettingsDialog, user/handler.go map, error message). Adding a 6th locale touches 5+ places with no enforcement. Folded into the existing deferred cleanup from 10-1.
- **PATCH `/users/:id/preferences` does not carry an `AbortController` in the login reconcile path** — if the user navigates away (or logs out as a different user) before the PATCH resolves, the response is still processed by the same closure. The id-guard prevents resurrection, but the request itself completes against the server. UX-tolerant rather than a bug. [client/src/features/auth/LoginPage.tsx handleSubmit]

## Deferred from: code review of 10-1-macedonian-and-croatian-translations (2026-05-09)

- **Quadruple source-of-truth for the supported-language set** — `server/internal/user/handler.go` (supportedLanguages map), `client/src/shared/components/LanguageSelector.tsx` (LANGUAGES const), `client/src/features/game/components/SettingsDialog.tsx` (LANGUAGES const), `client/src/shared/i18n/i18n.ts` (resources map), and `server/internal/apperr/errors.go:52` (error message string). Adding a 5th locale touches 4+ places with no enforcement. Cleanup, not a current bug.
- **`MatchRow` toggle hint nests block-level `<div>`s inside `<button>`** — Round-3 added a chevron + "Show / Hide hand details" hint inside the existing `<button>` element. Multiple block-level wrappers inside `<button>` is non-canonical HTML5 and may produce verbose announcements on screen readers. Defer to a dedicated a11y/semantic-HTML pass. [client/src/features/profile/MatchHistory.tsx:287-300]
- **`SettingsDialog` `i18n.language === lang.code` radio comparison brittle to region tags** — Latent: with current `lng: "en"` literal bootstrap and server-validated bare codes, region tags never appear. If a future locale-detection plugin is added, `"en-US" !== "en"` would leave no radio selected even when EN is active. [client/src/features/game/components/SettingsDialog.tsx:101]
- **`useAuthInit` does not validate `res.languagePreference` against the registered i18n resources map** — Server validation is the gate today. If DB rows ever escape via schema rollback or out-of-band write, `i18n.changeLanguage("de")` would set `lng = "de"`, fall back to EN content, and display a `"DE"` badge with no radio selected. [client/src/shared/hooks/useAuth.ts:47]
- **`HandResultsTable` empty-state could flash during background data refresh** — Round-3 changed `null` return → "No hand details for this match" message when `hands.length === 0`. If `hands` is briefly `[]` during a data refresh between cached and fresh values, users see the empty-state then sudden content. UX trade-off rather than a bug. [client/src/features/profile/MatchHistory.tsx:71-76]
- **Spec body for Story 10.1 Task 1.3 not retro-edited after Round-2 MK terminology corrections** — Round-2 changelog documents `даље→пас`, `налогодавач→наддавач`, `зови→најави`, `Делач→Дилер`, but Task 1.3's bullet list still cites the original terms. Documentation hygiene only.
- **`formatLocalizedDate` has no `timeZone` parameter** — `Intl.DateTimeFormat` accepted one; the new helper does not. No regression vs current behaviour (both default to local TZ). Worth carrying as a known gap if/when match completion times need explicit UTC display. [client/src/shared/lib/formatDate.ts]
- **No native-speaker QA of MK/HR translation tone/idiom** — ~580 lines of new translations per locale; automated parity check covers structure, not linguistic correctness. Round-2/Round-3 caught several issues but a native pass would surface anything missed (e.g. MK `Дилер` vs `Делач`, HR `lobi` vs `predvorje` consistency).

## Deferred from: code review of spec-fix-prompt-timer-no-reset-on-decision (2026-05-08)

- **Action-error path resets the active player's timer to a full duration** — `manager.go:184-188` calls `setTurnExpiry(session, oldState)` when `ApplyAction` errors, granting the active player a fresh `now + timerDurationSec`. Pre-existing pattern. With the new "preserve prompt budget" contract, any erroring action (illegal play, wrong-seat `announce_belot`, exhausted `surrender_request`, etc.) now becomes an asymmetric escape hatch from the doomed-player window. Fix scope: on action error, mirror the within-turn predicate (preserve `oldState.TurnExpiresAt` rather than calling `setTurnExpiry`). Surfaced by edge-case-hunter review. [server/internal/session/manager.go:184-188]
- **Match-end via auto-action chain carries stale `SurrenderProposerSeat` into the trailing `event:game_state`** — When a surrender request is pending and an auto-action chain ends the match naturally, `finalState.SurrenderProposerSeat` remains non-nil. The `event:match_end` payload is correctly tagged "natural"; the appended `event:game_state` may cause clients to render a stale surrender overlay alongside match-end. Pre-existing pattern (also reachable from non-auto-play match-ends). Fix scope: clear `SurrenderProposerSeat` and related fields on phase transition into `PhaseMatchEnd`, in the rules engine or on broadcast. [server/internal/session/manager.go:1115-1124]

## Deferred from: code review of 8.5-1-bug-and-race-triage (2026-05-02)

- **`handleReconnectTimeout` (abandonment match-end) violates persist-before-broadcast (D101-class, AC4-adjacent)** — `event:match_abandoned` and the trailing `event:game_state` are broadcast (reconnect.go:342-343) BEFORE `m.matchRepo.CreateWithHands` (line 369). Same antipattern AC4 just closed for `handleMatchEnd`, applied to the abandonment path. Strictly out of scope per AC4's exact wording (`event:match_end` only). [server/internal/session/reconnect.go:280-382]
- **`handleMatchEnd` `UpdateRoomStatus` failure strands room in `"playing"`** — Only `slog.Error` is emitted on `UpdateRoomStatus` failure; broadcast + `RemoveSession` continue regardless. Room ends up in `"playing"` indefinitely with no live session. Combined with the new wide-rejection `LeaveRoom` gate (`status != "waiting"` → 409 `GAME_ALREADY_STARTED`), every subsequent room operation rejects. Pre-existing pattern; pair with the AC3-gate narrowing patch when that lands. [server/internal/session/manager.go:802-815]

## Deferred from: code review of spec-timer-auto-actions-and-dialog-rings (2026-05-02)

- **`cancelTurnTimer()` does not increment `timerGeneration`** — pre-existing paper-guard race in the pause/unpause path. When `ActionPause` runs while a per-move timer goroutine is already past `time.AfterFunc` and blocked on `session.mu.Lock()`, `cancelTurnTimer()` does not bump the generation counter, so the stale handler eventually acquires the lock with a passing staleness check. Today the `default` case in `handleTimerExpiry`'s switch silently returns, so no corruption occurs — but a future patch that adds work above the switch could fire spuriously after pause. Fix scope: increment `session.timerGeneration` inside `cancelTurnTimer()` (or in the pause branch) so the staleness guard catches this case explicitly. [server/internal/session/manager.go:861-919, ws-edge-case review 2026-05-02] → Folded into Story 8.5-1

## Deferred from: code review of spec-quickplay-auto-seat-on-join (2026-04-29)

- **Auto-start broadcasts `system:game_started` even when `gameStarter.StartGame` failed** — clients navigate to `/game/{id}` for a game session that does not exist on the server. Pre-existing pattern duplicated from the `SelectSeat` auto-start branch (`server/internal/room/handler.go:670-731`). Fix scope: refactor both auto-start paths to gate the broadcast and the `playing` status flip on `StartGame` success. [server/internal/room/handler.go:1287-1298] → Folded into Story 8.5-1
- **`pickFirstEmptySeat` returning `apperr.ErrRoomFull` is reachable on `player_count` counter drift** — the QuickPlay retry loop only retries on `ErrRoomCodeTaken`/`ErrRoomNameTaken`, so a 5-seat-occupied room caused by counter drift surfaces as an opaque 5xx instead of falling back to a different/new room. Pre-existing counter-drift gap (Story 2.5 review explicitly deferred this). [server/internal/room/handler.go:1126-1135] → Folded into Story 8.5-1 (symptomatic fix; D29 root cause stays Phase 2)
- **`LeaveRoom` does not gate on `room.Status == "waiting"` under lock** — a leave can race the auto-start tx and `gameStarter.StartGame` runs with a `seatInfo` containing a player who has already left. Same race exists for the existing `SelectSeat` auto-start; this change adds a second exposure point. Fix scope: re-check `room.Status == "waiting"` in `LeaveRoom`'s tx, or serialize start/leave via an explicit lock. [server/internal/room/handler.go:443-542] → Folded into Story 8.5-1

## Deferred from: code review of 1-1-project-scaffold-and-development-environment (2026-04-10)

- **W1: fetchClient 401 handler doesn't implement refresh-then-retry cycle** — **RESOLVED in Story 1.3** — fetchClient now implements 401 → refresh → retry with `_isRetry` guard and concurrent refresh deduplication.
- **W2: apperr.Wrap wraps raw error instead of AppError** — **RESOLVED in mid-Phase 1 cleanup** — `Wrap` now wraps the `appErr` (not raw `err`), so `errors.As` correctly matches the AppError after wrapping.
- **W3: ErrorBoundary "Try again" can loop on deterministic errors** — **RESOLVED in mid-Phase 1 cleanup** — "Try again" now calls `window.location.reload()` for a clean restart. Added `resetKey` prop for route-change auto-recovery.

## Deferred from: code review of 1-2-user-registration (2026-04-10)

- **D1: Token lost on page refresh** — **MITIGATED in Story 1.3** — No Zustand persist middleware, but `useAuthInit()` calls refresh endpoint on mount using httpOnly cookie. Session survives refresh with brief unauth flash. Design intent met.
- **D2: fetchClient hard redirect on 401 breaks SPA UX** — **RESOLVED in Story 1.3** — Uses `setAuthRedirect()` callback pattern with React Router `navigate("/login")` instead of `window.location.href`.
- **D3: Default JWT secret warning-only** — **RESOLVED in mid-Phase 1 cleanup** — Config now calls `os.Exit(1)` in non-development environments when JWT secret is default.
- **D4: No rate limiting on registration endpoint** — **ACCEPTED for Phase 1** — Infrastructure-level concern. Address when adding API gateway or rate-limiting middleware.
- **D5: Refresh token Secure:true hardcoded** — **RESOLVED in Story 1.3** — `Secure: h.env != "development"` in `auth/handler.go` makes it environment-aware.

## Deferred from: code review of 1-3-user-login-and-session-persistence (2026-04-11)

- **D6: Logout fire-and-forget can orphan refresh cookie server-side** — **ACCEPTED for Phase 1** — By-design fire-and-forget. Server-side revocation requires DB session store (Phase 2).
- **D7: Refresh tokens stateless with no server-side revocation** — **ACCEPTED for Phase 1** — Requires DB session store or token-version column. Planned for Phase 2.
- **D8: Same signing secret for access and refresh tokens** — **ACCEPTED for Phase 1** — Audience claim differentiates. Separate keys planned for Phase 2 defense-in-depth.
- **D9: Mutable `var` AppError sentinels shared across goroutines** — **ACCEPTED** — Go `var` pointer sentinels are idiomatic for `errors.Is` matching. No code path mutates them; enforced by code review convention. Changing to functions would break all `errors.Is` usage.
- **D10: `apperr.Wrap()` wraps raw error instead of AppError** — **RESOLVED in mid-Phase 1 cleanup** — Same as W2; `Wrap` now wraps the AppError.
- **D11: Default JWT secret only warns, doesn't abort in production** — **RESOLVED in mid-Phase 1 cleanup** — Same as D3; `os.Exit(1)` in non-development environments.
- **D12: No Unicode normalization on email addresses** — **RESOLVED in mid-Phase 1 cleanup** — Added `norm.NFC.String()` normalization in both login and registration email processing paths.

## Deferred from: code review of 1-4-basic-player-profile-and-navigation-shell (2026-04-11)

- **D13: Duplicate `getUserID` helper diverges from `auth.GetUserID`** — **ACCEPTED** — Import cycle (`auth` imports `user`) prevents `user` from importing `auth.GetUserID`. Duplication is intentional and idiomatic Go. Both implementations are identical.
- **D14: `i18n.language` region subtag comparison** — **ACCEPTED** — Project only configures `"en"` and `"sr"` as supported locales with no region subtags. Will only break if locale detection plugins are added.
- **D15: Path parameter `:id` accepts 0 as valid user ID** — **RESOLVED in mid-Phase 1 cleanup** — Both `GetProfile` and `UpdatePreferences` now reject `paramID == 0` as `ErrBadRequest`.

## Deferred from: code review of 2-1-create-room-and-room-configuration (2026-04-11)

- **D16: FindByID returns (nil, nil) for missing rooms** — **ACCEPTED** — Established project pattern (FindByCode uses same convention). All callers check `room == nil` after nil-error return. Changing would break existing call sites.
- **D17: No rate limiting on room creation endpoint** — **ACCEPTED for Phase 1** — Infrastructure-level concern. Address when adding API gateway or rate-limiting middleware.
- **D18: No per-user active room count cap** — **ACCEPTED for Phase 1** — Business rule not specified. Address when product defines a limit.

## Deferred from: code review of 2-2-browse-and-search-rooms (2026-04-11)

- **D19: `handleWsMessage` uses unsafe `as` casts on raw `JSON.parse` output** — **RESOLVED in Story 4.1** — All `as` casts in `useWsDispatch.ts` are guarded by `if (type === CONSTANT)` discriminant checks.

## Deferred from: code review of 2-3-join-room-and-room-lobby (2026-04-11)

- **D20: WS events system:player_joined and system:player_left are TODO-only** — **RESOLVED in Story 4.7** — Server broadcasts both events in `room/handler.go`; client dispatches via `roomLobbyStore`.
- **D21: Copy Link copies room code, not a full URL** — **ACCEPTED** — Spec says "code/link"; current behavior matches intent for WhatsApp/Viber sharing.
- **D22: Orphan room_players rows for soft-deleted rooms** — **ACCEPTED for Phase 1** — No functional impact. Cleanup script planned for Phase 2.
- **D23: No unit test for useRoomLobbyUpdates handler logic** — **RESOLVED** — `useRoomLobbyUpdates.ts` superseded by `roomLobbyStore` + `useWsDispatch`; tests exist in `roomLobbyStore.test.ts` and `useWsDispatch.test.ts`. Dead file deleted in mid-Phase 1 cleanup.

## Deferred from: code review of 2-4-team-assignment-and-game-start (2026-04-11)

- **D24: `useRoomLobbyUpdates` hardcodes `id: 0` for joined players** — **RESOLVED** — Fixed in active code (`useWsDispatch.ts` uses `id: payload.userId`). Dead file `useRoomLobbyUpdates.ts` deleted in mid-Phase 1 cleanup.
- **D25: `onRoomUpdated` uses unsafe `as unknown as Room` cast** — **RESOLVED** — Dead file `useRoomLobbyUpdates.ts` deleted in mid-Phase 1 cleanup. Active code uses proper `roomLobbyStore` dispatch.

## Deferred from: code review of 2-5-quick-play-matchmaking (2026-04-11)

- **D26: TOCTOU race on FindPlayerRoom check before transaction** — **ACCEPTED for Phase 1** — Pre-existing pattern. Mitigated by low scale; DB unique constraints catch concurrent duplicates.
- **D27: Only 4th-seat player receives gameStarted notification** — **RESOLVED in Story 4.7** — Server broadcasts `system:game_started` via WS to all room participants. HTTP response `gameStarted: true` also returned to all.
- **D28: Client abort does not cancel server-side room join/creation** — **ACCEPTED for Phase 1** — Server completes the operation; user recovers via ALREADY_IN_ROOM on retry.
- **D29: player_count denormalized counter drift risk** — **ACCEPTED for Phase 1** — Counter tracked via atomic increment/decrement in transactions. Drift risk is low at Phase 1 scale; refactor to subquery count in Phase 2 if drift observed. → Symptom mitigated by Story 8.5-1; root cause stays accepted-for-Phase-2
- **D30: No TTL/cleanup for abandoned Quick Play rooms** — **ACCEPTED for Phase 1** — Lobby disconnect handler marks empty rooms as "completed". Background cleanup job for stale rooms planned for Phase 2.
- **D31: RoomLobby has no real-time refresh for other players' actions** — **RESOLVED in Story 4.7** — RoomLobby now uses `roomLobbyStore` with real-time WS dispatch for `player_joined`, `player_left`, `seat_updated`, `game_started` events.

## Deferred from: code review of 3-1-game-state-types-card-encoding-and-deck (2026-04-12)

- **D32: Duplicate playerID validation missing in `NewGame`** — **ACCEPTED** — Validation at boundary: session manager receives players from room handler which enforces unique DB users in seated positions. Pure game layer stays validation-free by design.
- **D33: Zero UserID (uint 0) accepted silently in `NewGame`** — **ACCEPTED** — Same as D32; GORM auto-increment guarantees DB IDs start at 1. Room handler validates players before session creation.

## Deferred from: code review of 3-2-trump-bidding-bitola-variant (2026-04-12)

- **D34: cloneGameState does not deep-copy pointer fields (TrumpCandidate, TrumpSuit, TrumpCallerSeat, etc.)** — After shallow struct copy, pointer fields like `*Card`, `*Suit`, `*int` are shared aliases between original and clone. Currently safe because code always assigns new pointers rather than writing through existing ones. Will become a real aliasing bug when play-phase handlers (Stories 3.3-3.6) are added if they write through pointer fields. Fix by deep-copying pointer fields in cloneGameState when adding card play logic. **RESOLVED in Story 3.3** — all 6 pointer fields now deep-copied.

## Deferred from: code review of 3-3-card-play-and-trick-resolution (2026-04-12)

- **D35: `legalCards` dereferences `TrumpSuit`/`LeadSuit` without nil guards** — **RESOLVED in mid-Phase 1 cleanup** — Added nil guard returning full hand if `TrumpSuit` or `LeadSuit` is nil.
- **D36: `currentTrickWinnerSeat` returns -1 for empty trick → `TeamForSeat(-1)` = -1 in Go** — **RESOLVED in mid-Phase 1 cleanup** — `isOpponentWinning` now returns `false` for empty trick or nil TrumpSuit, preventing the -1 sentinel from reaching `TeamForSeat`.
- **D37: No bounds check on `action.PlayerSeat` before array index** — **RESOLVED in mid-Phase 1 cleanup** — `handlePlayCard` now validates `action.PlayerSeat` is in range [0,3] before any array access.

## Deferred from: code review of 3-6-match-completion-and-special-conditions (2026-04-12)

- **D38: Nil TrickWinnerSeat dereference in `scoreHand()`** — **RESOLVED in mid-Phase 1 cleanup** — Added nil guard with early return in `scoreHand()` before dereferencing `TrickWinnerSeat`.

## Deferred from: nav bar user avatar addition (2026-04-12)

- **D39: AppLayout tests use `className` string-contains assertions for active tab styling** — **RESOLVED in mid-Phase 1 cleanup** — Tests now use `toHaveAttribute("aria-current", "page")` instead of `className.toContain("border-accent")`.
- **D40: No `afterEach` store reset in AppLayout tests** — **RESOLVED in mid-Phase 1 cleanup** — Added `afterEach` that resets `useAuthStore` to initial state.

## Deferred from: code review of 4-3-game-table-ui-layout-seats-and-cards (2026-04-12)

- **D41: EVENT_CARD_PLAYED cardId[0]/[1] parsing assumes 2-char format** — **RESOLVED in mid-Phase 1 cleanup** — Added bounds check: `if (!payload.cardId || payload.cardId.length < 2) return;` before accessing indices.

## Deferred from: code review of 4-4-trump-bidding-and-declaration-ui (2026-04-12)

- **D42: `playableCardIds` includes ALL hand cards with no legal-move filtering** — **ACCEPTED for Phase 1** — Server rejects illegal plays with clear error toast. Adding server-side `legalMoves` to GameState is a Phase 2 UX enhancement.
- **D43: `PlayerState.username` missing from server Go `PlayerState` struct** — **RESOLVED in mid-Phase 1 cleanup** — Added `Username string` to `PlayerState`, `PlayerSeatInfo`, and `NewGame()`. Server now populates username from room player data.
- **D44: `TrumpSelectedPayload.trumpSuit` unsafe `as` cast from `string` to `Suit | null`** — **ACCEPTED** — Handler returns early without processing the payload; cast is unreachable. The full authoritative state follows via `event:game_state`.
- **D45: `window.confirm()` for back-button interception** — **ACCEPTED for Phase 1** — Standard browser API, works across platforms. Custom modal replacement planned for Phase 2 UX polish.

## Deferred from: code review of 4-5-per-move-timer-and-auto-play (2026-04-12)

- **D46: GetStateSnapshot returns mutable pointer** — **RESOLVED in mid-Phase 1 cleanup** — Now returns a shallow copy (`snapshot := *session.gameState; return &snapshot`) so callers cannot observe concurrent mutations.
- **D47: legalCards dereferences TrumpSuit without nil check** — **RESOLVED in mid-Phase 1 cleanup** — Same as D35; nil guard added.
- **D48: Client clock skew — no server-client time sync** — **ACCEPTED for Phase 1** — Spec accepts ±1s tolerance. Server-client time sync via `serverTimeMs` field planned for Phase 2.

## Deferred from: code review of 5-1-player-pause-system (2026-04-13)

- **D53: `sendGameError` routes ALL game errors as `error:invalid_action`** — **RESOLVED in mid-Phase 1 cleanup** — `sendGameError` now uses `errors.Is` dispatch to map `apperr` sentinels to typed WS error events (`error:not_your_turn`, `error:wrong_phase`, `error:illegal_play`, `error:pause_exhausted`, `error:no_active_pause`, `error:not_room_owner`, `error:player_disconnected`).
- **D54: Disconnect while paused leaves game frozen** — **RESOLVED in Story 5.3** — `HandleDisconnect()` in `reconnect.go` auto-clears disconnected player's pause and resumes game if no other pauses remain.

## Deferred from: code review of 4-6-score-panel-score-reveal-and-match-flow (2026-04-13)

- **D49: LastHandResult not cleared in startNewHand()** — **ACCEPTED** — `LastHandResult` intentionally persists through `startNewHand()` so the session manager can broadcast it. Overwritten by the next `scoreHand()` call. Client ignores it outside score reveal flow.
- **D50: cloneGameState shallow-copies LastHandResult pointer** — **RESOLVED in mid-Phase 1 cleanup** — `cloneGameState` now deep-copies `LastHandResult` pointer field.
- **D51: matchDurationSec includes session setup time** — **ACCEPTED for Phase 1** — Overstates actual play time by including lobby/deal/bidding. Acceptable approximation.
- **D52: Second hand_scored during reconnect while overlay active loses that hand's score reveal** — **ACCEPTED for Phase 1** — Edge case only during reconnection. The overlay guard prevents UI glitches; the missed score data is cosmetic (match scores are correct). Overlay queue for Phase 2.

## Deferred from: code review of 4-7-room-lobby-websocket-wiring (2026-04-13)

- **D53: `userId` used as `RoomPlayer.id` field in WS dispatch** — **ACCEPTED** — DB row ID not available from WS event; `userId` serves as a unique client-side identifier. No API calls use this `id` field.
- **D54: Dead code `useRoomLobbyUpdates.ts` with known bugs** — **RESOLVED in mid-Phase 1 cleanup** — File deleted. Active code in `useWsDispatch.ts` + `roomLobbyStore` has no known bugs.
- **D55: `project-context.md` not updated for 5th Zustand store** — **RESOLVED in mid-Phase 1 cleanup** — Updated to list 5 partitioned stores including `roomLobbyStore`.

## Deferred from: code review of 5-2-room-owner-pause-override (2026-04-13)

- **D56: Room ownership transfer during active game not synced to session** — **ACCEPTED for Phase 1** — OwnerSeat fixed at game start by design. Owner must be seated to start game, and leaving during game is not a normal flow.
- **D57: No integration test for broadcast `OwnerOverride: true`** — **ACCEPTED** — Low priority test gap. Broadcast code is straightforward and covered by manual testing.
- **D58: No WS-layer test for `error:not_room_owner` event delivery** — **ACCEPTED** — D53 fix enables proper dispatch. Integration test is low priority.

## Deferred from: code review of 5-3-disconnect-detection-and-reconnect-countdown (2026-04-13)

- **D59: `username` field in `PlayerDisconnectedPayload` is always empty** — **RESOLVED in mid-Phase 1 cleanup** — `HandleDisconnect` now reads `gs.Players[seat].Username` (populated via D43 fix).
- **D60: `ERROR_PLAYER_DISCONNECTED` unreachable on wire** — **RESOLVED** — D53 fix dispatches `error:player_disconnected` via `errors.Is(err, apperr.ErrPlayerDisconnected)`.
- **D61: `startNewHand` does not reset disconnect fields** — **RESOLVED in mid-Phase 1 cleanup** — `startNewHand()` now resets `DisconnectedSeat = -1` and `ReconnectExpiresAt = nil`. `HandleReconnect()` also clears on reconnect.

## Deferred from: code review of 5-4-reconnection-and-state-restoration (2026-04-13)

- **D62: `handleReconnectTimeout` stub leaves game in PhaseDisconnected after window expires** — RESOLVED in Story 5.5: `handleReconnectTimeout` now fully implements match abandonment (transition to `PhaseMatchEnd`, persist match record, broadcast `event:match_abandoned`, remove session).

## Deferred from: code review of 5-5-match-abandonment-on-timeout (2026-04-14)

- **D63: Page refresh during/after abandonment shows blank game page** — **RESOLVED in mid-Phase 1 cleanup** — Added `useEffect` in `GamePage.tsx` that detects stale `match_end` phase with no overlay data and redirects to lobby.

## Deferred from: auth init bug fix (2026-04-14)

- **D64: `doRefresh()` in fetchClient does not pass AbortSignal to `refresh()`** — Pre-existing. The 401 retry path in `fetchClient` calls `refresh()` without an AbortSignal. Not related to StrictMode double-mount fix. Low risk at current scale.
- **D65: GORM logger uses `log.New` (plain text) while app uses `slog` (JSON)** — Pre-existing. GORM's default logger already output plain text before configuration change. Creating a proper slog adapter is a Phase 2 observability improvement.

## Deferred from: code review of fix-auth-routing-and-refresh (2026-04-16)

- **D66: Stale `gameState` in gameStore on re-login after session expiry** — Pre-existing. When 401 interceptor calls `authStore.logout()`, it clears token and user but does not reset `gameStore`. If the user logs in again, `useReconnectionRedirect` sees stale game state and may redirect to a game page for a game that has ended server-side. Low risk: server will reject actions on ended games. → Folded into Story 8.5-1

## Deferred from: code review of declaration-reveal-cards-and-timing (2026-04-17)

- **D67: DeclarationReveal anchors panel to `declarations[0].playerSeat`** — If the server ever broadcasts declarations from two teammates on the winning team (e.g. seat 0 has a tierce and seat 2 has a different sequence), the panel anchors to the first entry only. Current spec and bitola dedup rules imply a single declarer wins the clash, so this is theoretical. Revisit if multi-teammate broadcasts become possible (e.g. Croatian variant where both teammates' declarations can survive). **PROMOTED 2026-08-18 to Story 12.5 (Croatian Declaration Overlap)** — that moment has arrived and it is no longer theoretical. Croatian allows a card to count in more than one declaration (confirmed 2026-08-18), so a SINGLE player can hold two surviving melds, on top of the two-teammates case. Closing D67 is an explicit AC of Story 12.5. **RESOLVED in Story 12.5** — by verification, no code change needed: the reveal already maps every declaration in the payload and sums them all, `declarations[0]` survives only as an emptiness guard, and there is nothing left to anchor because the panel was deliberately centred (its `PANEL_POSITIONS` table is gone and it no longer uses `compassOffset`, which is still live for the trick layout). Multi-meld rendering was already covered by a two-teammates test and a one-seat two-melds test; 12.5 consolidated the latter into an exact-count regression (both rows, the shared card once per row, two declarer rows with the right seat, total = the sum) and added the server-side proof that a single seat can hold two surviving melds. The broadcast helper already emits one flat entry per meld, each carrying its own seat; the extended contract golden pins the payload/schema shape for that case but not the producer loop, which stays covered by the game-package tests.

## Deferred from: code review of belot-rebelot-prompt-and-reveal (2026-04-17)

- **D68: `prefersReducedMotion` snapshotted once via `useMemo`, not reactive** — `BelotReveal.tsx` and `DeclarationReveal.tsx` both read `window.matchMedia('(prefers-reduced-motion: reduce)').matches` once at mount and never subscribe to the `change` event. If a user toggles the OS-level reduce-motion setting mid-session, subsequent reveals use the stale value until the next page load. Shared pattern — fix once by introducing a `useReducedMotion` hook that subscribes to media-query changes. → Folded into Story 8.5-3
- **D69: Belote reveal lost on reconnect during the 4s window** — Server's `event:belot_announced` fires once on announcement. On reconnect, the server sends `event:game_state` (which carries `belotAnnounced: true`) but does not replay the cardId-bearing reveal event. A user refreshing during the 4s reveal window — or a late-joining spectator — never sees the overlay. Spec treats the reveal as purely visual so the gap is acceptable; revisit if spectators are added or if reveal persistence becomes important (would need `belotAnnouncedCardId` carried in `GameState`). → Folded into Story 8.5-3

## Deferred from: code review of fix-declaration-reveal-broadcast-missing (2026-04-17)

- **D70: Tie-bias in declaration-winner derivation** — `manager.go`'s `broadcastDeclarationsResolvedIfTransition` (and the original inline block it replaced) reports the winner by `DeclarationPoints[Red] > 0 ? Red : (Blue > 0 ? Blue : null)`. If both teams somehow have declaration points simultaneously, Red wins silently. Pre-existing bias; the game-layer `resolveDeclarations` is supposed to zero the losing team, so this is defensive only. Revisit if game-layer invariants weaken.
- **D71: Stale `declarationReveal` surviving a mid-animation reconnect** — `useWsDispatch.ts` `setGameState` does not reset `declarationReveal`. If a client disconnects during the 4s reveal window and reconnects quickly, the store still holds the prior payload and the overlay re-renders on top of the reconnected game state. Low-severity UX edge. Fix by resetting reveal fields inside `setGameState` on reconnect, or by clearing them in the reconnect flow specifically. → Folded into Story 8.5-3

## Deferred from: code review of 6-1-global-lobby-chat (2026-04-18)

- **D72: Server-side sanitization of chat text + username** — control characters, RTL overrides, and zero-width joiners are broadcast verbatim. JSX auto-escapes XSS, but malicious unicode can disrupt the chat list rendering. Phase 1 chat is intentionally unmoderated per Dev Notes; revisit when chat moderation lands in Phase 2. [server/internal/chat/handler.go:96-107]
- **D73: WS router dispatches actions via `go r.ActionHandler(...)` without per-client serialization** — two concurrent chat messages from the same client can be processed out of submission order, producing out-of-order timestamps. UI renders by append order. Pre-existing pattern affecting all action handlers; not introduced by Story 6.1. [server/internal/ws/router.go:19]
- **D74: Lobby right-column `min-h-150` (≈600px) forces page-level scroll on viewports below 600px tall** — breaks the nested chat auto-scroll because `scrollIntoView` scrolls the page instead of the chat list. UX spec targets 1280×720 minimum viewport, so out of scope; revisit if mobile/small-viewport support is added. [client/src/features/lobby/LobbyPage.tsx:169]
- **D75: `chat.handleGlobal` collapses transient DB error and "user deleted" into the same silent drop** — sender gets no feedback in either case, so a transient failure permanently loses the message with no retry. Asymmetric log payload (`error=<nil>` vs real error). Phase 1 chat has no retry/error UX surface; revisit alongside moderation work. [server/internal/chat/handler.go:96-100]
- **D76: `i18n.test.ts` does not assert key parity between `en.json` and `sr.json`** — `chat.*` keys are in parity manually but adding a key to one file but not the other would not fail any test. Pre-existing infrastructure gap not introduced by this story. [client/src/shared/i18n/i18n.test.ts]
- **D77: No end-to-end test covering sender's own-message echo through a real `*ws.Hub`** — handler tests use a hub spy; the real `*ws.Hub.BroadcastToUsers` path is exercised by `ws_test.go` but not in conjunction with the chat handler. Unit coverage is comprehensive (11 handler tests); integration test would duplicate `ws_test.go` machinery. [server/internal/chat/handler_test.go]

## Deferred from: code review of 6-2-match-scoped-chat (2026-04-18)

- **D78: Race between `session.Manager.MatchParticipants` and `Hub.BroadcastToUsers`** — Participants are read under `RLock` and released before broadcast. If `RemoveSession` runs between the read and the broadcast (e.g. match-end teardown racing with an in-flight match chat send), the broadcast fans out to player IDs whose session is already gone. Consequence is mild: a late match chat reaches players who just returned to the lobby, reseeding their (already cleared) `matchMessages`. Fix requires the session manager to own the broadcast atomically under the write lock — architectural change. Systemic concurrency concern shared with other session broadcasts; revisit alongside chat moderation work in Phase 2. [server/internal/chat/handler.go:130-178]
- **D79: No rate limiting on match chat** — a participant can flood the 3 other players' client queues at WS ingress rate. No per-user cooldown, token bucket, or flood check. Same pre-existing gap as D72 for global chat; Phase 1 chat is intentionally unmoderated. Revisit when chat moderation lands in Phase 2. [server/internal/chat/handler.go:130-178]
- **D80: `RFC3339Nano` server timestamps provide nanosecond precision but no monotonic ordering guarantee under NTP clock corrections** — messages sorted by timestamp may misorder on backward clock jumps. Systemic across all server-stamped events (timers, disconnects, declarations), not specific to chat. Fix would require a monotonic per-session sequence number carried alongside the timestamp. Project-wide concern not introduced by this story; out of scope. [server/internal/chat/handler.go:161]
- **D81: Concurrent `action:chat_message` goroutines per client can interleave** — same pre-existing router pattern that Story 6.1 deferred as D73. A sender's two rapid-fire messages may reach peers out of submission order. Fix would be a per-client serialization layer at the router. Router-level change out of scope for a chat story. [server/internal/ws/router.go:19]

## Deferred from: code review of 7-1-match-history-display (2026-04-19)

- **D82: Offset-based pagination duplicates / skips rows on concurrent match completions** — a new match completing between page 1 and page 2 shifts rows, causing the same `match.id` to appear on two pages → React duplicate-key warnings in `useInfiniteQuery`'s `flatMap`. Acceptable for Phase 1's low match-completion rate; revisit with cursor pagination `(completed_at, id)` or client-side dedupe by match id. Pre-existing offset-pagination pattern across the codebase. [client/src/shared/hooks/queries/useMatches.ts, server/internal/match/gorm_repo.go `GetMatchesForUser`]
- **D83: `openIds` Set accumulates entries for matches no longer in the visible list** — tiny memory retention, no functional break. Prune against current `items` on each render, or use `Map<number, boolean>` with pagination-aware pruning. Cosmetic micro-optimisation. [client/src/features/profile/MatchHistory.tsx:344]
- **D84: `Load more` button stays enabled during background refetch** — only `isFetchingNextPage` disables the button; a click while page 1 is refetching fires `fetchNextPage` against a stale `total`. Rare in practice (react-query `staleTime` guards). Also disable on `query.isFetching`. [client/src/features/profile/MatchHistory.tsx:413-425]
- **D85: `loadUsernamesForMatches` returns empty string for soft-deleted users** — row renders with `teammate=""` / empty opponent name instead of a "Deleted user" placeholder. UX polish only; no data leak. Revisit alongside Epic 11 public profiles. [server/internal/user/handler.go:249-273]
- **D86: `strconv.ParseUint(..., 10, 64)` cast to `uint` can truncate on 32-bit builds** — `uint(paramID)` drops high bits on 32-bit platforms; a foreign `:id = 4294967297` could equate to auth user `1` and bypass the 403 check. Mirrors the existing `GetProfile` pattern. Systemic fix: compare `uint64` to `uint64` everywhere, or enforce 64-bit build. Pre-existing. [server/internal/user/handler.go:181-185] → Folded into Story 8.5-2 (comparison fixed; 32-bit build guard added).

## Deferred from: code review of 7-2-expanded-player-profile (2026-04-19)

- **D87: No integration test exercises the real `FILTER … CASE` SQL** — `TestGetProfile_StatsMatchListTotal` asserts the `totalGamesPlayed == /matches.total` invariant at the mock level (`handler_test.go:82-116`), not against the Postgres Raw query in [gorm_repo.go:34-74](server/internal/match/gorm_repo.go#L34-L74). A typo or NULL-semantics mismatch in the real SQL would not fail any test. Requires a Postgres-backed repo test harness, which this project does not have today; pre-existing infrastructure gap.
- **D88: Stats / match-list status-filter desync risk** — both `GetStatsForUser` (FILTER clauses only cover `completed` + `abandoned`) and `GetMatchesForUser` (`WHERE status IN (completed, abandoned)`) independently hardcode the same two status values. If a future story introduces `in_progress` / `cancelled` / `forfeited` etc. and updates only one side, `totalGamesPlayed` silently drifts from the `/matches` list total, violating the AC #2 invariant. No current bug; future-proof flag. [server/internal/match/gorm_repo.go:46-62, :78]

## Deferred from: code review of spec-room-scoped-chat (2026-04-22)

- **D89: TOCTOU / two unsynchronised DB reads in `chatRoomMembership.RoomMembers`** — `FindByID` and `FindPlayersByRoomID` run outside a transaction, so `startGame` (or a concurrent leave/join) flipping state between the two reads produces a member list that doesn't reflect the authoritative room state at any single moment. Consequence is mild: a just-left player may receive one extra message; a just-joined player may miss one. Identical shape to the match-chat race deferred as D79. Fix would join `rooms.status = 'waiting'` into a single read or wrap both reads in `repo.RunInTransaction`. Systemic concurrency concern shared with other session broadcasts. [server/cmd/api/main.go:180-204]
- **D90: `ChatMessagePayload` has no `roomId` / `matchId` — client cannot verify scope identity** — the dispatcher's `currentRoomId !== null` guard prevents leaks into a cleared store but cannot distinguish "frame from room A" vs "frame from room B" when both are momentarily active. Server-authoritative recipient list protects today. Adding `roomId?` + `matchId?` to the payload lets the client drop foreign-origin frames defensively — mirrors the `room_player_*` event identity-check pattern at `useWsDispatch.ts:264,283,291,299`. Cross-scope improvement; touches all three scopes and both contract files. [client/src/shared/types/wsEvents.ts:266-273, server/internal/ws/events.go:128-135]
- **D91: Server accepts `RoomID = 0` on room-channel chat** — unlike the client (`ChatPanel.tsx` rejects non-positive integers), the Go handler only nil-checks `RoomID`. A request with `"roomId": 0` falls through to `RoomMembers(0)` which silently returns `(nil, false)` via the repo's not-found path. No exploit, but adds a pointless DB query on malformed input and diverges from client validation. Trivial fix: reject `*req.RoomID == 0` before the lookup. [server/internal/chat/handler.go:197-200]
- **D92: No client test for foreign-room isolation** — Spec AC "third player in a different room does not receive" is covered transitively by the server-side recipient filter (`TestHandler_RoomMessage_ExcludesNonMembers`). A dedicated client test would require either adding `roomId` to the payload (see D90) or a multi-room render harness. Tracked for after D90 lands so the discriminator exists to test against.
- **D93: No per-user rate limiting on chat sends** — all three scopes accept WS frames at ingress rate, so a compromised client can fan out two DB queries per frame on room chat (and broadcast storms on match/global). Matches Story 6.1 D72 / 6.2 D78 precedent: Phase 1 chat is intentionally unmoderated; revisit when chat moderation lands in Phase 2.

## Deferred from: counter-clockwise seat layout swap (2026-04-25)

- **D94: Story 4.3 spec contains inverted compass labels** — `_bmad-output/implementation-artifacts/4-3-game-table-ui-layout-seats-and-cards.md` line ~280 still describes turn order as "South→West→North→East, i.e., counter-clockwise around the table". After the layout correction, compass index 1 maps to East (right) and 3 maps to West (left), so the visual order is actually South→East→North→West — which is the geometrically correct counter-clockwise rotation when viewing the table from above. The spec doc's labels are stale; the code is now authoritative. Update doc when next touching Story 4.3. → Folded into Story 8.5-3

## Deferred from: code review of dealer-indicator-and-trump-caller-name (2026-04-26)

- **D97: DealerIndicator stays visible behind/over `MatchResult`, `ReconnectOverlay`, and disconnected-phase overlays** — The dealer pill is gated only on a resolvable username (`gameState.players.find(p => p.seat === dealerSeat)?.username`), with no phase guard. During `match_end`, `disconnected`, or while a reconnect overlay is up, the badge can show through depending on each overlay's z-index. Spec explicitly says no upper bound on visibility, so this is by design today. Revisit if overlays should hide table chrome. [client/src/features/game/GamePage.tsx:376-396] → Folded into Story 8.5-3
- **D98: No `truncate`/`max-width` clamp on dealer or trump-caller name spans** — Both `DealerIndicator` and the new caller-name span in `TrumpIndicator` render the username unclamped inside a flex pill anchored at `top-4 right-16`. A long username, RTL text, or emoji-heavy display name pushes the pill leftward into the seat/score area. No registration cap was checked in this story. Pick a UX-appropriate clamp (likely `max-w-[8rem] truncate` on the name spans plus `min-w-0` on the pill) when this lands. [client/src/features/game/components/DealerIndicator.tsx, client/src/features/game/components/TrumpIndicator.tsx] → Folded into Story 8.5-3

## Deferred from: code review of bitola-deal-flip-bid-flow (2026-04-26)

- **D95: `reshuffleAndRedeal` does not reset `BelotAnnounced` / `DeclarationsResolved` / `PendingBelotSeat`** — Pre-existing behavior surfaced by the deal-flip-bid review. If the engine ever reaches `reshuffleAndRedeal` while these flags are set (today only reachable via the bidding-double-pass path, where they are zero by construction), they leak into the next bidding round. Not exploitable today because the only caller is `handlePassTrump` and bidding never sets those flags. Fix would zero them alongside the other reset block at `bidding.go:reshuffleAndRedeal`. Defensive cleanup; no current bug. [server/internal/game/bidding.go:130-160]
- **D96: `Deck` field is broadcast in every `event:game_state` frame, exposing the 11 undealt stage-2 cards** — SUPERSEDED 2026-08-20: folded into Story 12.10 (per-recipient snapshot projection), which fixes hands and deck together. — Explicitly out of scope per the spec's `Never:` list, but the same systemic concern as opponent-hand exposure. A malicious client can read every stage-2 card before bidding ends, gaining a perfect-information advantage on whether to pick or pass. Fix requires either a per-seat broadcast DTO that strips hidden fields, or a `MarshalJSON` filter keyed off seat identity. Pairs with the latent hand-exposure issue across the WS protocol. [server/internal/game/state.go:70 + every `BroadcastToUsers` call]
  update_2026-08-21: CLOSED by Story 12.10. Neither of the two fixes sketched above was used: `GameState.Deck` is now `json:"-"` — removed from the wire OUTRIGHT rather than masked or counted, because the client had zero consumers (only a `?? []` coercion in `matchStore.ts`) and a `deckCount` would be dead weight. `z.strictObject` on the TS side means the field cannot quietly return. The event is named `event:match_state`, not `event:game_state` (this entry's name was always wrong). The hand-exposure half is fixed in the same story via `game.ProjectForSeat` — see the Story 12.1 card-privacy entry's closing update below (2026-08-21).

## Deferred from: code review of bitola-round2-trump-prompt-shows-candidate (2026-04-26)

- **D97: Round-2 trump prompt can vertically overflow on short mobile viewports** — Surfaced when the candidate `PlayingCard` was added above the round-2 suit-selection grid. Total content (title + lg card + 4 suit buttons + PASS) can exceed ~360px viewport height on mobile landscape with no `max-h` or `overflow-y-auto` on the dialog body, leaving the PASS button unreachable. Pre-existing for round 1 (candidate + PICK + PASS) but worsened by round 2's denser stack. Fix would add `max-h-[90vh] overflow-y-auto` to the inner dialog or restructure the suit grid into a 2x2 layout for very-short viewports. Cosmetic; not a blocker for the desktop path. [client/src/features/game/components/TrumpPrompt.tsx:67-119] → Folded into Story 8.5-3

## Deferred from: code review of 8-1-room-owner-pre-game-controls (2026-04-26)

- **D98: `kickedUsername` falls back to empty string on transient `FindPlayersByRoomID` error** — In `KickPlayer`, the pre-tx lookup `prePlayers, _ := h.repo.FindPlayersByRoomID(uint(roomID))` silently drops the error. On a transient repo failure, `kickedUsername` stays `""` and the `system:player_left` broadcast carries `username: ""`, rendering empty names in remaining clients' UI. Same pattern lives in the existing `LeaveRoom` handler — pre-existing convention, not regressed by this story. Fix would log the error and either fail-fast or capture the username from a different source. [server/internal/room/handler.go:766]
- **D99: `tx.FindPlayerRoom` is a global lookup, brittle to stale rows in other rooms** — `FindPlayerRoom(req.UserID)` returns the first room the user appears in across `waiting`/`playing` rooms. If a stale row exists in another room, `target.RoomID != uint(roomID)` produces `ErrNotInRoom` even though the player IS a member of the target room. A "is user X a member of room Y" query (e.g. `FindPlayerInRoom(roomID, userID)`) would be safer and reusable across `KickPlayer`, `LeaveRoom`, `SelectSeat`. Pre-existing repository surface; deferred. [server/internal/room/handler.go:793, server/internal/room/gorm_repo.go FindPlayerRoom]

## Deferred from: code review of 8-2-team-surrender (2026-04-27)

- **D100: Manager tests rely on `time.Sleep(30-100ms)` for async dispatch** — Six new surrender end-to-end tests use sleeps to wait for goroutine-driven broadcast. Flaky on loaded CI (Windows particularly). Pre-existing pattern in `manager_test.go`; the surrender additions inherit it. Replace with sync primitives (channels, `sync.WaitGroup`) or a poll-with-timeout helper across all session-manager async tests. [server/internal/session/manager_test.go] → Folded into Story 8.5-1 (new tests only; full sweep out of scope)
- **D101: `event:match_end` broadcast precedes DB persistence in `handleMatchEnd`** — Surfaced by the surrender match-end review. Same race as the natural-end path: a client receiving `match_end` and immediately calling a hypothetical match-record API would see no row yet. Pre-existing; not surrender-specific. Fix would persist before broadcasting or move persistence into the same goroutine before `BroadcastToUsers`. [server/internal/session/manager.go:handleMatchEnd] → Folded into Story 8.5-1
- **D102: `SurrenderPrompt` uses `absolute inset-0` instead of `fixed`** — Could leak through if the parent stacking context is wrong (scrolled/zoomed view). Mirrors `BelotPrompt` shape. Tests pass. Verify visual behavior on mobile and low-resolution viewports as part of UX polish; align with shadcn `Dialog` pattern used by `SurrenderButton` if needed. [client/src/features/game/components/SurrenderPrompt.tsx] → Folded into Story 8.5-3
- **D103: `SurrenderPrompt` has no Escape-key dismissal (decline)** — Standard accessibility practice for modal dialogs is Escape == cancel. The `useFocusTrap` hook does not currently bind Escape; the shadcn `Dialog` used in `SurrenderButton` handles this automatically, leaving the two prompts inconsistent. Add Escape→onDecline either in the hook or in `SurrenderPrompt` directly. Affects `BelotPrompt` symmetrically. [client/src/features/game/components/SurrenderPrompt.tsx, client/src/shared/hooks/useFocusTrap.ts] → Folded into Story 8.5-3
- **D104: `session.Manager` tests assert state-level rejection but not WS-level error broadcast** — Surrender rejection paths (non-partner accept, exhausted-after-decline, second-request-while-pending) currently verify `GetStateSnapshot` is unchanged; the spec's Task 8.2 listed the wire-level assertions on top (e.g. seat 1 receives `error:invalid_action`). The session manager takes a concrete `*ws.Hub`, so substituting a recorder requires either (a) an `interface Hub { SendToUser; BroadcastToUsers; ... }` refactor, or (b) wiring the `httptest.Server` + real WS-client pattern from `server/internal/ws/ws_test.go` into `session_test`. Either route is broader than this story. The apperr-level rejection codes are already covered by `surrender_test.go` (errors.Is matchers) and `sendGameError`'s switch table. Pick the cleanest path when the next session-manager test needs wire-level coverage. [server/internal/session/manager_test.go, server/internal/session/manager.go]

## Deferred from: center declaration dialog (2026-04-29)

- **D111: `spec-belot-rebelot-prompt-and-reveal.md` claims Belote's reveal "mirrors" the declaration reveal positioning** — After centering the declaration reveal, the two reveals diverge: declaration centers, Belote still anchors to the announcer's seat. The spec text (`_bmad-output/implementation-artifacts/spec-belot-rebelot-prompt-and-reveal.md:22`) is now stale documentation. Decide whether Belote should also center (separate request) or update the spec. Out of scope per the explicit request to center the declaration dialog only. → Folded into Story 8.5-3
- **D112: `PauseOverlay` / `ReconnectOverlay` (z-20, full-viewport `bg-background/80`) silently cover the centered declaration reveal during a mid-reveal pause/disconnect** — Pre-existing behavior; the 8s timer keeps running and the reveal is consumed behind the tinted overlay. Centering the panel makes this slightly more noticeable than the seat-anchored variant because it now sits exactly under the overlay's visual focus. Fix would queue or pause the reveal timer when an overlay is active. → Folded into Story 8.5-3

## Deferred from: code review of spec-team-us-them-and-cross-seat-layout (2026-05-02)

- **D113: `TeamStringForIndex` (Go) panics on input outside `{0, 1}`; TS `teamStringForIndex` callers collapse non-zero to `1`** — Both helpers assume a well-formed wire team index. Server-side panic in a session goroutine would crash the match. TS-side, every call site uses `(team === 0 ? 0 : 1)` which silently maps any future out-of-range value to teamB. Defensive hardening: Go variant returns `""` (or sentinel) instead of panic; TS variant accepts `number` and returns `null` for out-of-range; tighten Zod schemas to `z.union([z.literal(0), z.literal(1)])` so contract test rejects garbage at parse time. Pre-existing risk pattern, surfaced by the rename. [server/internal/game/state.go:130-145, client/src/shared/types/gameTypes.ts:45-47, client/src/features/game/components/{ScorePanel,ScoreReveal,MatchResult,DeclarationReveal,CapotAnimation}.tsx] → Folded into Story 8.5-2.
- **D114: `MatchResult` keeps physical column order (A on left, B on right) while flipping labels to Us/Them** — `ReconnectOverlay` and `MatchHistory` swap values to render viewer-first. Three components, three orderings. Pick one convention (likely viewer-always-first) and align. [client/src/features/game/components/MatchResult.tsx:30-31,73-92] → Folded into Story 8.5-3
- **D115: `TrumpIndicator` displays neutral "Team A" / "Team B" while in-game** — Every other in-game display uses Us/Them; TrumpIndicator is the outlier. Add `viewerTeam` prop and key the team-name label off `team === viewerTeam ? t('team.us') : t('team.them')`. Or document why TrumpIndicator stays neutral. [client/src/features/game/components/TrumpIndicator.tsx:47-54] → Folded into Story 8.5-3
- **D116: `OutcomeReason` Go field typed as raw `string`; Zod schema is `z.literal("surrender").optional()`** — Asymmetric strictness: TS rejects unknown values, Go accepts anything. If server emits a future `outcomeReason` value (e.g. `"timeout"`), strict Zod parse fails on the client. Fix would type `OutcomeReason` as a Go enum (`type OutcomeReason string` with constants). [server/internal/ws/events.go:121] → Folded into Story 8.5-2.
- **D117: Locale-overflow test (`RoomLobby.locale.test.tsx`) cannot fail in JSDOM** — `scrollWidth`/`clientWidth` always return 0; the comparison is trivially true. The accompanying `Tim A`/`Tim B` text-presence check covers i18n key mounting but not actual overflow. Wire the assertion into Playwright (or Vitest browser mode) to make AC-009 a real regression gate. [client/src/features/lobby/RoomLobby.locale.test.tsx] → Folded into Story 8.5-3
- **D118: `events_contract_test.go` bootstraps missing goldens silently** — CLOSED (verified 2026-08-20; the hard-fail was already in place, carrying an "AC10 / D118" comment). Left behind an unreachable write-the-golden branch after the `t.Fatalf`, since the regeneration path returns earlier; that dead code is now removed. Original text follows. — A contributor who deletes a golden gets a green test that blesses whatever the server emits at that moment. Make missing-golden a `t.Fatal` when running in CI mode (e.g. detect `os.Getenv("CI") != ""`) so only `UPDATE_GOLDENS=1` regenerates. [server/internal/ws/events_contract_test.go:264-272] → Folded into Story 8.5-3
- **D119: `RoomLobby.diamond.test.tsx` accepts either east/west assignment via `[east, west].sort() === opponents`** — A refactor that swaps east and west would silently pass. Tighten to assert `east === (viewerSeat+1)%4` and `west === (viewerSeat+3)%4` directly. [client/src/features/lobby/RoomLobby.diamond.test.tsx:185-199] → Folded into Story 8.5-3
- **D120: `DeclarationReveal.test.tsx` Us/Them assertions check `data-team` only, not visible text** — A future refactor that inverts the comparison would flip the rendered text but not the data attribute. Mock `useTranslation` and assert `toHaveTextContent("Us declared")` / `"Them declared"`. [client/src/features/game/components/DeclarationReveal.test.tsx:60-78] → Folded into Story 8.5-3
- **D121: `showSelfLabel` timer cleanup on viewerSeat change leaves label briefly anchored to old position during owner-driven seat swap** — During an owner swap that moves the viewer between seats, the existing label (`isCurrentUser && cardinal === "south" && showSelfLabel`) stays visible at the old south-mapped tile until the new tile's effect fires. Reset `showSelfLabel` to false at the top of the effect when `viewerSeat` changes, then re-set after the timeout. [client/src/features/lobby/RoomLobby.tsx:188-204]
- **D122: `prefersReducedMotion` snapshotted via `useMemo([])` doesn't react to OS-level toggles** — Same shared pattern as D68. Refactor into a `useReducedMotion` hook that subscribes to `mql.addEventListener('change', ...)` and use across `RoomLobby`, `MatchResult`, `BelotReveal`, `DeclarationReveal`, `DealAnimation`. [client/src/features/lobby/RoomLobby.tsx:183-186] → Folded into Story 8.5-3
- **D123: Empty `PlayerSeat` carries `data-team` attribute but uses neutral `border-border` instead of team color** — Pre-existing visual divergence from `RoomLobby` (which uses team-color border on empty seats). The `data-team` attribute is now rendered but unused for styling on the empty branch. Either apply team-color border or document the divergence. [client/src/features/game/components/PlayerSeat.tsx:33-43]
- **D124: Lockfile script self-exclusion is filename-based, brittle to rename or copy** — `--exclude="lockfile-old-team-tokens.sh"` matches basename. If the script is renamed or its regex copied to another file (doc, related script), that file fails CI by self-match. Replace with a marker line (e.g. `# LOCKFILE-PATTERN-EXEMPT`) and have the script skip files containing the marker. [scripts/lockfile-old-team-tokens.sh:70]
- **D125: Native-speaker review of Serbian "Tim A" / "Tim B" / "Mi" / "Oni"** — Native Beljot players typically say "naša ekipa"/"njihova ekipa" or use color names colloquially. "Tim A" reads as a placeholder transliteration. Have a Serbian Beljot player confirm the strings sound natural; substitute if not. [client/src/shared/i18n/sr.json:6-11] → Folded into Story 8.5-3 (review pending PR submission; sr.json unchanged at fold-in time)
- **D126: Go test assertion messages still mention "Red"/"Blue" as descriptive prose** — `scoring_test.go`, `fixtures.go` comments, `user/handler.go` comments use "Red team"/"Blue team" in English documentation. Doesn't trip the AC-001 regex (the regex targets identifiers, not prose). Cosmetic consistency cleanup; updates terminology for future readers. [server/internal/game/scoring_test.go, server/internal/game/testfixtures/fixtures.go] → Folded into Story 8.5-3 (verified clean — no Red/Blue prose remained at fold-in time)
- **D127: `viewerTeam` derivation in `GamePage.tsx` after `myPlayerSeat === null` early-return is fragile** — If `clearGame()` runs in the same React commit as a tail-of-match render, the JSX past the guard could read `myPlayerSeat` from a stale render. Speculative; no demonstrated regression. Snapshot via `useMemo([myPlayerSeat])` if the team-flip race ever surfaces. [client/src/features/game/GamePage.tsx:377]
- **D128: Unicode escapes `♠`/`♡`/`♢`/`♣` in `TrumpIndicator.tsx` changed to literal `♠♥♦♣`** — Drive-by edit during the rename pass. Functionally equivalent on a UTF-8 Vite project, but if the file was deliberately ASCII-escaped for tooling reasons, the diff silently regressed that. If literal codepoints are accepted as project convention, leave as-is; otherwise revert. [client/src/features/game/components/TrumpIndicator.tsx:11-16]

## Deferred from: code review of 8-3-in-game-emotes (2026-04-28)

- **D105: `lastEmoteAt` map grows unbounded for the lifetime of the process** → Folded into Story 8.5-2. — Per-user rate-limit map in `emote.Handler` is never pruned. One `time.Time` entry persists per unique user who has ever emoted; no eviction on session removal, no TTL sweep, no piggyback cleanup. Long-running server slowly accumulates one stale entry per lifetime user, not per active user. Fix would be either a periodic sweep of entries older than `rateLimitWindow` or a `RemoveUser(userID)` hook called from `session.Manager.RemoveSession`. [server/internal/emote/handler.go:40, :99-105]
- **D106: Cross-match rate-limit bleed-through silently drops first emote of new match** → Folded into Story 8.5-2. — Rate-limit state is keyed by userID with no session scoping. User emotes in match A at T=0, match A ends at T=1.5 s, user joins match B at T=2 s, tries to emote at T=2.5 s. Server's `acceptRateLimited` sees `2.5 - 0 = 2.5 s < 3 s` and silently drops with no echo, no error event, no UX feedback. Combine with D105 fix by clearing the user's entry when their session is removed. [server/internal/emote/handler.go:98-106]
- **D107: Picker `lastSentAt` resets when EmotePickerButton unmounts during phase transition** — Phase transitions (e.g. `playing` → `match_end` → next `dealing`) unmount and remount the picker via the parent's allowlist gate. The remounted picker has fresh `lastSentAt=0`, so tiles re-enable inside the 3 s window. The user clicks, sees no error, but the server silently drops the request via its authoritative rate-limit. UX disconnect with the server. Fix would lift `lastSentAt` to a `useRef` in GamePage or to the gameStore so it survives picker remounts. [client/src/features/game/components/EmotePickerButton.tsx:36, :92-100; client/src/features/game/GamePage.tsx:678-686] → Folded into Story 8.5-3
- **D108: System clock backwards-jump (NTP step / laptop sleep) locks picker for arbitrary time** — `cooldownRemaining = COOLDOWN_MS - (Date.now() - lastSentAt)`; if the system clock jumps backward by N ms, `cooldownRemaining` becomes `3000 + N`. The `useEffect` schedules a `setTimeout(cooldownRemaining)` for that long. Picker tiles stay disabled for the full N+3 s. Fix would be `performance.now()` (monotonic) or `Math.min(cooldownRemaining, COOLDOWN_MS)`. [client/src/features/game/components/EmotePickerButton.tsx:43, :94-95] → Folded into Story 8.5-3
- **D109: Stale `participants` slice broadcast after concurrent `session.Manager.RemoveSession`** — `MatchParticipantsByUser` takes RLock, captures `playerIDs`, releases. Concurrently `RemoveSession` (write lock) tears down the session. The emote handler then broadcasts to the now-orphaned IDs. `BroadcastToUsers` will deliver to any still-connected clients. The client-side `gameState === null` guard catches it only after `clearGame()` has run. Brief tail-of-match emote leak window. Fix would re-check session liveness inside the broadcast call or hold the lock through the broadcast. [server/internal/emote/handler.go:73-92; server/internal/session/manager.go:629-646] → Formally accepted in Story 8.5-2: BroadcastToUsers silently skips disconnected clients; tail-of-match emote delivery is benign. Tracking comment added to emote/handler.go. Will not be fixed unless a session-held broadcast interface is introduced (out of scope for this epic).
- **D110: Stale `activeEmotes` slot survives match-end overlay then re-renders on overlay dismiss** — Bubble unmounts when `matchEndData !== null` gates kick in, without firing `onDismiss`, so the seat's slot stays non-null. The spec explicitly tolerates this ("the store still records the latest emote so that re-emergence renders the next live one"). However, a 30 s-old slot rendered for another 2 s after overlay dismissal looks like a stale replay. Path is unlikely (match-end → lobby triggers `clearGame()`), but a freshness check `Date.now() - slot.receivedAt < DURATION_MS` would prevent it. [client/src/shared/stores/gameStore.ts; client/src/features/game/GamePage.tsx:691-707]

## Deferred from: code review of 8.5-2-server-concurrency-and-type-hardening (2026-05-02)

- **D129: `HandScoredPayloadSchema` team-index fields (`contractingTeam`, `lastTrickTeam`, `capotTeam`) remain `z.number()` after `winnerTeam` was tightened to `z.union([z.literal(0), z.literal(1)])` on `MatchEndPayloadSchema`, `TrickResolvedPayloadSchema`, and `DeclarationsResolvedPayloadSchema`** — Inconsistent validation depth; a server bug emitting a garbage team index on `HandScoredPayload` would pass Zod silently. Story 8.5-2 Task 1.5 explicitly marks these fields as out-of-scope ("include only if time allows"). Also affects the inline `lastHandResult` block in `EventGameStateSchema` and `BelotAnnouncedPayloadSchema.team`. Fix: tighten all remaining team-index fields to the same union. [client/src/shared/types/wsEvents.schemas.ts:162-178]
- **D130: `ScorePanel.tsx` calls `teamStringForIndex(showBonus.team)` without the `(x === 0 ? 0 : 1)` ternary guard used at every other call site** — Safe today because `showBonus.team` is statically typed `0 | 1` (from `useState<{ team: 0 | 1; amount: number } | null>`), so TypeScript resolves the overload to `TeamString`. If the state type is ever relaxed to `number`, the nullable overload activates and `bonusTeamString === viewerTeam` silently becomes `false` with no error. Fix: add the ternary guard for consistency and defensive correctness. [client/src/features/game/components/ScorePanel.tsx:52]

## Deferred from: code review of 8.5-3-ux-polish-and-shared-pattern-refactors (2026-05-03)

- **D131: `clearGame()` resets `lastEmoteSentAt = 0`; cross-match emote rate-limit desync** — Spec Task 8.1 mandates `lastEmoteSentAt: 0` in `initialState` so `clearGame()` resets it. Combined with the open D106 (server keeps user-keyed cooldown across sessions): user emotes near match-end at T=0, match A ends T=1.5 s, joins match B at T=2 s, tries to emote at T=2.5 s. Client cooldown reads 0 (cleared), server cooldown is still 2.5 s < 3 s and drops silently with no error event. Same UX pathology as D106; resolution probably belongs to Phase-2 economy work. [client/src/shared/stores/gameStore.ts:95,141]
- **D132: Reactive `useReducedMotion` restarts reveal/animation timers on mid-animation OS toggle** — All callsites that switched from `useMemo([])` to the live hook now have `prefersReducedMotion` in their `useEffect` dep arrays. If the OS-level setting toggles during a 1.5–8 s reveal window, the cleanup fires and the new effect re-schedules a fresh full-duration timer. Reveal can extend by several seconds; `DealAnimation` may rewind a stage. Edge case is narrow but no longer impossible after the AC1 refactor. Snapshot via `useRef` on first render if it surfaces. [client/src/features/game/components/{BelotReveal,DeclarationReveal,DealAnimation,CapotAnimation,ScoreReveal,ReshuffleAnimation}.tsx]
- **D133: Pause-then-resume re-mounts stale declaration/Belote reveal on top of later trick** — AC3 gates rendering on `!isOverlayActive`. The reveal component unmounts during overlay; its store payload is NOT cleared. When overlay dismisses (e.g., owner-resume after a long pause), JSX re-evaluates with `declarationReveal` still set, the component re-mounts and a fresh full-duration timer schedules. The reveal can pop on top of trick 2 or 3 with cards from trick 1's resolution. Spec AC3 explicitly tolerates this ("either fires or has already been consumed in the store"). Tighter UX would clear the payload from the store when the overlay mounts. [client/src/features/game/GamePage.tsx:666-689]
- **D134: GamePage test for AC3 belote-reveal hide passes via overlay gate, not AC2 dispatch reset** — `GamePage.test.tsx` calls `setGameState` directly, bypassing the dispatcher's clear path. The test passes because of the new render gate, not because of the dispatch reset. Coverage gap: the AC2 reset-on-game_state path lacks a true regression-gate test. Add a sibling test that drives `useWsDispatch.dispatchGameEvent({ type: EVENT_GAME_STATE, ... })` while reveals are seeded. [client/src/features/game/GamePage.test.tsx]
- **D135: `useFocusTrap` Escape handler attached to container — no-op if focus escapes the trap** — `useFocusTrap.ts:60` adds the keydown listener to `containerRef.current` (the inner `role="dialog"` div). Escape fires only if the keydown reaches that container. Browser dev tools moving focus to `<body>`, or a screen-reader navigation event, would make Escape silently fail to dismiss the prompt. Existing tests fire on `screen.getByRole("dialog")` so the regression is invisible to the test suite. [client/src/shared/hooks/useFocusTrap.ts:60]

## Deferred from: spec-fix-tie-fails-trump-caller (2026-06-06)

- **Bitola-variant hanging-points (carry-over) tie rule — NOT YET IMPLEMENTED.** On a tied hand (the taker's team total equals the opponents'), the engine currently awards ALL points to the opponents and the taker's team scores 0. That is the **Croatian-variant** rule. The **Bitola variant** should instead use *hanging points*: on a tie the hand's points are held over (nobody scores) and carried to the winner of the next decisive hand. Because only the Bitola variant ships today and hanging-points needs additional logic — a cross-hand carry-over accumulator, resolution when the next hand is decided, and match-end/target interaction — the simpler Croatian rule is applied to ALL variants as an interim stand-in. **When variants diverge (Epic 12 — Variant Expansion, Phase 3), Bitola must switch to hanging points while the Croatian variant keeps all-points-to-opponents.** Do not treat the current Bitola behavior as a bug before then. Decision site: `scoreHand` in `server/internal/game/scoring.go` (`contractingTotal <= opposingTotal`). Origin: story `3-5-hand-scoring-failed-hands-and-capot.md`; tracked by `spec-fix-tie-fails-trump-caller.md`. **PROMOTED 2026-08-18 to Story 12.7 (Bitola Hanging-Points Tie Rule)** via bmad-correct-course (sprint-change-proposal-2026-08-18.md) — no longer deferred; it is now a scheduled Epic 12 story with FR64 behind it. The 2026-08-18 rules audit confirmed the Croatian side needs NO change: the current all-points-to-opponents behaviour IS the Croatian rule, so only Bitola moves.

## Deferred from: spec-timer-drift-truthful-countdowns (2026-06-10)

- **D136: Fixed 400 ms `expiryGrace` can be smaller than the link's one-way latency** — the clock-offset estimate's residual error equals the minimum one-way latency observed in the sample window, so on links where that latency exceeds ~400 ms (poor mobile, intercontinental) the corrected countdown can still read >0 when the server's auto-action lands — the players-see-0-first guarantee degrades gracefully but silently. Fix would be an adaptive grace (sized from observed RTT per session) or at least telemetry counting how often auto-actions land while a client-acked render shows >0. [server/internal/match/live_match.go `expiryGrace`; client/src/shared/lib/clockSync.ts]

## Deferred from: code review of 10-2-501-point-match-mode (2026-06-11)

- **D137: Rules pages don't document the both-teams-cross-tied → taker-wins match tiebreaker** — The engine implements it (`determineMatchWinner`, asserted by `TestMatchEnd_BothTeamsExceed{1001,501}_TiedScore_ContractingTeamWins`), but all four rules-content match-end notes only say "the side with more total points takes the match" and are silent on the equal-totals case. Pre-existing gap (applies to 1001 too); one sentence per locale, mind the banned-word rule (phrase as "the taker's team", never "contract"). [client/src/features/rules/content/{en,mk,hr,sr}.ts]
- **D138: Mode-label mapping duplicated across three client sites with divergent fallbacks** — `RoomCard.modeLabel()` falls back to hardcoded English `` `${m} pts` ``, `RoomPage.matchModeKeys` misses render the raw value, and MatchPage's ternary silently treats any non-"501" as 1001. Each new mode must be registered in three places; a future "701" would label correctly in the lobby but render "/ 1001" in the match HUD. Consolidate into one shared mode→label/target helper. [client/src/features/lobby/components/RoomCard.tsx:17; client/src/features/room/RoomPage.tsx:52; client/src/features/match/MatchPage.tsx:1180]
- **D139: `matchMode` is stringly-typed end-to-end** — No `"1001" | "501"` union on `Room`/`MatchState` types (the CreateRoomModal preview props already use the union, proving it fits); server gate is a `map[string]bool`. A typo like `"510"` in any client mapping site compiles cleanly into the D138 fallbacks. Tighten when the next mode or the Croatian variant forces a types pass. [client/src/shared/types/apiTypes.ts; client/src/features/match/matchTypes.ts]
- **D140: `profile.matchHistory.mode` i18n map lacks a "501" entry in all four locales** — Latent: no consumer renders the mode today, but the matches API already returns `matchMode` per row, so the first consumer will echo a raw key for 501 matches. Story 10.2 spec explicitly left match history alone; add the sibling key when match history surfaces the mode. [client/src/shared/i18n/{en,mk,hr,sr}.json]
- **D141: sr landing mock `r2meta` "501 poen" diverges from sr lobby-label standard "bod"** — Story 10.2 aligned `lobby.createRoomModal.matchMode501` sr to "501 bod" (sibling consistency) and fixed the mk grammar nit, but the sr landing mock still reads "Hrvatska · 501 poen · opušteno" while a real room card would show "501 bod". Spec sanctioned only the mk fix this story. One-word change for a future sr terminology sweep. [client/src/shared/i18n/sr.json:812]

## Deferred from: code review of 10-3-bot-players (2026-06-12)

- **D142: Strict zod `PlayerStateSchema` + required `isBot` breaks every mixed-version window** — Old bundle (strictObject without the key) × new server (emits `isBot`) hard-fails every match snapshot in long-lived stale tabs — the project's documented stale-tab failure mode — and a new bundle against an old server fails on the missing required field. The `.strict()` architecture and the same-commit contract rule predate this story (spec mandated this exact shape), so the fix is a platform-wide version-skew policy (e.g. tolerate unknown keys, or a bundle-version handshake that forces refresh), not a per-field patch. [client/src/shared/types/wsEvents.schemas.ts:73]
- **D143: Owner-confirm dialogs' `pending` spinner is unreachable (kick + remove-bot)** — Both `handleKickConfirm` and `handleRemoveBotConfirm` close the dialog (`set*Confirm(null)`) before `await mutateAsync`, yet both dialogs wire `pending={mutation.isPending}` with a `confirmPending` spinner that can never render. Either keep the dialog open during the mutation or drop the dead pending plumbing — fix both call sites together. [client/src/features/room/RoomPage.tsx:497-503,565-573,1287-1288,1302-1303]

## Deferred from: spec-return-to-room-after-match — v2 presence layer (2026-06-16)

- **Return-to-room v2: presence layer ("waiting to return" + start-gating + barred-return-after-kick).** v1 (`spec-return-to-room-after-match.md`) ships the core: "Return to room" reopens the same room (`completed → waiting`, lazy/idempotent), re-seats returners on their original `room_players` seats, clears `room_bots`, adds the second result-dialog button + navigation, and lets the owner start manually once all 4 seats are occupied. v2 adds the **presence concept** the user requested so the lobby can tell "returned" from "still on the result dialog": (1) a server-side in-memory presence registry (roomID → set[userID]), seeded on reopen and by fresh joiners, cleared on match start / room close; (2) a new `system:player_returned` WS event (both contract files) + `returnedUserIds` on the room detail payload; (3) roomStore presence state + `RoomPage` "waiting to return" seat state; (4) **gate the owner Start button on every seated human being present** (not merely seated) so an absent ex-player is never pulled into the next match; (5) **bar a kicked player from re-entering** — `POST /rooms/:id/return` rejects callers no longer in `room_players`, client routes them to `/lobby` with a notice. Without v2, v1's gap is that a seat held by a not-yet-returned player counts as "occupied", so the owner could start before that player is actually back. Decisions already locked by the user: reserved seats with **no timeout**, **no auto-transfer** of ownership from an AWOL owner (remaining players just leave), bots cleared on reopen, owner presses Start. [server/internal/room/{rematch.go(new),handler.go}, server/internal/ws/events.go, client/src/shared/types/wsEvents.ts, client/src/shared/stores/roomStore.ts, client/src/features/room/RoomPage.tsx, client/src/features/lobby/useRoomUpdates.ts; origin spec-return-to-room-after-match.md]

## Deferred from: code review of spec-return-to-room-after-match v1 (2026-06-16)

- **D144: Reopened room is invisible to already-loaded lobby grids.** When a room goes `waiting→playing`, `useRoomUpdates` removes it from the `["rooms","waiting"]` cache (`client/src/features/lobby/useRoomUpdates.ts:55-73`), and match-end's `completed` transition broadcasts no lobby update. On v1 reopen, `ReturnToRoom` broadcasts `system:room_updated` (status `waiting`), but the client handler only `.map`s rooms already present — only `system:room_created` *adds* — so the reopened room never reappears for lobby browsers until a full refetch. Returners navigate by id and are unaffected, but a stranger can't discover/join the reopened room live (undercuts the user's "someone else takes their place"). Fix: on reopen broadcast `system:room_created` (or make the `room_updated` handler add-if-missing). [server/internal/room/handler.go ReturnToRoom; client/src/features/lobby/useRoomUpdates.ts:55-73]
- **D145: Non-returned players stranded on the stale result overlay when the next match starts (v1 no-presence consequence; concrete client-state TODOs beyond presence).** Even once v2 gates start on presence, two client-state bugs remain: (a) `MatchPage` never clears `matchEndData`/`overlayPhase` when a fresh non-`match_end` `match_state` arrives, so a new match can run underneath the old result overlay; (b) `system:match_started` is dropped by `useWsDispatch` when `currentRoomId === null` (reset on leaving RoomPage for the prior match), so a player who never returned to RoomPage won't auto-navigate into a freshly started match. Pair with the v2 presence work. [client/src/features/match/MatchPage.tsx (match_state → overlay reset); client/src/features/match/useWsDispatch.ts SYSTEM_MATCH_STARTED gate]
- **D146: `handleReturnToRoom` error toast is generic for all failures.** A 409 `MATCH_ALREADY_STARTED` (a new match started before the click) and a 404 `NOT_IN_ROOM` (kicked/left) both surface the same "couldn't return — it may no longer be available" message. Minor UX: map the error code to distinct copy. [client/src/features/match/MatchPage.tsx handleReturnToRoom catch; client/src/shared/i18n/*.json]

## Deferred from: code review of spec-return-to-room-presence-v2 (2026-06-16)

- **D147: lobby-disconnect `broadcastRoomUpdated` payload is incomplete, so the new D144 upsert can add a partial room card.** v2's D144 fix made the lobby `system:room_updated` handler add-if-missing (`client/src/features/lobby/useRoomUpdates.ts`). `LobbyDisconnectHandler.broadcastRoomUpdated` (`server/internal/room/lobby_disconnect.go:203-237`) builds its own payload that omits `players` and `isQuickPlay` (unlike the room handler's `roomLifecyclePayload`). If a lobby-timeout fires for a `waiting` room that is *absent* from a given browser's already-loaded grid, the upsert prepends a card missing seat chips + the quick-play flag until the next refetch. Narrow trigger (room must be waiting AND missing from that grid AND a lobby-disconnect timeout fires) and self-heals on refetch. Fix: make `lobby_disconnect.go`'s payload match `roomLifecyclePayload` (include `players` + `isQuickPlay`), or have it reuse the shared builder. [server/internal/room/lobby_disconnect.go:203-237; client/src/features/lobby/useRoomUpdates.ts]

## Deferred from: manual verification of return-to-room (2026-06-17)

- **D148: Hard refresh (F5) while seated in a room lobby loses your seat and ownership.** Pre-existing, not introduced by return-to-room (surfaced while verifying it). On a hard reload the `pagehide` handler fires a keepalive `/leave` beacon (`client/src/features/room/RoomPage.tsx:348-357`); the room page then reloads, the deep-link auto-join (`RoomPage.tsx:201-236`) re-establishes membership — but only as an **unseated** non-owner, because join doesn't restore the prior seat/team and ownership has already transferred to a remaining member. Net effect: refreshing inside a `waiting` room silently vacates your seat (and, if you were host, hands ownership away). The original intent ("refresh re-establishes membership") holds for membership but not for seat/ownership. Reproduced live during the 2026-06-17 verification (hard reload of `/rooms/:id` → server log `POST /leave` then `POST /join`, page showed owner unseated + "Hosted by <other>"). Fix options: have the `pagehide` beacon skip the leave for a member of a `waiting` room (let the reload's auto-join no-op on the still-present membership), or persist+restore the seat on reload. Distinct mechanism from the StrictMode dev-only leave fixed in `RoomPage.tsx` (that one was SPA-nav + StrictMode double-invoke; this one is the real `pagehide` beacon on hard unload). [client/src/features/room/RoomPage.tsx:201-236,331-357]
- **D149: No room/quick-play reconnection redirect on load (only games reconnect).** `useReconnectionRedirect` (`client/src/shared/hooks/useReconnectionRedirect.ts`) routes a player back into an active *match* when `event:match_state` arrives off the `/match` page on WS reconnect, but there is **no equivalent for a player who is a member of a `waiting` room or sitting in the quick-play queue**. If a client ends up authenticated-but-elsewhere while still a `room_players` member of a `waiting` room (e.g. a stale tab, a second device, or any future state-desync), the app never navigates them back to `/rooms/:id` (or `/matchmaking/:id`), and `Join`/`QuickPlay`/`CreateRoom` then reject with `ALREADY_IN_ROOM` — leaving them stuck until they deep-link the exact room URL by hand (the user's manual workaround). Largely mitigated already: the unseated-owner zombie (commit c3dc985, Bug A) and the missing `CreateRoom` guard (Bug B) were the main producers of hidden memberships, so this is now a defensive safety net rather than an active break — which is why it is deferred rather than shipped. Proposed approach: add a small `GET /rooms/mine` endpoint (server-side `FindPlayerRoom` → `{roomId, status, isQuickPlay}` or 204) and an always-mounted client redirect hook that, on auth-init/lobby load, routes a member of a `waiting`/`playing` room to the right page. **Must guard against a redirect loop with the leave flow** — the leave-on-unmount + `pagehide` leave are async, so the hook has to ignore a room the user is actively leaving (e.g. gate on the leave mutation settling, or skip when navigating *to* the lobby right after a leave). Pairs naturally with D148. [client/src/shared/hooks/useReconnectionRedirect.ts (model), new client redirect hook + GET /rooms/mine; server/internal/room/handler.go FindPlayerRoom]

## Deferred from: spec-reset-password (2026-06-17)

- **D150: Password reset does NOT invalidate existing sessions.** Decided up-front with the user to keep the reset feature's scope tight: `ResetPassword` updates only `users.password_hash`, so any access (15 min) / refresh (7 day) JWTs minted before the reset stay valid until they expire. Sessions are stateless signed JWTs with no server-side store, so force-logout of *other* devices requires extra infra: a `users.token_version` (or `password_changed_at`) column, bumping it on reset, and rejecting refresh tokens minted before the bump (a new claim + a check in `auth.Refresh`). The threat is modest here — forgot-password is the "can't log in" path, the active-attacker-session case is less common, and refresh tokens already age out — so this was deliberately deferred, not shipped. Implement when account-security hardening is prioritized; it composes cleanly with the existing stateless-JWT model. [server/internal/auth/{password_reset.go ResetPassword, service.go Refresh/Generate*}, server/internal/user (token_version column + migration), server/internal/apperr; origin spec-reset-password.md]
- **D151: forgot-password mints a new token without a transaction around delete+create.** `issueAndSend` calls `DeleteByUserID` then `Create` as two separate statements (`server/internal/auth/password_reset.go`). Two concurrent forgot-password requests for the same account can interleave so both rows survive, leaving more than one live reset link for that user — violating the "only the most recent link is live" intent (the single-threaded test `TestForgotPassword_ReplacesPriorTokens` doesn't exercise the race). Harm is low: every token is still single-use and 1-hour-expiring, and all resolve to the same account. Fix: wrap delete+create in one DB transaction, or upsert a single row per user. Surfaced by the blind + edge-case reviewers. [server/internal/auth/password_reset.go issueAndSend; server/internal/passwordreset/gorm_repo.go]
- **D152: No rate limiting on the unauthenticated `/auth/forgot-password` and `/auth/reset-password` endpoints.** forgot-password triggers real outbound email keyed on an attacker-supplied address (email-bombing / SMTP-quota-exhaustion vector); reset-password is an unauthenticated brute-force surface against the token (mitigated to near-zero by the 256-bit `crypto/rand` token, but still unthrottled). This is a **platform-wide** gap — `/auth/register` and `/auth/login` are equally unthrottled, so it predates this story — but the email-sending endpoint makes it newly worth addressing. Fix: add per-IP and per-email throttling (Echo middleware or a small in-memory limiter) across the public `/auth` group. Surfaced by the blind reviewer. [server/cmd/api/main.go auth route group; server/internal/auth/password_reset.go]

## Deferred from: code review of 9-1-coin-wallet-foundation (2026-06-18)

- **D153: `useDailyRewardGate` re-fires the daily-login claim on every `AppLayout` remount, not once per app session.** The gate's StrictMode ref guard (`claimedRef`) lives on the `DailyRewardGate` instance mounted inside `AppLayout`. `/match/:roomId` is a sibling route *outside* `AppLayout`, so leaving the lobby for a match unmounts the gate and destroys the guard; returning remounts it and re-POSTs `/wallet/daily-login`. No double-grant — the endpoint is idempotent (a same-UTC-day repeat returns `granted:false`) and the mid-day no-grant suppresses the dialog — so the only effect is a redundant request per navigation cycle. The hook's "once per session" doc comment is, in practice, "once per mount." Surfaced by the blind + edge-case reviewers during the 9.1 review; deferred as a minor efficiency item with no correctness or UX impact. Fix options if revisited: gate on a `sessionStorage` flag (true once-per-session), or hoist the claim to a session-scoped store/effect that survives route changes. [client/src/features/lobby/hooks/useDailyRewardGate.ts; client/src/shared/components/AppLayout.tsx; client/src/App.tsx route layout]

## Deferred from: code review of 9-4-quick-play-coin-bracketing (2026-06-19)

- **D154: Refund-on-`StartMatch`-failure on the Quick Play auto-start path is best-effort-with-log only.** When a default-bracket (500) auto-start charges all four humans via `ChargeStakes` and `StartMatch` then fails, `startAutoStartedMatch` refunds via `ApplySettlement` and only logs (`slog.Error`) if that refund itself fails (`server/internal/room/handler.go:1639-1654`) — leaving the four players debited 500 each with no retry, no outbox, and no durable reconciliation. This is **not new to this story**: 9.4 was explicitly told to mirror the manual `/start` charge block, and Story 9.3 already deferred "refund-failure coin-durability" as an accepted interim (do not build an outbox). The trigger is doubly unlikely — `StartMatch` failure is rare, and `ApplySettlement` failing on top of it rarer still — so it is deferred, not shipped. Fix when wallet durability is prioritized: a transactional outbox or a reconciliation job that replays failed refunds; would cover both the manual and auto-start charge paths at once. Surfaced by the blind reviewer during the 9.4 review. [server/internal/room/handler.go:1639-1654 (auto-start) + :2756-2784 (manual /start); server/internal/wallet/service.go ApplySettlement]

## Deferred from: bot-strategy planning session (2026-06-20)

Spun out while scoping `spec-improve-bot-bidding-and-lead-heuristics` (Goal A — bidding thresholds + master-trump/safe-low leads — ships first as its own PR). The three items below are follow-up bot play-quality heuristics, each its own spec/PR. Recommended order: **B → D → C** (clearest/highest-confidence first, fuzziest last). All assume the existing pure-`bot.Decide` architecture.

- **B (declaration memory — don't fight the partner's revealed cards).** Declarations reveal *exact* cards; the bot should fold them into memory as **known holdings by seat** and stop overtrumping/stepping on a partner who provably owns the winning cards (e.g. opponents void of trump, partner declared a high-trump run, bot holds the rest — it must let the partner cash, not draw their trumps). Needs a **new memory dimension** beyond Goal A's `bot.go`-only boundary: record revealed declaration cards in `memory.go`, expose known-holdings in `bot.View` (`view.go`), populate in `bot_driver.go` (engine already carries `Declaration.Cards`/`PlayerSeat`). **Constraint:** per the rules only the **winning** declaration team's cards are revealed publicly — so a bot reliably learns its partner's exact cards only when their team won the contest (and learns winning opponents' cards otherwise). High value, clear, deterministic. **Review note carried from Goal A:** Goal A's master-trump lead is intentionally conservative — it won't draw trumps when the *partner* holds the master (e.g. the Jack), because from the seat's redacted view the Jack is "unseen" and indistinguishable from an opponent holding it (edge-case reviewer finding, accepted as intended for A). Declaration memory relaxes exactly this: once the partner has *declared* high trumps, the bot KNOWS the team controls the top and can stop fighting them and reconsider drawing/support — fold that into the lead logic. Engine source of truth (read-only): `GameState.Players[seat].Declarations` (each `Declaration` has `.Cards`/`.PlayerSeat`, see `types.go`), `GameState.DeclarationsResolved`, reveal fires at trick-2 start (`server/internal/match/live_match.go:721`). [server/internal/bot/memory.go, view.go, bot.go; server/internal/match/bot_driver.go; server/internal/game/declarations.go (source of revealed cards)]

- **D (tend to win the last trick — +10 "dix de der").** The literal 8th trick is **forced** (one card each, `len(legal)==1`), so this is really **endgame card retention**: in the late tricks (≈2 cards left), avoid squandering the card that would win trick 8 — preserve a master/high trump or a guaranteed side-suit winner for last when the +10 is contestable. A `chooseFollow`/`chooseLead` refinement; stays inside `bot.go` + tests, but interacts with the existing follow logic and needs care not to bleed points to grab a trick that is already lost. Medium effort, mostly deterministic. [server/internal/bot/bot.go chooseFollow/chooseLead]

- **C (Ace-without-the-10 → continue the suit; partner signaling).** When the bot cashes an Ace it does not hold the 10 behind, the partnership should keep leading that suit so the opponent's 10 eventually drops/gets trumped. This is a partnership **convention**, so it only works reliably **bot-with-bot** — a human partner won't follow the signal and the bot cannot decode a human's intent — and it is the lowest-confidence to make correct deterministically. Design the convention (sender encoding vs. receiver-side inference from played-card memory) before committing an implementation shape; consider gating it to all-bot partnerships. Likely the natural boundary where rule-based play plateaus and the determinized-Monte-Carlo upgrade path (noted in the 10-3 strategy discussion) would do better. Fuzziest; do last. [server/internal/bot/bot.go chooseLead + partner-coordination inference] **Investigated & decided 2026-06-20 — NOT implementing as a deterministic heuristic; folds into the ISMCTS upgrade.** Reduced to a pure per-seat `bot.Decide`, the convention collapses to a lead-suit bias ("prefer a 0-point lead in a broken side suit whose Ace is in `PlayedCards` and whose 10 is still a `threats` card"). But *capturing* the floating 10 requires our side to **ruff** the trick the opponent is forced to play it on — multi-trick geometry a per-seat function can neither engineer nor verify. So the deterministic realization only changes *which* 0-point card is led, with no reliable capture and a real downside: it voluntarily concedes tricks in the opponents' broken suit in every line the squeeze never lands. Receiver-side inference is also out — `PlayedCards` carries no seat attribution, so the bot can't tell who cashed the Ace without a larger seat-tagged-history memory change. The all-bot sim can't even validate it (random-legal opponents don't intelligently retain the 10, so any delta is noise). Cost (new `chooseLead` branch + a `View.PartnerIsBot` gating field + `bot_driver` wiring + tests + ongoing soundness-review burden) far exceeds the uncertain, probably-zero, possibly-negative benefit. The deterministic A→B→D bot-strategy arc concludes here; "capture the opponents' floating 10" belongs to the determinized-MC/ISMCTS initiative, which can search the ruff geometry.

## Deferred from: code review of spec-bot-last-trick-retention (item D, 2026-06-20)

- **Endgame retention is trick-7-outcome-blind — it spends the spare to keep the master without weighing the card points conceded now.** `retainLastTrickWinner` (server/internal/bot/bot.go) fires whenever the bot holds the master trump at `len(v.Hand)==2`, returning the other card unconditionally. On a *follow* this is harmless (when retention fires the spare is itself a winning trump that takes trick 7 and the master still banks trick 8 — verified no bleed), but on a *lead* the bot deliberately leads junk and concedes trick 7, which can bleed that trick's card points — including a partner's forced high card — to the opponents in exchange for the guaranteed +10. The change consciously accepted this (spec frozen "Ask First: Capot-pursuit special-casing"; Design Notes flagged the lead-bleed as second-order and deferred it to review), and the empirical guard confirms no regression (`TestSimulation_HeuristicBeatsRandomBaseline` 70–75%, ≥60%). The edge-case reviewer also raised Capot cases (own-team going for Capot; denying opponents' Capot) — both analysed as non-regressions, since holding a junk card already precludes the team's own sweep and the master always denies the opponents' sweep on trick 8; the residual is purely the trick-7 card-point swing above. **Fix when bot play-quality is revisited:** gate retention on a trick-7 EV/`TricksWon`/`HandPoints` comparison (the View already carries `TricksWon`/`HandPoints`), or fold it into the determinized-Monte-Carlo upgrade path (10-3 strategy discussion) which would weigh the concession natively. Low per-occurrence cost, lead-only, and net-positive today. [server/internal/bot/bot.go retainLastTrickWinner] **Investigated & decided 2026-06-20 — keep `retainLastTrickWinner` as-is; a sound trick-7-EV gate needs the out-of-scope determinized search.** At `len(Hand)==2` the bot knows the 6 remaining cards but NOT their distribution among the 3 other seats; a correct trick-7 EV requires enumerating those distributions under the void constraints — a determinized search this arc explicitly bans ("Never: add Monte-Carlo/ISMCTS"). The master wins exactly one trick and the +10 always attaches to trick 8, so retention already banks the guaranteed +10 either way (net-positive, exactly as item-D's review concluded); the residual is a genuinely distribution-dependent trick-7 swing. A deterministic gate would be either near-empty (`TricksWon`/`HandPoints` yield no clean fork; the certain-only variant fires ~never) or an unsound guess that violates the carried lesson ("would-help ≠ sound"). Fold into the determinized-MC/ISMCTS upgrade, which weighs the concession natively.

## Deferred from: code review of 9-5-xp-and-level-system (2026-06-22)

- **`abandonPartialXPFactor` float truncation is untested for non-1.0 values.** `deltas[seat] = int(float64(base) * abandonPartialXPFactor)` truncates toward zero. With the current factor of `1.0` this is a no-op and every test passes, but the const exists precisely so a future tuning pass can discount the non-abandoning team's XP (e.g. `0.5`). The moment it is changed, the truncation-vs-rounding policy is undefined and unverified (`int(99 * 0.5) = 49`; float representation error can shift exact boundaries). **Fix when the factor is tuned:** decide and document round vs. floor, and add a table-test case for the non-1.0 factor alongside the change. [server/internal/match/xp_award.go:61 `abandonPartialXPFactor` / :61 `computeXPAwards`]

> ~~`total_xp` Go `int` vs Postgres `INTEGER` width mismatch~~ — **RESOLVED 2026-06-22 during the same review.** Migration 000011 was unshipped, so the column was widened to `BIGINT` (matching the 64-bit Go `int` accumulator) rather than deferred. down/up roundtrip re-verified on the dev DB; user-pkg DB tests green.

## Deferred from: code review of 9-6-private-rooms (2026-06-24)

- **`UpdateRoomPrivacy` is a non-transactional read-modify-write with a full-row save.** The handler does `room, _ := h.repo.FindByID(id)` → mutate `room.PasswordHash` → `h.repo.Update(room)` (which is `db.Save`, persisting ALL columns), with no row lock and no enclosing transaction (server/internal/room/handler.go:816-850). A concurrent join (`PlayerCount` increment) or `StartMatch` (`Status` waiting→playing) that commits between the read and the save can be clobbered by the stale full-row write — a classic TOCTOU / lost-update window. **Not new to this story:** it mirrors the codebase-wide room-mutation pattern, and Story 9.6 was explicitly told (Dev Notes, 9.3 intelligence) NOT to refactor the shared join/seat/transfer machinery; a `FindByIDForUpdate`-in-a-tx primitive already exists for the paths that need it. The owner-only + waiting-only guard makes the window narrow, and subsequent `system:room_updated` / seat broadcasts self-heal the transient drift, so it is deferred, not shipped-broken. **Fix when room concurrency is hardened:** route the privacy edit through `FindByIDForUpdate` inside a transaction (or re-check `Status == "waiting"` and use a column-scoped `Updates` instead of `Save`), covering this endpoint alongside the other owner-action mutations. Surfaced by the blind reviewer during the 9.6 review. [server/internal/room/handler.go:816-850 UpdateRoomPrivacy; server/internal/room/gorm_repo.go:97 FindByIDForUpdate, :108 Update]

- source_spec: `_bmad-output/implementation-artifacts/spec-google-sso.md`
  summary: Profile "Linked accounts" management — authed `GET/POST/DELETE /api/v1/users/:id/identities(/:provider)` endpoints (incl. `hasPassword` exposure and the unlink-blocked-when-passwordless guard) plus the `LinkedAccounts` profile section UI with link/unlink dialogs and i18n.
  evidence: Split from the Google SSO spec at the step-02 token checkpoint (~3,000 tokens vs 1,600 ceiling); profile-side management is independently shippable once SSO login/register and the identity infrastructure land, and reuses the same `user_identities` table, provider registry, and `LinkAccountDialog`.

- source_spec: `_bmad-output/implementation-artifacts/spec-google-sso.md`
  summary: Bind GIS credentials to the browser session with a `nonce` (one-shot, server-verified) so an exfiltrated Google ID token cannot be replayed against `/auth/sso/:provider(/link)` during its ~1h lifetime.
  evidence: Blind-reviewer hardening finding — verification checks signature/audience/issuer/expiry but nothing ties the token to the requesting session; GIS supports `nonce` for exactly this. Needs a server-side one-shot nonce store, so out of patch scope.

- source_spec: `_bmad-output/implementation-artifacts/spec-google-sso.md`
  summary: Soft-deleted users permanently squat their `(provider, provider_user_id)` identity — `user_identities` unique index is unconditional and FK cascade fires only on hard delete, so their Google sub can never log in or relink; handle identity cleanup when an account-deletion feature ships.
  evidence: Latent schema interaction found in review — `users` uses soft delete with partial unique indexes, `user_identities` does not; no deletion feature exists today so nothing is user-visible yet.

- source_spec: `_bmad-output/implementation-artifacts/spec-google-sso.md`
  summary: No rate limiting on unauthenticated auth endpoints (`/auth/login`, `/auth/sso/:provider`, `/auth/sso/:provider/link`) — each request costs a bcrypt compare and/or an outbound Google validation.
  evidence: Pre-existing platform gap (login was already unthrottled) widened by the two new SSO endpoints; needs a middleware decision, not a story-scoped patch.

- source_spec: `_bmad-output/implementation-artifacts/spec-google-sso.md`
  summary: Pre-existing auth pages (LoginPage, RegisterPage, ResetPasswordPage) hardcode English "Hide/Show password" aria-labels; move them to i18n keys across all four locales.
  evidence: Surfaced while reviewing the new LinkAccountDialog (whose labels are being fixed in-story); screen-reader users in mk/hr/sr currently hear English.

## Deferred from: code review of spec-profile-linked-accounts (2026-07-07)

- source_spec: `_bmad-output/implementation-artifacts/spec-profile-linked-accounts.md`
  summary: `UnlinkIdentity`'s "cannot unlink the last sign-in method" guard is a non-atomic read-check-delete (no transaction / row lock) and counts identities for de-registered providers as valid sign-in methods — a passwordless account with 2+ providers could be stripped of every usable login by two concurrent cross-provider DELETEs, or by unlinking its only usable provider while a de-registered one remains.
  evidence: Flagged independently by both review layers. NOT reachable today — only `google` is registered and the `(provider, provider_user_id)` + `(user_id, provider)` unique indexes cap a single-provider account at one identity, so `len(idents) <= 1` always holds for a passwordless account. Becomes live the moment a 2nd provider ships; fix belongs with that story (route the count-check + delete through a `FindByUserID`-in-a-tx primitive, and count only registry-resolvable providers). [server/internal/auth/profile_identity_handler.go:174-219 UnlinkIdentity]

- source_spec: `_bmad-output/implementation-artifacts/spec-profile-linked-accounts.md`
  summary: SSO-registered (passwordless) users have no profile-side account self-management screens — they can neither set a password nor change their username. A Google-registered account is therefore stuck with a machine-generated handle AND can never unlink Google, because the "cannot unlink your only sign-in method" guard requires a password there is no in-app way to set. Build one profile "Account" settings surface covering (a) set/change password and (b) change username.
  evidence: Confirmed live in the UI (2026-07-07): a Google-registered account shows "Linked as ..." with Unlink disabled and the hint "Set a password before unlinking your only sign-in method", yet no set-password screen exists — the only route to a password today is the public forgot-password email flow. Separately, the username is auto-generated from the Google display name (space/accents/non-ASCII stripped: "Emilijan Koteski" -> "EmilijanKoteski", collisions suffixed "...1", "...2") with no rename path. Needed: (1) an authed set/change-password endpoint reusing Register/ResetPassword bcrypt + validation, which flips `hasPassword` and unblocks Unlink; (2) an authed username-change (`PATCH /users/:id`) enforcing the 3-20 `[a-zA-Z0-9_]` + uniqueness rules (23505 -> USERNAME_TAKEN); (3) the profile UI for both. Also revisit accent/Cyrillic handling in `generateSSOUsername` so mk/sr/hr names get sensible defaults. `UserRepository` already has `UpdatePasswordHash` but no handler/UI, and no username update at all. [server/internal/auth/sso_handler.go:310 generateSSOUsername; server/internal/user/repository.go UserRepository; client/src/features/profile/components/LinkedAccounts.tsx passwordlessHint]

## Deferred from: code review of spec-abandonment-per-player-results (2026-07-09)

- source_spec: `_bmad-output/implementation-artifacts/spec-abandonment-per-player-results.md`
  summary: RETIRED 2026-07-19 — implemented after a user-reported profile bug (loss followed by an abandonment win showed "Cold streak · 1"). All three career-aggregate queries (streak, top partners, top rivals) now carry the same `abandoned_by IS NOT NULL AND abandoned_by <> viewer` gate as `GetStatsForUser`; pinned by integration tests incl. the exact reported scenario. Original entry follows.
  original_summary: Career aggregates (current streak, top partners, top rivals in `GetCareerAggregatesForUser`/`GetTopPartnersForUser`/`GetTopRivalsForUser`) still count only `status='completed'` matches, so abandonment-derived wins/losses now shown in headline stats and history are invisible to streaks/partner/rival records.
  evidence: Deliberate Ask-First boundary in the spec (kept completed-only to contain blast radius), but both review layers flagged the resulting inconsistency: a player whose matches ended by opponent abandonment gains wins in stats/history while streaks/partners/rivals pretend those matches never happened. Extending the three queries with the same `abandoned_by IS NOT NULL AND abandoned_by <> viewer` gate used in `GetStatsForUser` would restore consistency. [server/internal/match/gorm_repo.go:161-355]
- source_spec: `_bmad-output/implementation-artifacts/spec-bot-secure-take-and-boss-preserving-smear.md`
  summary: RETIRED 2026-07-15 — pulled back into the source spec by the PO the same day (endgame exception: at 2 cards in hand a cuttable non-trump boss is smeared, not hoarded; guard holds only when opponents are provably out of trump). No longer deferred.
  evidence: Emilijan: "this rule should not apply when it is trick 7 when the opponent provably retains a trump, i dont want to donate 11 pts". Implemented in bestSmear via len(hand)==2 + !opponentsOutOfTrump.
- source_spec: `_bmad-output/implementation-artifacts/spec-bot-secure-take-and-boss-preserving-smear.md`
  summary: Simulation-gated tuning pass over the new secure-take/boss-guard heuristics; four review-surfaced refinements rejected or out of scope for the spec but worth A/B-ing against the simulation harness - (1) cost/benefit threshold on the material-stake gate (today any >0 stake can spend a 20-pt master to secure a 3-pt trick), (2) endgame uncuttable-boss guard ignores WHO leads trick 8 (partner-led trick 8 makes the hoarded boss a coin flip), (3) partnerWinIsSafe's trump-threat leg is a raw unseen scan while securelyWins is seat-aware (bot ducks smears onto provably safe partner ruffs), (4) no arbitration between mid-hand secure-spend of the master and trick-7 retention pricing last-trick control at +10.
  evidence: Blind Hunter round 3 (2026-07-16) probe-verified each scenario against LegalCards; none regress the approved behavior vs baseline in a way the PO wanted changed now, but all four are measurable strategy dials the ≥60% simulation guard could adjudicate.

## Deferred from: code review of spec-mobile-dialog-font-i18n-fixes (2026-07-19)

- source_spec: `_bmad-output/implementation-artifacts/spec-mobile-dialog-font-i18n-fixes.md`
  summary: Suit names interpolate title-cased mid-sentence in mk/hr/sr score-reveal subtitles ("зедовте адут на Пик" / "zvali adut Pik") — non-standard orthography; would need lowercase suit-name variants in i18n.
  evidence: Pre-existing pattern (the old "твојот тим зеде {{suit}}" had it too); `match.suits.*` values are standalone title-cased labels reused mid-sentence. Fixing means new lowercase i18n entries and auditing every mid-sentence interpolation site, out of scope for this polish pass.
- source_spec: `_bmad-output/implementation-artifacts/spec-mobile-dialog-font-i18n-fixes.md`
  summary: Pre-existing `npx tsc --noEmit` failure on the branch — MatchmakingPage.test.tsx and RoomPage.bots.test.tsx RoomDetail mocks are missing the required `returnedUserIds` field.
  evidence: Blind Hunter reproduced it on files untouched by this spec; vitest and eslint gates pass, but any future type-check gate will trip on it. Introduced whenever `returnedUserIds` was added to RoomDetail without updating these two test mocks.

## Deferred from: code review of spec-mobile-back-history-shaping (2026-07-19)

- source_spec: `_bmad-output/implementation-artifacts/spec-mobile-back-history-shaping.md`
  summary: A match opened as the tab's first in-app history entry (manually typed/bookmarked /match/:id URL) has no back-press leave confirmation — the pop is a cross-document navigation that useBlocker cannot intercept, and the spec's frozen constraints ban the sentinel-entry and beforeunload workarounds.
  evidence: Both review agents confirmed the old pushState sentinel covered this case and useBlocker cannot. Residual risk is small (normal reconnect flows land on /lobby first and push into the match, so the blocker works there; server-side disconnect pause still protects the match). A spec-compliant fix exists — on deep-link match mounts, rewrite the entry to /lobby and push the match on top — but needs its own design pass against the splash/guard effects.
- source_spec: `_bmad-output/implementation-artifacts/spec-mobile-back-history-shaping.md`
  summary: The pre-existing error/guard redirects (RoomPage 284/326/377/1553, MatchmakingPage 119/135/145, MatchPage 754/826) still use navigate("/lobby", { replace: true }), which from a [lobby, page] stack leaves a duplicate [lobby, lobby] — one ghost back-press after rare error bounces.
  evidence: Blind Hunter traced the stack shapes. The spec deliberately kept these guards untouched as safety nets; converting them to returnToLobby() is safe only with per-site reasoning about their delayed timers and hasLeftRef unmount guards, so it was split out rather than patched blind.

## Deferred from: code review of 9-7-honor-score-system (2026-07-29)

- source_spec: `_bmad-output/implementation-artifacts/9-7-honor-score-system.md`
  status: PARTIALLY RESOLVED 2026-07-29 (E2E pass) — the visible contradiction is fixed; only the live re-sync remains deferred.
  summary: Absent players never receive their own `event:honor_updated`. The abandoning seat is by definition the one with no live socket when `handleSeatReconnectTimeout` broadcasts, so the player whose honor just got WORSE is the only one never told. What is LEFT deferred is only the live push: a tab that stays open with a valid access token shows the pre-abandonment score in the TopBar chip until its next token refresh (~11 min, deferred entirely while the tab is hidden), a reload, or a visit to /profile.
  evidence: Flagged independently by both blind layers. `Hub.SendToUser` (server/internal/ws/hub.go:178-185) is a silent no-op with no queue when the user has no registered client, and coins + XP already behave identically from the same three-loop broadcast block (reconnect.go:617-625) — this is an inherited pattern, not a 9.7 regression. Verified E2E 2026-07-29: nothing durable is lost. The charge is committed before the broadcast, and every read path recomputes from the stored weights, so the absent player's next login envelope AND profile read both returned the charged value (honor_b: charged to 31/problematic while its socket was closed, read back 31 on both paths). The write is correct; only one client's in-memory copy was stale. RESOLVED HERE: the worst symptom this entry described — "/profile can show a real 44 in HonorPanel beside a stale 90 in the header chip", i.e. two different numbers for the same quantity on one screen — is fixed by hydrating `authStore.user` from the profile response (`ProfilePage.tsx`, the second of the two fixes this entry originally proposed). STILL DEFERRED: re-sending the snapshot on WS reconnect, so a long-lived open tab self-corrects without visiting /profile. Deliberately not done for honor alone — coins and XP have the identical gap from the identical loop, and a honor-only push would diverge three sibling signals that today behave the same. [server/internal/match/reconnect.go:623-625; server/internal/ws/hub.go:178; fix at client/src/features/profile/ProfilePage.tsx]

- source_spec: `_bmad-output/implementation-artifacts/9-7-honor-score-system.md`
  summary: Honor commits in its own transaction BEFORE the match row is persisted, so a crash in that window leaves counters that the migration header's own "re-runnable reconciliation recipe" silently reverts. Worse, that recipe's `UPDATE ... FROM agg` only touches rows present in `agg`, so a user whose only honor came from a lost match row is never corrected at all — they are absent from the aggregate and keep the orphaned counters permanently.
  evidence: `recordHonor` is invoked at live_match.go:1168 and commits + broadcasts before `CreateWithHands` at live_match.go:1203 — 35 lines and one transaction boundary earlier, despite the comment at live_match.go:1126 stating the ordering contract as "Persist matchRecord (CreateWithHands) and update room status FIRST". Pre-existing and shared: settlement and XP sit in exactly the same band, and the abandonment finalizer broadcasts before persisting as a known open 8.5-1 item. Fixing it for honor alone would leave the same hole for coins and XP, so it belongs with a pass that moves all three inside (or after) the match-row transaction. [server/internal/match/live_match.go:1168 vs :1203; server/migrations/000017_add_honor_to_users.up.sql:29-40]

## Deferred from: code review of 9-7-honor-score-system, pass 2 (2026-07-29)

- source_spec: `_bmad-output/implementation-artifacts/9-7-honor-score-system.md`
  summary: The per-seat presence snapshot that Story 9.7's honor gate depends on fails OPEN. `HandleDisconnect`'s phase switch only marks `Connected = false` for drops observed in PhasePlaying / PhaseBidding / PhasePaused / PhaseHandComplete (plus the PhaseDisconnected concurrent path); the `default:` arm returns for the transient phases (dealing, trick_resolving, hand_scoring, match_end) without touching `Connected` and without arming a timer. That seat then reads `Connected == true` for the rest of the match, so if another seat later abandons, a genuinely-absent player is credited a completion.
  evidence: Found by the Edge Case Hunter on review pass 2 and confirmed by reading the switch. The `[F3]` phase gate is deliberate and predates honor (its comment explains the PhaseHandComplete inclusion in terms of a mobile-socket jam), so widening it would change disconnect/pause behaviour well beyond this story and needs its own design pass. Consequence inside 9.7 is LENIENT — the seat is credited, which is exactly the pre-2026-07-29 behaviour — and the transient phases are brief enough that deliberately timing a drop into one is impractical, so this weakens rather than defeats the anti-gaming gate. A fix would either widen the switch or cross-check the snapshot against live hub registration instead of trusting `Connected`. [server/internal/match/reconnect.go:105-108 HandleDisconnect; consumed at reconnect.go:570-574 -> match/honor_record.go:59]

- source_spec: `_bmad-output/implementation-artifacts/9-7-honor-score-system.md`
  summary: No index on `matches.completed_at`, so the honor trend query must materialise and sort the viewer's entire match-participation set on every profile load before its `LIMIT 40` takes effect. `EXPLAIN` on the dev DB shows `Limit -> WindowAgg -> Sort (completed_at DESC, id DESC) -> Seq Scan on matches`; existing indexes cover `player1..4_id`, `status`, `abandoned_by`, `room_id`, `has_bots` only.
  evidence: Surfaced by the Acceptance Auditor on review pass 2. Precedent-consistent rather than a regression — the pre-existing `GetMatchesForUser` has the identical shape and the same missing index — and harmless at current data volumes. Flagged because the story's prose and the repository doc comment both describe the fetch as a "bounded 2x20 window", which is true of the output rows but not of the work done to produce them. An index on `(completed_at DESC)` or a composite covering the participation predicate would fix both call sites at once; belongs with a profile-read performance pass, not here.

## Deferred from: implementation of 9-8-honor-gated-rooms (2026-07-30)

- source_spec: `_bmad-output/implementation-artifacts/9-8-honor-gated-rooms.md`
  summary: No owner endpoint to edit `min_honor` / `allow_new_players` after creation. The gate is create-time only, so an owner who wants to tighten or relax a live room must close it and make a new one. Story 9.8's Open Question 1.
  evidence: Deliberately out of scope — 9.8's ACs demand create-time config only, and the story's scope guardrails list the edit endpoint as follow-up work. 9.6 shipped `POST /rooms/:id/privacy` because its AC6 explicitly demanded it; nothing in 9.8's ACs does. The seam already exists: an edit endpoint would reuse `honorGateError` plus the AC6 mid-session re-check, but it needs a PO decision of its own for the "owner tightened the gate on already-seated players" case — eject them immediately, or grandfather them in until the next return/start? Both are defensible and the answer changes the endpoint's shape.

- source_spec: `_bmad-output/implementation-artifacts/9-8-honor-gated-rooms.md`
  summary: An honor ejection at match start tells the OWNER nothing about why. They receive a 409 and watch a seat vanish, with no statement that an honor gate did it — identical to today's insolvency behaviour. Story 9.8's Open Question 5.
  evidence: Inherited from 9.3 rather than introduced here: `ejectSeatsAtStart` pushes the reason-specific event only to the ejected player, and the remaining players get a plain `system:player_left`. Fixing it means a new owner-facing notice carrying which seat failed and why, which is a per-seat disclosure decision (revealing another player's honor state to the owner) that Epic 11's cross-player honor visibility story owns. Doing it for honor alone would also leave insolvency inconsistent.

- source_spec: `_bmad-output/implementation-artifacts/9-8-honor-gated-rooms.md`
  summary: Honor ejection is lazy — it fires at `ReturnToRoom` / `StartMatch`, not the instant honor drops. A player whose score falls below the threshold stays a room member until they try to come back or the owner starts. Story 9.8's Open Question 4.
  evidence: Accepted by design, not an oversight. Pushing a player out at match end would need the honor write path (in `match`) to reach into room membership (in `room`), crossing a package boundary this codebase deliberately keeps one-way (`room -> user -> match`, and `match` never imports `room`). The lazy check is also the one that fires in practice: honor only moves at match end and the abandoner's `room_players` row survives, so abandon -> reconnect -> "Return to room" is the realistic sequence and it IS gated. The only exposure is a seat that looks occupied in the lobby for slightly longer than strictly necessary.

- source_spec: `_bmad-output/implementation-artifacts/9-8-honor-gated-rooms.md`
  summary: The usable range of `min_honor` is far narrower than the 0-100 the field accepts. Because of the Beta(4,1) prior a fresh account sits at 80, a flawless 20-match record caps at 96, and a rounded 100 needs ~195 decayed completions inside a single 90-day half-life — so anything above ~95 is effectively "nobody" and anything at or below 80 admits every newcomer by default. Story 9.8's Open Question 6.
  evidence: The create-modal hint now steers owners toward the meaningful 85-95 band and the migration header records the arithmetic, but no value is warned about or rejected: the DB CHECK and the handler both accept the full range (owner freedom, matching `coin_buy_in` having no maximum). Retuning `honorPriorCompleted` / `honorPriorAbandoned` would move the whole band, and 9.7's Open Questions 1-3 already flag those constants as unvalidated placeholders — so a warning threshold hard-coded now would be tuned against numbers that are themselves provisional.

- source_spec: `_bmad-output/implementation-artifacts/9-8-honor-gated-rooms.md`
  status: RESOLVED 2026-07-30 (fixed in this branch at the PO's direction, overriding 9.8's scope guardrail).
  summary: `000017_add_honor_to_users.down.sql` advertised the up migration's backfill as "written to be re-runnable as a reconciliation recipe", which the up migration's own header flatly contradicts ("THE BACKFILL BELOW IS DEPLOY-ONLY. DO NOT RE-RUN IT").
  evidence: Found while writing migration 000018. Pre-existing on the 9.7 branch: review pass 2 corrected the up-migration header but not the down file, so the two disagreed about the same statement. Initially left untouched because 9.8's scope guardrails forbid editing migration 000017; the PO chose to fix it here instead, since both files ship in the same feature. FIX: the down header now states plainly that the reversal is LOSSY and that a down-then-up cycle re-runs the deploy-only backfill by construction, enumerates the two things that are destroyed (operator pardons, and the concurrent-double-disconnect rule -- `matches` has no per-seat presence, so a second absent seat gets re-credited a completion), records that the "re-runnable" claim was stale so nobody restores it, and points at the correct reconciliation approach. Comment-only: `git diff` confirms not one SQL statement changed, and the six DROPs were executed against the live dev schema inside a rolled-back transaction (all six columns dropped cleanly, then rolled back -- dev DB left at version 18 with all six columns intact, deliberately avoiding a real `down 2; up 2` cycle because that would itself re-run the backfill this comment warns about).

## Deferred from: code review of 9-8-honor-gated-rooms (2026-07-30)

- source_spec: `_bmad-output/implementation-artifacts/9-8-honor-gated-rooms.md`
  summary: `ReturnToRoom`'s gates — both the 9.3 insolvency gate and the new 9.8 honor gate — run before the transaction that rejects a `playing` room with `ErrMatchAlreadyStarted`. A stale client's `POST /rooms/:id/return` arriving while a match is live would therefore free the caller's `room_players` row, decrement `player_count`, and (if they were the owner) transfer ownership or close the room, all under a live match session.
  evidence: Found independently by the Blind Hunter and the Edge Case Hunter. Pre-existing rather than introduced: the insolvency gate directly above the honor gate has the identical shape and shipped in 9.3, and the honor gate was placed after it exactly as D3 specified. Both reviewers labelled the stale-client precondition UNPROVEN — every path traced requires the failing player to have already passed the return gate before the match started. The honor gate does make the surrounding path more travelled, since honor moves at match end, which is precisely when a lagging client is most likely to still be holding a "Return to room" affordance. Cheap fix if it is ever taken: check `Status == "playing"` before both gates rather than inside the reopen tx. Deferred because it touches 9.3's hardened path for an unproven trigger.

- source_spec: `_bmad-output/implementation-artifacts/9-8-honor-gated-rooms.md`
  summary: When `ejectSeatsAtStart`'s transaction rolls back, the per-user ejection push still fans out. Players whose seats were NOT actually freed receive `system:honor_ejected` (or `system:insolvent_ejected`), so their client redirects to the lobby, shows the ejection modal and sets `hasLeftRef`, while server-side they are still seated. [server/internal/room/handler.go:1600-1608]
  evidence: Unchanged 9.3 code, and the asymmetry is explicit: the comment at :1600 says "Fall through: still push the per-user ejections + lobby refresh below", while the `system:player_left` fan-out at :1627 IS correctly guarded on `txErr == nil && !roomClosed` ("the seats were not actually freed"). The two reviewers appeared to disagree here and were in fact describing different broadcasts — `player_left` is suppressed, the per-user push is not. Deferred because it is pre-existing and shared with insolvency; honor only makes the path more travelled. A fix would guard the push loop on `txErr == nil` too, but that trades a false ejection notice for silence on a genuine one, so it needs its own call.

- source_spec: `_bmad-output/implementation-artifacts/9-8-honor-gated-rooms.md`
  summary: Two other hand-built room payload maps omit `minHonor` / `allowNewPlayers`: the QuickPlay `system:room_created` broadcast and the disconnect-driven `system:room_updated`. [server/internal/room/handler.go:3615-3628, server/internal/room/lobby_disconnect.go:222-236]
  evidence: AC5's rationale ("this map is hand-built, not the struct") applies verbatim to these two, which the story never enumerated — but the hole is older and broader than 9.8: both maps already drop `coinBuyIn`, `isPrivate`, `players` and `ownerUsername`. Currently masked in both cases. The QuickPlay map only ever describes a synthesized room, which IS ungated by construction (AC12), so `undefined` renders as no chips, which is correct. The disconnect payload's consumer merges (`{...r, ...payload}`), so cached honor values survive. The residual exposure is `useRoomUpdates`' upsert branch, which inserts a stripped payload as a whole `Room` — a gated room re-added to the grid that way would render without chips and interpolate `undefined` into the join-rejection toast. The right fix is to route all three maps through `roomLifecyclePayload` rather than patch two more key lists, which is a refactor of its own.

- source_spec: `_bmad-output/implementation-artifacts/9-8-honor-gated-rooms.md`
  summary: `ejectNotice` carries only `kind: ejectKindHonor` and never which of the two gates failed, so a `NEW_PLAYER_NOT_ALLOWED` ejection is pushed with `honorNotice(room.MinHonor, snap.Score)` and the modal renders "Your honor dropped below this room's minimum of 0 — it's now 80." [server/internal/room/handler.go:1146,1935 -> RoomEjectionModal.tsx:130-139]
  evidence: Confirmed UNREACHABLE on this branch, and the reasoning is what makes it worth recording rather than fixing blind: `IsNewPlayer` only ever transitions true->false (9.7's `ResetHonor` preserves `honor_completed_total`), the join gate blocks New Players at entry, `CreateRoom`'s D7 self-gate blocks a New Player owner from setting `allow_new_players = false`, and there is no owner-edit endpoint. It goes live the moment the deferred owner-edit endpoint (Open Question 1, above) ships, because an owner tightening `allow_new_players` on a seated New Player is exactly that endpoint's motivating case. Widen the notice to carry the failing gate at the same time as that endpoint, not after.

- source_spec: `_bmad-output/implementation-artifacts/9-8-honor-gated-rooms.md`
  summary: `ReturnToRoom`'s insolvency eject passes `nil` for `extraEligible`, so in a room with `min_honor > 0` an insolvent owner's seat can transfer to a present-and-solvent heir who does not pass the honor gate. The gated room is then transiently owned by someone barred from it. [server/internal/room/handler.go:1890]
  evidence: Raised by the Acceptance Auditor as informational, and it is NOT an AC7 violation — AC7's Given is scoped to "the owner is honor-ejected", and the honor-ejection path does supply the predicate. The `StartMatch` insolvency call sites justify their own `nil` soundly (the honor prefilter runs first and returns early on any failing seat), and `startAutoStartedMatch`'s two are correct because quick-play rooms are ungated. Consequence is bounded and self-correcting: the barred heir is ejected at the next return or start, which is the documented lazy-check behaviour already deferred as Open Question 4. Fixing it means threading an honor predicate into the insolvency path too, which widens 9.3's surface for a transient state that resolves itself.

- source_spec: `_bmad-output/implementation-artifacts/9-8-honor-gated-rooms.md`
  summary: A stranger who completes `JoinRoom` + `SelectSeat` between the hoisted `members` read and the eject transaction is judged an ineligible heir (they are absent from the pre-read candidate snapshot map), so the room closes instead of transferring to them. [server/internal/room/handler.go:1856 vs the in-tx re-read at :1221]
  evidence: Raised by the Edge Case Hunter with reachability explicitly UNPROVEN — it needs two HTTP requests landing inside a few-millisecond window, and it cannot happen at match start (the room is `playing`, so `JoinRoom` rejects). Structurally a new instance of the pre-existing hoisted-`balances` race directly above it, which is inherent to reading eligibility inputs outside the row lock — and reading them outside the lock is itself a deliberate, reviewed decision (no wallet or honor read inside a transaction). Fails conservatively: it closes a room rather than mis-assigning ownership. Joins the same class as the non-transactional room-mutation TOCTOU already deferred from 9.6.

## Deferred from: honour redesign (2026-07-31)

- source_spec: `_bmad-output/implementation-artifacts/spec-honour-redesign.md`
  summary: The TopBar honour chip cannot show a newcomer's "2 / 5" progress, so it keeps the bare "New Player" label — a label that says nothing about how to leave that state. The profile band DOES show the counter, so the two surfaces are deliberately asymmetric.
  evidence: The auth envelope carries only `honorScore` / `honorTier` / `isNewPlayer` (`server/internal/auth/handler.go:60-62`); the raw counts are not on it. This is the cheapest of the three dropped items: `user.NewHonorSnapshot` ALREADY computes `CompletedTotal` and `AbandonedTotal` (`user/honor.go:131-140`), so it is two struct fields plus two assignments with NO extra query. The handler comment at `:57-59` justifies excluding only the TREND (which needs a `matches` query); the counts' omission is not justified anywhere and looks incidental. Worth doing the next time the envelope is touched.

- source_spec: `_bmad-output/implementation-artifacts/spec-honour-redesign.md`
  summary: No recovery estimate anywhere. The ejection modal says how far below the bar you are but not how many finished matches would put you back above it, and the explainer cannot say "two more keeps you Exemplary". Both were drawn in the design (R8, R2) and both were dropped.
  evidence: The client PROVABLY cannot compute this, which is why it was dropped rather than approximated. Let `D = C+4`, `E = 4A+1`; the client knows only `s = 100·D/(D+E)`, which fixes the ratio `E = D(100−s)/s` but not `D` itself. Adding `n` completions gives `s' = 100(D+n)/(D+n+E)` — a function of `D`, which is unrecoverable because the decayed weights and `honorDecayedAt` are all `json:"-"` (`user/model.go:44,45,49`). Substituting the raw undecayed `honorCompletedTotal`/`honorAbandonedTotal` is NOT equivalent: it silently assumes `f = 1`, so for any account with history older than a few weeks it overstates both weights and yields a number that contradicts the authoritative score displayed on the same screen — exactly the class of bug the `honor_score`-snapshot warnings at `user/model.go:64-73` exist to prevent. Two options: expose the weights + `honorDecayedAt` on `ProfileResponse`, or (cleaner, and consistent with the server-authoritative doctrine at `honor.go:8-28`) add a computed `honorMatchesToNextTier`.

- source_spec: `_bmad-output/implementation-artifacts/spec-honour-redesign.md`
  summary: The abandoning player never sees their own honour movement on the result surface. The match-result overlay now shows "Honour 95 -> 96" for everyone who finishes, but the player who dropped out sees nothing — which is precisely the player the number was most likely to teach something.
  evidence: A visible consequence of an already-deferred 9.7 limitation: `Hub.SendToUser` is an unqueued no-op, so an absent player never receives their own `event:honor_updated`, and the abandoning seat is BY DEFINITION absent when `handleSeatReconnectTimeout` fires. Until this redesign that had no user-visible effect (the event was discarded visually anyway); now it means one outcome of four renders no honour row. The stash is correctly reset in both the `match_end` and `match_abandoned` handlers so a STALE movement is never shown in its place — showing nothing is the honest degradation. Fix options: refetch the profile when the abandoned overlay mounts (client-only, cheap), or queue per-user pushes for absent players (the general fix, which also closes the 9.7 item).

- source_spec: `_bmad-output/implementation-artifacts/spec-honour-redesign.md`
  summary: `MatchHistory.tsx:231` still carries `bg-surface-2`, a class that generates no CSS.
  evidence: `--surface-2` exists as a raw `:root` var but has no `--color-*` alias in `@theme inline`, so the utility is never generated and the element renders transparent. Found while migrating `HonorPanel` (which had the same dead class at `:64` — that one is gone with the file). Left alone here because it is outside this spec's surfaces and is a pre-existing cosmetic issue, not something the redesign introduced. One-word fix when someone is next in that file: `bg-surface-elevated`.

## Deferred from: 11-3 (public player profiles, 2026-08-14)

- source_spec: `_bmad-output/implementation-artifacts/11-3-public-player-profiles.md`
  summary: The public profile page (`/players/:id`) has no "Add Friend" action. Story 11.3 delivers the public READ surface only; the friend request button is Story 11.2's to add onto it. [client/src/features/profile/PublicPlayerProfilePage.tsx — insertion comment left where the button mounts]
  evidence: DELIBERATE scope handoff (Design Decision D4), not an oversight. There is zero friend backend on `master` — no model, endpoint, or friendship-state — so a button here would be dead/placeholder or would drag 11.2's whole scope into this story, contradicting the reorder rationale (do not build against non-existent dependencies). AC4 layout parity still holds without it: Add Friend is an ADDITIVE element, not part of the read layout. 11.2 mounts the button at the documented insertion point in PublicPlayerProfilePage.tsx (just under the IdentityHero) and adds the friend model + `POST` endpoint; nothing in 11.3 needs revisiting.

## Deferred from: code review of 11-3-public-player-profiles (2026-08-14)

- source_spec: `_bmad-output/implementation-artifacts/11-3-public-player-profiles.md`
  summary: No rate-limiting or caching on the newly-public read endpoints `GET /users/:id/{profile,career,matches}`. Lifting the self-gate lets any authenticated viewer enumerate the whole user table, forcing ~5 uncached SQL round-trips per profile hit (`FindByID` + `GetStatsForUser` + honor recompute + honor-trend scan; career adds 4 aggregates incl. the new `GetCareerPointsForUser`). [server/internal/user/handler.go:254,387,645]
  reason_deferred: Auth-gated + indexed lookups; rate-limiting is a platform-wide gap best solved by a dedicated infra story, not scoped into 11.3.
  evidence: The *absence* of HTTP rate-limiting/caching is a pre-existing Phase 1-2 posture (the only limiter in the server is the per-user WS emote cooldown); what 11.3 introduces is the *exposure* of these three specific endpoints to cross-user enumeration. Bounded by the auth requirement (attacker needs a valid account) and by the queries being indexed lookups. The right fix is a platform-wide rate-limit + short-TTL cache story, not two more guards bolted onto this handler.

- source_spec: `_bmad-output/implementation-artifacts/11-3-public-player-profiles.md`
  summary: The "New Player" honor counter can render "N / 5" with N > 5. The numerator is `completedTotal + abandonedTotal` against the fixed denominator `HONOR_NEW_PLAYER_MIN_MATCHES` (5), so a player who is still a New Player by completed count but has abandonments can display e.g. "7 / 5". [client/src/features/profile/components/HonorHeroBand.tsx:93]
  evidence: PRE-EXISTING Story 9.7 behavior, reused verbatim by 11.3 — the spec explicitly directs reuse ("HonorHeroBand handles isNewPlayer 'N / 5' internally"), so it is out of scope for an 11.3 acceptance gap and was surfaced by the Acceptance Auditor as a non-blocking observation. `isNewPlayer` is gated on completed matches only (`< HONOR_NEW_PLAYER_MIN_MATCHES`), while the progress fraction beside it adds abandonments into the numerator. Now more visible because 11.3 renders this band on every player's public profile. Fix when next in HonorHeroBand: either clamp the numerator to the denominator, or base the fraction on the same completed-only count the `isNewPlayer` flag uses.

## Deferred from: code review of 11-1-player-search (2026-08-14)

- source_spec: `_bmad-output/implementation-artifacts/11-1-player-search.md`
  summary: The newly-public authenticated search endpoint `GET /api/v1/users?search=` has no rate-limiting or throttling. Any authenticated account can enumerate usernames via repeated substring probes (each response ordered, capped at 10, self + soft-deleted excluded). [server/internal/user/handler.go]
  reason_deferred: Auth-gated + result-capped + usernames are already public in-game; rate-limiting is a platform-wide gap best solved by a dedicated infra story, not scoped into a search story.
  evidence: Same platform-wide posture already deferred by Story 11.3's review for the public profile reads — the only limiter in the server today is the per-user WS emote cooldown. 11.1 introduces the first `ILIKE` search endpoint; the enumeration surface is bounded (a valid account is required, ≤10 rows per response) but unthrottled. The right fix is a platform-wide rate-limit + abuse-guard story covering all newly-public authenticated reads, not a bespoke guard bolted onto this one handler.

## Deferred from: implementation of 11-2-friend-requests-and-friend-list (2026-08-15)

- source_spec: `_bmad-output/implementation-artifacts/11-2-friend-requests-and-friend-list.md`
  summary: `TestGormUserRepository_SearchByUsername` (the Story 11.1 DB-backed search test) is NOT isolated to its own fixtures — its "case-insensitive substring, self + soft-deleted excluded" subtest asserts that a search for `ali` returns exactly `[Alice, alicia]`, but the query runs against the WHOLE shared dev DB (`:5433`). Leftover Playwright/manual-E2E accounts (`alice_e2e`, `alicia_e2e`, created 2026-08-15 by a separate search-feature E2E session) also match, so the subtest FAILS against a polluted dev DB. [server/internal/user/user_test.go:155]
  reason_deferred: Pre-existing test-isolation weakness in a file Story 11.2 never touches. Story 11.2's own DB tests are correctly isolated (per-test `tx.Begin()` + `t.Cleanup(Rollback)`) and leaked nothing — verified 0 `friendships` rows and 0 `@f.test` users remained after the run. Fixing the search test (scope its assertions to test-created ids, or run it against an ephemeral DB) is unrelated to friend requests, and deleting E2E accounts this story did not create is out of scope (and the wrong fix — the test should not depend on global DB state). Story 11.2's `go test ./...` is otherwise fully green (all 19 packages incl. the new `friend` package pass).
  evidence: The failing assertion's diff is purely the two `_e2e` rows (`extra elements in list B: alice_e2e, alicia_e2e`); Story 11.2 touches only `internal/friend` (new), `internal/apperr`, `internal/ws`, and `cmd/api` — none of which the search test exercises. A clean DB or a CI ephemeral DB passes the subtest unchanged. The right fix is to make `SearchByUsername`'s substring subtest assert against only the ids it seeded (or reset the `users` table for that test), owned by whoever next hardens the Story 11.1 search tests.

## Deferred from: code review of 11-4-friend-whisper-chat (2026-08-16)

- source_spec: `_bmad-output/implementation-artifacts/11-4-friend-whisper-chat.md`
  summary: Draft is cleared optimistically before a whisper can be rejected. `sendWhisper` calls `setDraft("")` the instant the `action:whisper` is emitted; if the server rejects with `error:not_friends` / `error:whisper_blocked_in_game` / `error:whisper_recipient_offline`, the user's typed message is already gone and only a toast remains — they must retype. [client/src/features/chat/ChatDock.tsx:205-209]
  reason_deferred: Inherits the established Epic 6 chat optimistic-clear pattern — there is no per-message delivery ack to correlate a rejection back to the draft, so a clean restore-on-failure needs a client-side pending/echo-correlation mechanism that whisper deliberately does not add (ephemeral, no ack). More noticeable for whisper than chat only because whisper surfaces rejections as toasts where chat drops silently. Low impact; retype cost is small. Fix belongs to a future chat-input hardening pass (optimistic pending state + rollback) shared across all channels, not this story.
  evidence: `sendWhisper` (ChatDock.tsx:205-209) sets the draft empty unconditionally before the WS round-trip; the three whisper error branches in useWsDispatch.ts:959-970 only `toast.error` and never repopulate the input.

- source_spec: `_bmad-output/implementation-artifacts/11-4-friend-whisper-chat.md`
  summary: The `Tab` key is intercepted for whisper channel cycling whenever ≥1 whisper thread exists — even while focus is on the primary channel input — so a keyboard-only user cannot Tab from the message input to the Send button or out of the dock while any thread exists. [client/src/features/chat/ChatDock.tsx:432-435]
  reason_deferred: This is the spec-mandated (AC5) Valorant-style Tab cycling, accepted as a deliberate tradeoff (PO/review decision 2026-08-16). Low impact — Send remains clickable and Enter still sends, so the dock is fully operable; only the standard Tab-to-next-control focus path is overridden. Refining the scope (e.g., only cycle when already on a whisper thread, or preserve a focus-escape path) is a design choice, not a clear bug fix, and belongs in a dedicated a11y pass across the chat dock rather than this story.
  evidence: `onKeyDown` at ChatDock.tsx:432-435 calls `e.preventDefault()` + `cycleChannel(...)` for any `Tab` while `threadKeys.length > 0`, regardless of the active channel; there is no alternate keyboard path out of the input while a thread exists.

## Deferred from: implementation of 11-5-friend-room-invites (2026-08-16)

- source_spec: `_bmad-output/implementation-artifacts/11-5-friend-room-invites.md`
  summary: `TestLobbyDisconnect_FreesAfterTimeout` fails under `go test -race`: the test double `mockRepoForLobby` has no mutex, so its `RemovePlayer` / `FindPlayersByRoomID` maps are written by the `LobbyDisconnectHandler`'s `time.AfterFunc` goroutine while the test goroutine reads them. Two `WARNING: DATA RACE` reports, then `race detected during execution of test`. [server/internal/room/lobby_disconnect_test.go:48]
  reason_deferred: PRE-EXISTING and unrelated to Story 11.5 — verified by stashing the entire story (`git stash -u`) and reproducing the identical failure on the baseline commit `a0db52f`. Story 11.5 touches neither `lobby_disconnect.go`, `lobby_disconnect_test.go`, nor `NewLobbyDisconnectHandler` (the invite auto-void on disconnect is wired in `main.go`'s `hub.SetDisconnectHandler`, deliberately, because `LobbyDisconnectHandler.HandleDisconnect` early-returns for users not in a room — i.e. every invitee). `make test` does not pass `-race`, so CI is unaffected. Fixing it means adding a `sync.Mutex` to the test double, which belongs to whoever next hardens the lobby-disconnect tests.
  evidence: The race is entirely between the timer goroutine and the test goroutine over the mock's own slices/maps; no production type appears on either side of either report. Story 11.5's own concurrent code IS race-clean — `go test ./internal/room/ -race -run 'TestInvite|TestListInvitable|TestJoinRoom'` passes, including `TestInviteRegistry_ConcurrentAccess` (50 goroutines issuing/consuming/voiding grants).

## Deferred from: code review of 11-5-friend-room-invites (2026-08-16)

- source_spec: `_bmad-output/implementation-artifacts/11-5-friend-room-invites.md`
  summary: The invite panel issues one `FindPlayerRoom` DB round-trip per friend (`availabilityReason`), so a 100-friend roster is 100 queries — and the hook uses `staleTime: 0` + `refetchOnMount: "always"`, so every panel open pays it. `InviteToRoom` compounds this with redundant re-reads: `FindByID` + `FindPlayersByRoomID` (requireRoomMember) → `FindPlayerRoom` → `FindByID` + `FindBotsByRoomID` (roomIsFull) → `FindPlayersByRoomID` again (`usernameInRoom`), even though the caller's username is already in the slice `requireRoomMember` loaded and discarded. [server/internal/room/invite_handler.go:143-150,212,228,304,337]
  reason_deferred: Perf-only, no correctness impact, and the central design choice is deliberate and defended: Task 2 named the batched `lobby.GetStats` recipe (`FindUserIDsByRoomStatus("waiting")`), but the implementation chose per-friend `FindPlayerRoom` because it is the *same predicate* (waiting OR playing) that `JoinRoom`'s already-in-room gate uses — so the panel can never advertise a friend whose join would then be rejected. Batching that without losing the predicate equivalence is real work. Friend lists are small in Phase 1-2. Belongs to a future invite/presence batching pass alongside any friend-list scaling work.
  evidence: `ListInvitableFriends` loops the friend rows calling `availabilityReason`, which calls `repo.FindPlayerRoom(friendID)` per iteration; no batching or cap. `useInvitableFriends.ts` sets `staleTime: 0` and `refetchOnMount: "always"`.

- source_spec: `_bmad-output/implementation-artifacts/11-5-friend-room-invites.md`
  summary: `npx tsc --noEmit` against the full `tsconfig.json` reports ~30 type errors, all in client test files — missing `minHonor` / `allowNewPlayers` on `Room` literals and missing `returnedUserIds` on `RoomDetail` literals across `MatchmakingPage.test.tsx`, `MatchmakingDiagram.test.tsx`, `MatchPage.test.tsx`, `RoomPage.bots.test.tsx`, `RoomPage.diamond.test.tsx`, `RoomPage.locale.test.tsx`, `RoomPage.test.tsx`. Nothing in CI catches them: `make lint` runs only ESLint + Prettier, and `npm run build` uses `tsconfig.build.json`, which excludes `**/*.test.ts(x)`. [client/src/features/**/*.test.tsx]
  reason_deferred: PRE-EXISTING and not caused by Story 11.5 — the fields originate in Stories 9.7/9.8, and this story's `apiTypes.ts` change is purely additive (adds the `InvitableFriend` interface, widens nothing, makes nothing required). Verified: `tsc -p tsconfig.build.json --noEmit` is clean, `vitest` is fully green (110 files / 1192 tests), and every other gate passes. The real issue is the CI gap — type errors in test files are structurally invisible. Fixing means updating the stale fixtures AND adding a test-inclusive typecheck step to `make lint`, which is a project-wide chore, not a Story 11.5 change.
  evidence: `npx tsc --noEmit` (root tsconfig) errors exclusively in `*.test.tsx` files this diff never touches; `npx tsc -p tsconfig.build.json --noEmit` exits clean; `client/tsconfig.build.json` contains `"exclude": ["**/*.test.ts", "**/*.test.tsx", "src/test-setup.ts"]`; `Makefile` `lint:` target runs `npx eslint . && npx prettier --check .` with no `tsc`.
  update_2026-08-20: CLOSED. The count had grown from ~30 to **101** by 2026-08-20 (every one still in a `*.test.ts(x)` file). Fixed by (a) adding `makeRoom` / `makeRoomPlayer` / `makeRoomDetail` / `makeRoomInvite` to `client/src/test-utils.tsx` alongside the existing `makeUser`, and routing every stale fixture through them, so the NEXT required field on `Room`/`RoomDetail`/`RoomInvite` is a one-line change instead of a 57-site break; (b) installing `@types/node` and adding `"node"` to the root tsconfig's `types`, with `tsconfig.build.json` overriding `types` back to `["vite/client"]` so node globals stay invisible to browser source; (c) `make lint` now runs `npx tsc --noEmit` (root tsconfig, tests INCLUDED) ahead of eslint/prettier. Verified: `tsc --noEmit` 0 errors, `tsc -p tsconfig.build.json` clean, 1352/1352 vitest green, `make lint` green.
  found_while_fixing_2026-08-20: the stale `RoomInvite` fixture was hiding a real assertion gap — `RoomInviteModal.test.tsx` asserted `room.errors.honorTooLowGeneric` for HONOR_TOO_LOW only because its fixture omitted `minHonor`. A real invite always carries it (Story 11.5 added the field for exactly this), so the invite path takes the SPECIFIC arm of `joinFailure`. Test split out and corrected to assert the named floor.

## Deferred from: code review of randomize-first-dealer (2026-08-18)

- source_spec: `_bmad-output/implementation-artifacts/spec-randomize-first-dealer.md`
  summary: Mutation-detection strength for `live_match.go:310` (`maybeScheduleBotAction` in `StartMatch`) dropped from 100% to ~75% of runs, and the surviving detector reports the regression as a generic 20s timeout.
  reason_deferred: A deterministic fix needs the opening bidder guaranteed to be a bot without calling `BotSchedule`, which means an all-bot match (`mixedPlayers(0,1,2,3)`). That configuration is never exercised today (no human seats at all, so `humanUserIDs` is empty and every broadcast goes nowhere) and validating it is its own piece of work. Not worth risking new flakiness inside this change.
  evidence: Measured directly. With line 310 replaced by a comment, `go test -count=6 -run TestBot ./internal/match/` fails `TestBotMatch_ProgressesToNextHandWithoutClientActions` in 5 of 6 runs; it survives when the random dealer is 3, which puts the human seat 0 on the opening bid so nothing needed scheduling. Before this change dealer was always 0, so the same mutation was caught every run.

- source_spec: `_bmad-output/implementation-artifacts/spec-randomize-first-dealer.md`
  summary: `TestBot_RemoveSessionCancelsPendingBotTimers` cannot actually prove timer cancellation -- a leaked `time.AfterFunc` holding the dead `LiveMatch` (with its game state, bot memory and player IDs) alive would still look silent.
  reason_deferred: `RemoveSession` sets `session.closed = true` (live_match.go:579) BEFORE `cancelAllBotActionTimers()` (:582), and `applyAndBroadcastActionWith` returns early on `session.closed` (:365-369). So the observable outcome is identical whether or not the timer was cancelled. Proving cancellation needs a timer-introspection seam in `export_test.go` (e.g. `BotTimerPendingForTest(roomID, seat)`), which is new test surface better added deliberately with its own tests.
  evidence: Replacing `session.cancelAllBotActionTimers()` at live_match.go:582 with a comment leaves the test passing. The assertion has been relabelled in this change to state that it pins the outcome rather than the mechanism.

- source_spec: `_bmad-output/implementation-artifacts/spec-randomize-first-dealer.md`
  summary: `simulation_test.go` pins the dealer to seat 0, which hands the positional advantage (opens bidding, leads trick 1, anchors the declaration tie-break) to the random-legal baseline team for all 200 hands; removing the pin would restore symmetry.
  reason_deferred: Not a regression -- `NewGame` hardcoded dealer 0 before this change, so the >=60% threshold was calibrated under exactly this bias. Removing the pin is a strict improvement rather than a fix, and it changes what the bot-quality gate measures, so it belongs with whoever owns the bot heuristics.
  evidence: `bidding.go:124` (opening bidder leads trick 1) and `declarations.go:440` (`trickLeaderSeat := (state.DealerSeat + 1) % 4` decides equal-value declaration contests). Measured heuristic share over five runs: pinned 70.2-73.4%, unpinned 68.2-74.3% -- both clear the 60% threshold, so the pin buys no stability.

- source_spec: `_bmad-output/implementation-artifacts/spec-randomize-first-dealer.md`
  summary: In `TestBot_ActsAfterMinDelayAndBeforeTimerExpiry` the `elapsed < 10s` ceiling no longer observes a real per-move timer -- it is arithmetic against the literal `10` passed to `StartMatch`.
  reason_deferred: `StartMatch` arms the turn timer via `startTimerLocked(session, gs.ActivePlayerSeat)` for the RANDOM dealer's opening seat, and `SetGameStateForTest` only swaps `lm.gameState` without re-arming. The injected `NewGameJustDealt()` also carries no `TurnExpiresAt` and no `TimerDurationSec`. Restoring a genuine timer observation needs either a re-arm seam or a fixture that carries timer state -- more than a comment fix, and the >=1s floor (the assertion this test exists for) is unaffected.
  evidence: `live_match.go:302` arms against `gs.ActivePlayerSeat`; `export_test.go:101-111` swaps state only; `testfixtures/fixtures.go:33-46` sets no timer fields.

- source_spec: `_bmad-output/implementation-artifacts/spec-randomize-first-dealer.md`
  summary: `testfixtures.NewGameJustDealt()` is not a faithful stand-in for a `StartMatch` session state, and three bot-driver tests now push it through the real broadcast path.
  reason_deferred: `NewGameJustDealt` leaves `DisconnectedSeat` at the zero value, which means "seat 0 is disconnected", whereas `NewGame` explicitly sets `-1` for "nobody disconnected" (state.go:214); `OwnerSeat` is likewise 0 rather than the owner `StartMatch` resolves (live_match.go:264-270). Nothing reads those fields in these tests today, so nothing fails -- but any future assertion about pause-override or reconnect behaviour in them would reason about a state production cannot produce. Fixing the factory affects every fixture consumer and needs its own sweep.
  evidence: `testfixtures/fixtures.go:33-46` versus `state.go:214` and `live_match.go:264-270`.

- source_spec: `_bmad-output/implementation-artifacts/spec-randomize-first-dealer.md`
  summary: `NewGame` has no seam for a caller-chosen dealer, so every test needing a known first dealer must work around it.
  reason_deferred: Adding one was explicitly gated as "Ask First" in the spec and not approved, so it stayed out of scope. Recorded because the workaround cost is already visible: `simulation_test.go` mutates the struct after construction (leaving deal order and bidding rotation disagreeing) and the bot-driver tests inject a whole replacement fixture. A `NewGameWithDealer(..., dealerSeat int)` with `NewGame` delegating to it would remove both.
  evidence: `state.go:200` reads the package-global `math/rand/v2` with no injection point; `internal/game` has no `export_test.go`, and one would not help cross-package anyway.

- source_spec: `_bmad-output/implementation-artifacts/spec-bot-forced-overplay-secure-take.md`
  summary: On a trump-led trick with `trickPoints == 0` the bot declines to spend a control trump, but Bitola's overplay rule FORCES the remaining seats to contribute trumps -- so a "worthless" trump-led trick is often not worthless, and the bot concedes real material to keep the J/9.
  reason_deferred: `trickPoints(v, trump) > 0` was chosen deliberately as the trump-lead threshold (PO decision, 2026-08-18) because it is the test that stops the bot burning a master to win nothing. Re-pricing it to anticipate forced contributions would reopen exactly the pointless-trick guard that review loop 1 was about, on a spec that had already looped twice. Tune it behind a simulation harness that can actually measure the change (see the sibling entry on `TestSimulation_*`).
  evidence: Measured through `game.ApplyAction` by an adversarial review agent: trump hearts, seat 3 leads 8H (0 pts), bot holds JH QH 7C, seat 1 holds AH as its only heart, partner void in hearts and trump. Bot plays QH (current rule) -> final 0 / 14 against the bot's team; bot plays JH -> 31 / 0 for it. A 45-point swing on a trick the threshold calls worthless. Guard at `bot.go` `trumpEconomyTake`, trump-lead branch.

- source_spec: `_bmad-output/implementation-artifacts/spec-bot-forced-overplay-secure-take.md`
  summary: `TestSimulation_HeuristicBeatsRandomBaseline` cannot detect bot strategy regressions, so it is not a usable quality gate for heuristic tuning.
  reason_deferred: Only the random baseline's RNG is seeded; `game.NewGame` shuffles freely, so deals differ run to run. Making the simulation deterministic needs a dealer/shuffle seam in `internal/game`, which is itself already deferred ("`NewGame` has no seam for a caller-chosen dealer"). Out of scope for a bot-package bugfix.
  evidence: 15 runs with the trump-lead guard enabled -> mean 73.5% win rate (range 69.9-77.8); 15 runs with it disabled -> mean 73.8% (range 68.6-79.2). The delta is inside the noise band, so the suite could neither confirm the intended improvement nor catch the two regressions that review found. It does establish legality: zero `ApplyAction` errors across roughly 6000 simulated hands either way.

- source_spec: `_bmad-output/implementation-artifacts/spec-bot-forced-overplay-secure-take.md`
  summary: The non-trump analogue of the trick-7 retention leak is open -- at exactly two cards `retainLastTrickWinner` will still spend a spare that a yet-to-play opponent can over-ruff, donating the trick while the bot banks the master for the +10.
  reason_deferred: Extending the fix to non-trump leads was explicitly gated "Ask First" in the spec and not approved; the approved scope covers trump leads only, matching the reported bug. Recorded because the shape is identical and the reasoning already exists in the trump-lead guard.
  evidence: The new retention guard is gated on `v.CurrentTrick[0].Card.Suit == trump`. An adversarial review agent enumerated 5152 two-card non-trump-lead endgame positions and confirmed the guard changes none of them; separately, neutralizing that suit check alters 350 non-trump-lead decisions -- e.g. hand JH KH, partner led AS, seat 3 played TS (21-point pot) -> KH with the check, JH without it. The over-ruff shape lives in the same `return spare` path.

- source_spec: `_bmad-output/implementation-artifacts/spec-bot-forced-overplay-secure-take.md`
  summary: `retainLastTrickWinner` prices last-trick control at +10 unconditionally and never reads `v.TricksWon`, so with a **capot** in hand (team has swept tricks 1-6, making the last trick worth +100) it can spend the spare, lose trick 7, and kill the capot.
  reason_deferred: Out of the approved scope, which is `trumpEconomyTake` only; `retainLastTrickWinner` is explicitly gated "Ask First" after two reverted attempts to modify it. This gap is pre-existing and unchanged by the fix. Fixing it properly means teaching retention about capot AND about who leads trick 8, which is a separate heuristic with its own measurement burden.
  evidence: An adversarial review agent found a legal line by DFS and replayed it through `game.ApplyAction`: deal s0 `7C 7S AC AD AH JD JH KH`, s1 `7D 8C 8D 8H 8S AS KC TH`, s2 `7H 9H JC KD KS QH QS TC`, s3 `9C 9D 9S JS QC QD TD TS`, seat 0 leads. At trick 7 `TricksWon == [6,0]`, partner leads 7H, seat 3 discards JS, bot holds AH KH. Retention spends KH, seat 1's TH is forced over it, and the capot dies -> 130 hand points. Playing AH instead holds the sweep and KH wins trick 8 -> 252.

- source_spec: `_bmad-output/implementation-artifacts/spec-bot-forced-overplay-secure-take.md`
  summary: When the bot LEADS trick 7 holding master + spare, `retainLastTrickWinner` always leads the spare, which can be over-trumped and donate a trick worth more than the +10 it is protecting.
  reason_deferred: Same scope gate as the capot entry. Measurement says the current choice is right on balance, so this is a refinement rather than a defect: over 52 053 fully-legal trick-7 lead positions, leading the spare was better 9 815 times against 6 009 for leading the master. Distinct from the already-logged non-trump *follow* gap.
  evidence: Enumeration by an adversarial review agent over fully-legal trick-7 lead positions, deltas evaluated to 60 hand points. The guard at `bot.go` `retainLastTrickWinner` has no lead-specific branch.

- source_spec: `_bmad-output/implementation-artifacts/spec-svg-card-deck.md`
  summary: `CardFlight` derives its start/end scale from `getBoundingClientRect()` of *rotated* wrappers, so a flight's first and last frame can be ~18% larger than the settled card it hands off to.
  reason_deferred: Pre-existing. The hand fan rotates each card (`PER_OFFSET_DEG`) and the E/W trick slots rotate 8deg, so the measured bbox has always exceeded the card's own box; this change only altered the constant the ratio divides by (72 -> 75). Fixing it means dividing by a `CARD_SIZES`-derived width instead of the bbox, which shifts every flight's scale curve and needs its own visual pass.
  evidence: `CardFlight.tsx` computes `fromScale = f.fromRect.width / BASE_W` from rects captured in `MatchPage`; a rotated 90px card reports a bbox wider than 90px, so the ratio overshoots. Identified independently by an edge-case review agent.

- source_spec: `_bmad-output/implementation-artifacts/spec-svg-card-deck.md`
  summary: The 32 deck faces are served from the site root, which `Caddyfile.prod` matches with `Cache-Control: no-cache`, so every page load revalidates all 32 instead of serving them from cache.
  reason_deferred: The fix is a production Caddy config change (an immutable-cache rule scoped to `/cards/*`), which is deploy infrastructure rather than app code and should not ride along silently in a frontend change. Note the URLs are ID-derived and therefore not content-hashed, so a long max-age needs a deliberate cache-busting story first.
  evidence: `Caddyfile.prod` scopes its immutable rule to `path /assets/*`; Vite copies `public/` to the site root, so `/cards/*.svg` falls through to the `@unhashed` no-cache rule. Both review agents flagged it. The spec's own manual check expects 200-from-cache on reload and will instead see 304s.

- source_spec: `_bmad-output/implementation-artifacts/spec-svg-card-deck.md`
  summary: `warmDeck` cannot prevent the blank-card flash on the *opening* deal, because it starts in the same frame as the first cards' own image requests.
  reason_deferred: Fixing it properly means either 32 `<link rel="preload">` tags in `index.html` or warming the deck on room/match entry -- a different change with its own placement question, and the code comment now states the limitation rather than overclaiming. Subsequent tricks, prompts, and hands are already covered.
  evidence: The warm-up runs in the first mounted card's `useEffect`, i.e. after that card's `<img src>` is in flight; `client/index.html` preloads nothing. Raised by an adversarial review agent.

- source_spec: `_bmad-output/implementation-artifacts/spec-svg-card-deck.md`
  summary: `TrumpPrompt`'s card-imitating suit surfaces take their background and border from `cardFace.ts` but keep their own unrelated corner radii (`rounded-[5px]` on the 22x30 chips, `rounded-md` on the 52px picker tiles) and aspect ratios.
  reason_deferred: Cosmetic and currently invisible -- these are suit-glyph tiles, not card faces, and nobody sees them beside a card at matching scale. Binding them fully would mean exporting a radius policy for non-card surfaces, which is more shared API than the problem justifies today.
  evidence: `TrumpPrompt.tsx` tile styles set radius via Tailwind classes while the card computes `width * 0.05`; a future deck with a different `rx` would move the cards and leave the tiles behind. Raised by an adversarial review agent as residual drift.

- source_spec: `_bmad-output/implementation-artifacts/spec-profile-remove-friend-and-decline.md`
  summary: `RemoveFriendDialog` is the third near-verbatim clone of the confirm-dialog shape (`UnlinkAccountDialog`, the remove-bot dialog) -- extract a shared `ConfirmDialog` before the pattern drifts.
  reason_deferred: Pre-existing duplication; extracting a shared abstraction touches surfaces outside this story and deserves its own pass over all call sites.
  evidence: Both profile dialogs line up almost line-for-line (same `showCloseButton={false}`, same `if (!next && !pending) onClose()`, same brass icon chip, same ghost-cancel/destructive-confirm footer). Raised by an adversarial review agent.

- source_spec: `_bmad-output/implementation-artifacts/spec-profile-remove-friend-and-decline.md`
  summary: No axios request timeout exists anywhere, so a hung mutation locks any pending-guarded confirm dialog (remove-friend, unlink-account) with every dismissal path disabled.
  reason_deferred: App-wide client configuration, not caused by this story; fixing only `removeFriend` would be inconsistent -- needs a global `axiosClient` timeout decision plus a sweep of pending-guarded dialogs.
  evidence: Neither axios instance in `client/src/shared/api/axiosClient.ts` sets `timeout`, and `refetchOnWindowFocus` is disabled, so nothing unsticks a hung `isPending`. Verified by an edge-case review agent.
  update_2026-08-20: CLOSED. Re-verified still open (neither instance set `timeout`), then fixed globally: both `axiosClient` and `axiosPublic` now carry `REQUEST_TIMEOUT_MS = 15_000`. Chosen because every endpoint here is a short REST call — no uploads, no long-polling, and live gameplay runs over the WebSocket, which is untouched. The error path additionally maps `ECONNABORTED`/`ETIMEDOUT` to a distinct `TIMEOUT` code (status 0) so callers can say "took too long" rather than "you are offline"; nothing in product code branched on `NETWORK_ERROR`, so the split is non-breaking. Covered by `client/src/shared/api/axiosClient.test.ts` (both instances carry a finite timeout; timeout maps to TIMEOUT; a real network failure still maps to NETWORK_ERROR).

- source_spec: `_bmad-output/implementation-artifacts/spec-profile-remove-friend-and-decline.md`
  summary: `useAcceptFriendRequestMutation.onError` only toasts -- after a 404 (request resolved elsewhere) the stale Accept button survives in cache, the same gap this story fixed for remove/decline.
  reason_deferred: Pre-existing behavior on an untouched mutation; fixed decline/remove because this story added their profile surfaces, but accept's error path predates the change and was deliberately left alone to keep the diff scoped.
  evidence: `useFriendMutations.ts` accept `onError` invalidates nothing, while its siblings now resync `friends.requests()`/`friends.status()`; identical failure mode was confirmed for decline by both review agents.

- source_spec: `_bmad-output/implementation-artifacts/spec-12-1-variant-rule-config-and-croatian-bidding.md`
  summary: `event:match_state` broadcasts every player's full hand and the 11-card undealt deck to all four seats, so any player can read the whole table's cards from devtools or a websocket inspector.
  reason_deferred: Pre-existing, and Story 12.1 was deliberately scoped to Croatian-only masking so Bitola stays a regression surface rather than a refactor target. Closing it properly needs a per-recipient projection seam: `buildMessage` (`live_match.go:1277`) takes no recipient and `BroadcastToUsers` (`ws/hub.go:188`) takes pre-serialized bytes, so ~21 `match_state` broadcast sites plus `SyncStateOnConnect` would change, along with the client's `z.strictObject` `PlayerStateSchema`, `matchTypes.MatchState`, and the opponent card-count rendering at `MatchPage.tsx:1607` — arguably its own shippable goal.
  evidence: `state.go:11` `Hand []Card \`json:"hand"\`` and `state.go:86` `Deck []Card \`json:"deck"\`` carry no `json:"-"` and no per-seat conditionality; `server/internal/ws/testdata/events/event_match_state.json` contains all four `hand` arrays with concrete ranks and suits; `MatchPage.tsx:1607` renders opponents as `player.hand.length`, i.e. the client already holds the real cards and only chooses not to draw them. No test anywhere asserts card privacy. Found while investigating Story 12.1's server-side masking AC.
  update_2026-08-20: PROMOTED to **Story 12.10 — Per-Recipient Snapshot Projection (Card Privacy)**, `backlog`, on owner instruction. Registered in `epic-12-context.md` and `sprint-status.yaml`. Absorbs D96 (the `Deck` field exposed in every `event:game_state` frame) — same defect, same wire, one fix.
  update_2026-08-21: CLOSED by Story 12.10. Masking now happens at the serialization boundary, per recipient: the new pure `game.ProjectForSeat(gs, seat)` (server/internal/game/projection.go) clones the state, empties every other seat's `hand` (a projection-computed `handCount` carries the public length on all four seats), and every `event:match_state` send routes through it — one new per-user-frames primitive (`ws.Hub.SendFrames` + the same method on `match.Broadcaster`) keeps one state event ONE logical call across all 23 sites (all-humans broadcasts, the remaining-seats disconnect subset, and the `SyncStateOnConnect` unicast). `Deck` left the wire entirely (`json:"-"`, closing D96 above). TWO FURTHER HIDDEN-INFORMATION VECTORS beyond hands+deck were found during spec distillation and are fixed inside the same projection: every seat's not-yet-resolved `declarations[].cards` (declaration cards ARE in-hand cards; until resolution nil'd the losers they shipped to all four seats) and `pendingBelotSeat` (broadcast to everyone, announcing that a seat holds the second trump royal before the player chooses to announce — masked to null for every seat but its own). The client renders opponents from `handCount ?? hand.length` plus `faceDownCount ?? 0` (rolling-deploy safe), and the regenerated `event_match_state.json` golden is marshalled through the seat-0 projection, so the golden itself proves privacy: one real hand, three empty ones, four handCounts, no deck key. Covered by `internal/game/projection_test.go` (all four seats x five phase shapes, structural card scan, source-unmutated) and `internal/match/state_frames_wiring_test.go` (each recipient receives a DIFFERENT frame carrying only their own hand; bot seats get no frame). One deliberate deviation from the spec's "no pre-existing test under internal/game/ modified" constraint: `state_test.go`'s `TestGameStateJSONCamelCaseKeys` listed `"deck"` among expected wire keys, which is unsatisfiable alongside the mandated `json:"-"` — the single line was removed with a comment; every other pre-existing game test passes unmodified.

- source_spec: `_bmad-output/implementation-artifacts/spec-12-1-variant-rule-config-and-croatian-bidding.md`
  summary: Under `VariantRules.AllPassOutcome == AllPassDealerMustPick`, a Croatian dealer who times out on their round-2 turn would livelock bidding — `handleTimerExpiry` auto-issues `pass_trump`, the engine rejects it because the dealer must pick, and the error path re-arms the same seat's timer, so the hand never advances.
  reason_deferred: Unreachable today — that config belongs to a variant Story 12.8 has not yet made selectable. Resolving it means deciding what suit to name on an absent player's behalf, which is a rule decision belonging with the same story that teaches bots to bid without a candidate (12.8 per the Epic 12 context). Story 12.1 deliberately shipped the forced-pick rule without inventing that policy.
  evidence: The re-arm uses `time.Until(*oldState.TurnExpiresAt)`, already <= 0 when a timeout produced the error, so `time.AfterFunc` fires immediately and the reject-re-arm cycle is a hot spin that burns a core rather than a quiet stall. `server/internal/match/live_match.go:1498` carries a `TODO(croatian-enablement)` at the exact auto-action site, inside the `gs.Phase == game.PhaseBidding` branch that unconditionally issues `ActionPassTrump`. `TestCroatianDealerCannotPassInRound2` confirms the engine rejects that action under the Croatian config. Surfaced by the implementing agent and verified against the timeout path.
  update_2026-08-20: The "hot spin that burns a core" claim in the evidence above is WRONG, and so is the same claim in the `TODO(croatian-enablement)` comment itself (now at `live_match.go:1522`). `handleTimerExpiry`'s auto-action error path calls `m.startTimerLocked` (`live_match.go:1616` -> `:1429-1435` -> `armTurnTimerLocked`), which arms a FULL fresh `timerDurationSec` window — there is no `time.Until` anywhere in `handleTimerExpiry`. The `time.Until(*oldState.TurnExpiresAt)` re-arm the evidence cites lives in `HandleAction`'s error path (`live_match.go:424`), a different path, and `armTurnTimerLocked` clamps it with `max(remaining,0)+expiryGrace` (400ms). Real severity: a reject/re-arm loop at one timer period, forever, with one `slog.Error` per cycle — the hand never advances, but no core is burned. Verified by reading both paths during Story 12.8 planning. Fix the comment when the fix lands.
  update_2026-08-20: CLOSED by Story 12.8. `handleTimerExpiry` gained a forced-pick arm ahead of the general bidding arm, guarded by the new exported `game.MustPickTrump`, that auto-names a suit via the new pure `game.AutoPickTrumpSuit` instead of passing. Covered by `TestHandleTimerExpiry_ForcedDealerPickResolvesBidding`. See also the 2026-08-20 correction above: the severity was a bounded reject loop, not a hot spin.

- source_spec: `_bmad-output/implementation-artifacts/spec-12-1-variant-rule-config-and-croatian-bidding.md`
  summary: A bot bids Croatian round 2 on six of the eight cards it actually holds — `bot.View` carries no face-down cards, so the round-2 reveal never reaches the bot's decision input.
  reason_deferred: Story 12.1 scoped the bot change to a nil-candidate panic guard only; teaching bots to bid without a candidate is assigned to Story 12.8 by the Epic 12 context, and evaluating the full eight-card hand is the same piece of work.
  evidence: `server/internal/bot/view.go:15` is a bare `Hand []game.Card` and `buildBotView` (`server/internal/match/bot_driver.go:204-207`) populates it from `gs.Players[seat].Hand` alone, which under `DealShapeAllBeforeBidding` holds only the six open cards until bidding resolves. Raised independently by all three review layers.
  update_2026-08-20: CLOSED by Story 12.8. `bot.View` gained `FaceDownCards`, populated by `buildBotView` only when `FaceDownRevealed` and only for that seat, and `decideBid` counts the pair. Kept OUT of `Hand` so the playable set stays the playable set. Covered by `TestBuildBotView_FaceDownGating` and paired bidding rows that prove round 2 counts the pair and round 1 does not. (superseded 2026-08-22: Croatian bidding is single-round; the reveal event, FaceDownRevealed flag and RevealFaceDownOnRound2 field were removed — see spec-fix-croatian-single-round-bidding.md)

- source_spec: `_bmad-output/implementation-artifacts/spec-12-1-variant-rule-config-and-croatian-bidding.md`
  summary: A bot dealer hits the same forced-pass livelock as a timing-out human, and `bot_test.go`'s shared post-assertion encodes a Bitola-only rule as universal, which will block the first candidate-less bot test row.
  reason_deferred: Same root cause and same owner as the timeout livelock above — choosing a suit on the dealer's behalf is a rule decision belonging to Story 12.8. Unreachable until the variant is selectable.
  evidence: `decideBid` returns `pass_trump` when no suit clears its threshold; under `AllPassDealerMustPick` the engine rejects that, and `server/internal/match/bot_driver.go:185-195` deliberately re-arms the seat, re-running `Decide` over unchanged state and reproducing the identical rejected pass. Separately, `server/internal/bot/bot_test.go:266` asserts `action.Suit` is nil for every round-1 pick, which is true only for the candidate variant. A bot dealer is the more likely trigger than a human timeout.
  update_2026-08-20: CLOSED by Story 12.8. `View.MustPickTrump` (derived from the rule config, never a variant name) makes `decideBid` return its best-scoring suit instead of passing; the Bitola-only "round-1 pick carries no suit" assertion is now scoped to the candidate variant. Covered by `TestDecide_ForcedDealerNeverPasses` and `TestBot_ForcedDealerPickAdvancesHandWithoutRejection`, plus deletion of the Croatian simulation's forced-pick substitution.

- source_spec: `_bmad-output/implementation-artifacts/spec-12-1-variant-rule-config-and-croatian-bidding.md`
  summary: The forced dealer is shown a Pass button the server will reject, and the refusal reuses `ErrInvalidBid`, so the client cannot tell "you must pick" from "bad suit" even if it wanted to.
  reason_deferred: `TrumpPrompt` has no `canPass` prop and Story 12.1's task list covered only the free four-suit grid; the prompt is unreachable until 12.8 exposes the variant. A distinct sentinel is public error-surface API that was not in scope.
  evidence: `server/internal/game/bidding.go` refuses the dealer's fourth round-2 pass under `AllPassDealerMustPick` (pinned by `TestCroatianDealerCannotPassInRound2`), while `TrumpPrompt.tsx` renders Pass unconditionally and the `waitingRound2` copy other seats read still says "to pick a suit or pass". Raised by two review layers.
  update_2026-08-20: PARTLY CLOSED by Story 12.8. `TrumpPrompt` gained `canPass`, fed from a new `GameState.MustPickTrump` wire flag, and hides Pass plus swaps the sibling-seat waiting copy. Still open by explicit decision: the refusal still reuses `ErrInvalidBid`, so the client cannot distinguish "you must pick" from "bad suit" — a new public error sentinel was on the spec's Ask First list and was not taken.
  update_2026-08-20b: APPROVED by owner 2026-08-20 — introduce a separate sentinel so the client can distinguish "you must name a suit" from "bad suit". Scheduled into the Epic 12 deferred-cleanup work.
  update_2026-08-20c: CLOSED. New `apperr.ErrMustPickTrump` (`MUST_PICK_TRUMP`, 400) returned by `handlePassTrump` in place of `ErrInvalidBid`; new `ws.ErrorMustPickTrump` (`error:must_pick_trump`) dispatched by `sendGameError`; client constant `ERROR_MUST_PICK_TRUMP` added to `GAME_ERROR_TYPES` and mapped to a new `match.errors.mustPickTrump` toast in all four locales. The two existing engine assertions that pinned the forced pass as `ErrInvalidBid` now assert the new sentinel, one of them with an explicit `NotErrorIs(ErrInvalidBid)` so the two stay distinguishable; the genuine bad-suit assertions were left on `ErrInvalidBid`. The UI still hides Pass in this state, so the toast is the defence-in-depth path for a stale client.

- source_spec: `_bmad-output/implementation-artifacts/spec-12-1-variant-rule-config-and-croatian-bidding.md`
  summary: Lobby and room-page variant labels have no Croatian branch, so a Croatian room would read an unlocalized, title-cased "Croatia" in all four locales.
  reason_deferred: Only reachable once 12.8 allows Croatian rooms to exist. Story 12.1 added `match.variants.croatia` for the in-match path only; the lobby surfaces use a different key namespace with a deliberate title-case fallback.
  evidence: `client/src/features/lobby/lib/roomLabels.ts:10` special-cases `"bitola"` alone and falls through to title-casing, and `client/src/features/room/RoomPage.tsx:75`'s `variantKeys` map has a `bitola` key only. `lobby.createRoomModal.variantCroatia` already exists while `lobby.card.variantCroatia` does not. Raised by two review layers.
  update_2026-08-20: CLOSED by Story 12.8. `lobby.card.variantCroatia` added in all four locales; `roomLabels.variantLabel` and `RoomPage`'s `variantKeys` both map `croatia`. Covered by four-locale `it.each` cases in `RoomCard.test.tsx` and `RoomPage.locale.test.tsx` that also assert the title-cased fallback is gone.

- source_spec: `_bmad-output/implementation-artifacts/spec-12-1-variant-rule-config-and-croatian-bidding.md`
  summary: During Croatian bidding, each opponent's card-back stack reads 6 while that opponent actually holds 8, because the two face-down cards are deliberately absent from other seats' snapshots.
  reason_deferred: A direct and accepted consequence of keeping the face-down cards out of every snapshot (the design the spec chose); it is invisible until 12.8 makes the variant selectable, and whether to render two extra backs is a presentation decision for the story that surfaces the variant.
  evidence: `client/src/features/match/MatchPage.tsx` passes `player.hand.length` as `cardCount` in every phase, and the Croatian deal leaves only six cards in `Players[i].Hand` until bidding resolves. The spec's Design Notes accept it; recorded here so 12.8 actually reads it.
  update_2026-08-20: CLOSED for opponents by Story 12.8 via a new per-seat `PlayerState.FaceDownCount` on the wire (a count leaks nothing; the card identities stay out of every snapshot). NOT closed for the viewer's OWN seat — see the 2026-08-20 entry near the end of this file.

- source_spec: `_bmad-output/implementation-artifacts/spec-12-1-variant-rule-config-and-croatian-bidding.md`
  summary: `DealAnimation` still holds its trump-phase window open over an empty table centre when the variant flips no candidate, and nothing visually represents the two face-down cards being dealt.
  reason_deferred: Purely presentational, unreachable until 12.8, and the right treatment (a face-down pair animating to each seat) is a design question rather than a defect in the dealing rules Story 12.1 delivered.
  evidence: `DealAnimation.tsx` self-hides on `dealPhase === "done" && !trumpCandidate` but still spends the `MOTION.DEAL_PHASE_TRUMP` duration with nothing to show. Raised by one review layer as residual polish.
  update_2026-08-20: PARTLY CLOSED by Story 12.8 — the dead trump-flip beat is skipped when there is no candidate. Still open by explicit decision: nothing visually represents the two face-down cards being dealt; the spec forbade designing new choreography.
  update_2026-08-20b: CLOSED — WONTFIX by owner decision. The count going 6 -> 8 at your own seat and x6 -> x8 at every other seat is sufficient signal that two more cards arrived. Dealing sound, and possibly an animation, are a later pass; do not re-file this.

- source_spec: `_bmad-output/implementation-artifacts/spec-12-5-croatian-declaration-overlap.md`
  summary: Nothing mechanically detects drift between the Go declaration detector plus its variant presets and the hand-written TypeScript mirror of both.
  reason_deferred: Pre-existing duplication established by `spec-declaration-prompt-sum-and-bitola-dedup.md`; Story 12.5 only added one more mirrored fact to it. Closing it properly means a new cross-language golden surface, which is its own story.
  evidence: `client/src/features/match/lib/declarations.ts` and `client/src/features/match/lib/variantRules.ts` both declare themselves mirrors of `server/internal/game/{declarations,types}.go` in prose, and the only enforcement is two independently hand-written test suites. Flipping a preset in `RulesFor`, or changing the Go dedup, fails nothing on the client. The repo already has the mechanism: a Go test could emit a `{variant -> rule facts}` and `{hand -> melds per config}` golden under `internal/ws/testdata/`-style `UPDATE_GOLDENS=1`, imported the way `wsEvents.contract.test.ts` already imports `declarations_resolved.json`, without ever serializing the rule config to a client.
  update_2026-08-20: CLOSED, built exactly as sketched. `server/internal/game/rules_contract_test.go` (package `game`, so it reaches the UNEXPORTED `detectDeclarations` without widening the API) emits two goldens under `server/internal/game/testdata/contract/`: `variant_rules.json` (`{variant -> all seven rule facts}` from `RulesFor`) and `declaration_melds.json` (five hands x both overlap settings, run through the real detector). `client/src/features/match/lib/engineMirrors.contract.test.ts` imports them read-only and asserts the TS mirrors reproduce them. `VariantRules` stays `json:"-"` — nothing is serialized to a client at runtime; this is a build-time contract only. The rules golden also pins the FIELD SET, so a new Go rule field surfaces as a failure rather than being silently unmirrored. Verified adversarially: flipping `croatia.declarationOverlap` to `false` on the client fails the gate.

- source_spec: `_bmad-output/implementation-artifacts/spec-12-5-croatian-declaration-overlap.md`
  summary: The reveal's tiebreaker line now renders when a single seat holds two overlapping melds, even though no clash was compared and the opposing team declared nothing.
  reason_deferred: The gate is `payload.declarations.length > 1`, and leaving it untouched was an explicit human decision during Story 12.5 planning (the line still names the meld that won, and the same "implies a contest" nuance already existed in Bitola whenever two teammates declared). Recorded so the next owner of this copy decides deliberately rather than rediscovering it.
  evidence: `client/src/features/match/components/DeclarationReveal.tsx` computes `showTiebreaker` from the meld count alone, and Croatian overlap makes two melds from one seat the common case rather than a rarity. The server never sends the losing team's melds, so the client cannot distinguish "won a clash" from "only team to declare" without a payload change.
  update_2026-08-20: DECIDED by owner 2026-08-20 — show the line only on a real clash. Take the payload change: add a `contested` flag to `event:declarations_resolved`, set when the opposing team also declared, and gate `showTiebreaker` on it instead of `declarations.length > 1`. Rides along with the typed-payload rework and its golden regeneration rather than paying a second wire change.
  update_2026-08-20b: CLOSED, done exactly that. `contested` is computed by the new `bothTeamsDeclared(oldState)` helper — it must read the PRE-resolution state because `resolveDeclarationsForHand` has already deleted the losing team's melds by broadcast time; `oldState` is a deep clone (`cloneGameState` clones each player's `Declarations` and their `Cards`), so the loser's melds are intact there. No new engine field was needed. `DeclarationReveal.showTiebreaker` now reads `payload.contested`, and its tests were rewritten around the real distinction: two melds from one seat with `contested:false` (the ordinary Croatian overlap hand) hides the line while still rendering both melds; one meld with `contested:true` shows it.

- source_spec: `_bmad-output/implementation-artifacts/spec-12-5-croatian-declaration-overlap.md`
  summary: The in-app rules reference states neither the one-card-one-group rule nor the Croatian overlap exception, so it now under-documents both variants' declaration rules.
  reason_deferred: `epic-12-context.md` assigns reconciliation of the already-shipped rules-reference content against real engine behaviour to Story 12.8, and Story 12.5's Never list forbids doing 12.8's work. Filed so 12.8 is actually handed the two new facts instead of inferring them.
  evidence: `client/src/features/rules/content/en.ts` documents the clash chain including "a four-of-a-kind beats a run at equal value" but never says a card may belong to only one declaration, which is the rule Bitola enforces. Story 12.5 also changed the Bitola equal-value tie to keep the four-of-a-kind, and added the Croatian exception where one card may count in several melds. All four locales' rules content needs both facts.
  update_2026-08-20: Owner assigned to **Story 12.9**, not 12.8 — carry both facts there.

- source_spec: `_bmad-output/implementation-artifacts/spec-12-5-croatian-declaration-overlap.md`
  summary: No test exercises the match-layer broadcast loop that flattens several melds per seat into the `event:declarations_resolved` payload.
  reason_deferred: The producing loop is pre-existing code that Story 12.5 did not touch, and the story's engine-level tests prove the multi-meld state it would serialize. Adding match-package coverage means driving a Croatian trick 1 through the manager with a captured hub, which is a test-infrastructure task beyond this story's task list.
  evidence: The contract case in `server/internal/ws/events_contract_test.go` marshals a hand-written map literal and byte-compares it with the golden; it never constructs a `GameState` nor calls `broadcastDeclarationsResolvedIfTransition`. Collapsing that broadcast loop to the first meld per seat would keep the Go contract test, the client contract test and every match-package test green, while a Croatian hand scored 250 and the reveal rendered a single 200 row.
  update_2026-08-20: Partially closed by Story 12.6. `server/internal/match/declaration_phase_test.go` now drives resolution through the manager with a captured hub and asserts `event:declarations_resolved` fires, names the team the engine chose, and precedes the trick-1 `match_state` — so the test-infrastructure blocker this entry cited is gone, and deleting either broadcast call site now fails a test. Still open: nothing asserts the payload's `declarations` array, so the several-melds-per-seat flattening this entry was filed about remains unverified. A Croatian overlap hand driven to resolution and checked meld-by-meld would close it.
  update_2026-08-20b: CLOSED, done exactly that. `TestDeclarationPhase_RevealCarriesEveryMeldPerSeat` drives a Croatian hand to resolution and compares the payload's `declarations` array meld-by-meld against the resolved engine state (expectations DERIVED from that state, so a meld-value change moves both sides together rather than leaving a stale constant). The existing Croatian fixture already yields a seat holding two melds, and the test asserts that precondition explicitly so it cannot silently stop exercising the flattening. Verified adversarially: collapsing the producer loop to the first meld per seat now fails with "should have 3 item(s), but has 2".
  found_while_testing_2026-08-20: the same test pinned WHERE `contested` is computed. Every seat declares in that hand, so both teams put melds on the table, and resolution then deletes the loser's — the test asserts the loser's melds are gone AND that `contested` is still true, which an implementation reading the post-resolution state could not produce. `TestDeclarationPhase_RevealReportsAnUncontestedWin` covers the other arm (team A skips, one team declares, several melds, `contested:false`) — the case that proves meld count could never have stood in for the flag.

- source_spec: `_bmad-output/implementation-artifacts/spec-12-6-croatian-declaration-phase.md`
  summary: The `phase` string is never pinned across the Go/TypeScript boundary, so a server phase constant and its client union member can drift with no test failing anywhere.
  reason_deferred: This is structural and pre-existing — it affects all eleven phase values equally, not just `declaring`. Closing it means either adding a golden that carries each phase or generating the client union from the Go constants, both of which are contract-infrastructure work well outside a single story's task list.
  evidence: `client/src/shared/types/wsEvents.schemas.ts` types `phase` as a plain `z.string()` rather than an enum, and `server/internal/ws/testdata/events/event_match_state.json` is deliberately unchanged by Story 12.6 and carries only `"phase": "playing"`. `game.PhaseDeclaring = "declaring"` and the `"declaring"` member of the client `Phase` union are therefore matched by nothing but hand-copying. Story 12.6's entire wire contract is "the phase string is the only field carrying the new state", so that one string is load-bearing and unverified.
  update_2026-08-20: CLOSED for all eleven phases, not just `declaring`. Chain, with no unguarded link: (1) `game.AllPhases()` is the canonical ordered list, and `TestAllPhasesCoversEveryConstant` scans `types.go` for `Phase = "..."` declarations and fails if one is declared but unlisted (or listed but undeclared); (2) the ws contract test writes it to `testdata/events/phases.json`; (3) the client's `SERVER_PHASES` const in `matchTypes.ts` is now the single source for BOTH the `Phase` union (`"" | (typeof SERVER_PHASES)[number]`) and the Zod enum; (4) `wsEvents.contract.test.ts` compares `SERVER_PHASES` to the golden as an ordered list. `wsEvents.schemas.ts` `phase` went from `z.string()` to `z.enum(SERVER_PHASES)`, so an unknown phase is now rejected instead of rendering a blank table. `""` stays client-local and out of the wire vocabulary. Verified adversarially both ways: adding a constant without listing it fails the Go guard; appending to the golden fails the TS pin.

- source_spec: `_bmad-output/implementation-artifacts/spec-12-6-croatian-declaration-phase.md`
  summary: The `event:declarations_resolved` payload is built as an untyped `map[string]interface{}` in the match layer, so it is bound to neither the typed `ws.DeclarationsResolvedPayload` nor the golden that pins it.
  reason_deferred: Pre-existing — the map predates Epic 12 and Story 12.6 was forbidden from widening or restructuring any payload. Fixing it means changing the producer to emit the typed struct, which shifts key order and forces a golden regeneration, i.e. a deliberate wire-shape change that needs its own story.
  evidence: `server/internal/match/live_match.go` hand-builds the reveal payload as a map, while `server/internal/ws/events_contract_test.go` marshals the typed `ws.DeclarationsResolvedPayload` literal and byte-compares it with `testdata/events/declarations_resolved.json`. Nothing connects the two, so the keys the manager actually puts on the wire are neither type-checked nor golden-checked. Story 12.6 puts a second variant's reveal onto that same untyped map.
  update_2026-08-20: CLOSED. There was in fact no `ws.DeclarationsResolvedPayload` type at all — the contract test marshalled its own hand-written map literal, so the "typed struct" the entry assumed existed did not. Now added (`ws.DeclarationsResolvedPayload` + `ws.DeclarationPayload`), `broadcastDeclarationsResolvedIfTransition` builds and emits that struct instead of `map[string]interface{}`, and the contract test marshals the same type. Key order shifted from map-alphabetical to struct order, so the golden was regenerated — the deliberate wire-shape change this entry predicted. `winnerTeam` is now `*int` rather than `interface{}`.

- source_spec: `_bmad-output/implementation-artifacts/spec-12-6-croatian-declaration-phase.md`
  summary: During the Croatian declaration phase the three seats that are not on the clock see no explanation of what the table is waiting for.
  reason_deferred: Story 12.6's Boundaries forbid adding i18n keys, and any affordance here is new copy in four locales. Recorded so the omission is a decision rather than an oversight.
  evidence: Unprompted seats render a normal table — eight cards, an empty trick, `trickNumber: 0` — with only another seat's timer ring moving. With meld-holding seats each able to consume a full turn, the table can appear frozen for up to four turn durations (two minutes at a 30s timer) before the first card is played. Bitola never shows this, because its declarations happen inside a trick that is visibly progressing.
  update_2026-08-20: CLOSED — new `DeclarationWaiting` banner, shown to the seats not on the clock whenever `phase === "declaring"`. Keyed off the server's phase, never off the variant. Deliberately mirrors `TrumpPrompt`'s waiting arm exactly (same panel, placement, team-coloured name, `pointer-events: none`) so the two "someone else is deciding" states read as one pattern. Copy added in all four locales as `match.declaration.waiting`; the 12.6 Boundaries that forbade new i18n keys no longer apply. Covered by `DeclarationWaiting.test.tsx`, including a per-locale pass that asserts mk stays all-Cyrillic.

- source_spec: `_bmad-output/implementation-artifacts/spec-12-6-croatian-declaration-phase.md`
  summary: At both boundaries of the Croatian declaration phase an overlay covers a seat whose per-move timer is already running, and the phase makes this systematic rather than incidental.
  reason_deferred: Both behaviours are pre-existing Bitola patterns that Story 12.6 inherited rather than introduced, and the reveal's timing and centring are frozen by `spec-12-5-croatian-declaration-overlap.md` and its upstream specs. Changing either needs a deliberate decision about whose clock pays for a reveal.
  evidence: `showDeclarationPrompt` requires `trumpReveal === null`, so the first prompted seat's clock runs while the trump-take reveal still hides their prompt; and at the handoff into trick 1 the leader's fresh timer starts underneath the 8s declaration reveal. Under this phase the first prompted seat and the trick-1 leader are both always `(DealerSeat+1)%4`, so the same seat pays both costs every hand.
  update_2026-08-20: CLOSED — ACCEPTED by owner 2026-08-20, no change. Both reveals already render an `AutoCloseRing` (X + countdown) at `duration={8}`, so a player who dismisses early gets the time back. Owner accepts the worst case, which is a 10s room (the floor enforced at `server/internal/room/handler.go:507`) leaving ~2s if the reveal is left to auto-close. Do not re-file; revisit only if the timer floor drops below 10s.

- source_spec: none
  summary: Bitola hanging-points tie rule (Story 12.7) — deliberately NOT built; Bitola keeps borrowing the Croatian tie rule (a tied hand is lost, all points to the taker's opponents).
  evidence: |
    Decided by the project owner on 2026-08-20, mid-planning, and it supersedes the epic
    sequence "12.1 -> 12.5 -> 12.6 -> 12.7 -> 12.8". Hanging points was only ever a *Bitola*
    change; the Croatian tie rule is exactly what already ships, so the Croatian variant needs
    nothing from this story and 12.8 is unblocked without it. `VariantRules.TieRule` keeps
    resolving bitola -> `TieRuleHangingPoints` as a statement of the authentic rule, but stays
    UNREAD by the engine — do not treat the Bitola tie behaviour as a bug, and do not "fix" the
    rules-reference tie sentence ("Fall short — or even tie — and the hand is lost"), which is
    correct for both variants under this decision.

    Findings from the planning pass, preserved so they are not re-derived if this ever revives:
    - Single decision site is `scoreHand` in `server/internal/game/scoring.go:84-116`; keep the
      Capot arm first (with enough declarations a Capot could in principle tie).
    - Two `HandScore` fields suffice — pool outstanding after the hand, and pool folded into this
      hand's award; `>0` on the first is unambiguously "this hand hung". A third boolean would
      only distinguish a Croatian tie, which `FailedContract` already covers.
    - The accumulator is per-match, so `startNewHand` (`scoring.go:161-225`) must NOT reset it.
    - Trap: `client/src/shared/types/wsEvents.schemas.ts:126-142` is a `z.strictObject` for
      `lastHandResult` inside `MatchStateSchema`, and `gameStateSample()` in
      `server/internal/ws/events_contract_test.go:37-80` leaves `lastHandResult` nil — so a new
      `HandScore` key does NOT move `event_match_state.json` and no golden catches the drift,
      while every live `match_state` parse would fail.
    - Match history is REST-fed from persisted `hand_results` rows, so surfacing it there needs
      migration 000020 (000019 is the highest existing) plus `match.HandResult`,
      `user.MatchHandView` and its mapping loop at `internal/user/handler.go:881-903`.
    - `internal/game/testfixtures/fixtures.go` has no Croatian last-trick factory; the three
      existing Bitola tie tests (`scoring_test.go:167,176,196`) are the only tie coverage.
    - Locked terminology, had it shipped: en "Held-over points" / mk „Виснати поени" /
      hr „Viseći bodovi" / sr „Viseći poeni"; tie subtitle en "Tied · the hand hangs" /
      mk „Изедначено · раката виси" / hr+sr „Izjednačeno · ruka visi".

- source_spec: `_bmad-output/implementation-artifacts/spec-12-8-croatian-variant-enablement.md`
  summary: Reconcile the in-app rules reference against real engine behaviour for BOTH variants, in all four locales — Story 12.8 acceptance criterion (d), split into its own spec.
  evidence: |
    Split from Story 12.8 at intent capture on 2026-08-20 by owner decision, so the enablement
    build stays focused on "can you actually play it". The reference has NO variant split today:
    `client/src/features/rules/content/{en,mk,hr,sr}.ts` (~300 lines each) describe the Bitola
    flow only — face-up trump candidate (`en.ts:106`), melds "announced on your turn in the first
    trick" (`en.ts:170`) — both of which are wrong for Croatian after 12.1 and 12.6.
    `content/rulesContent.parity.test.ts` gates identical section ids, order AND block shapes
    across all four locales, so every new or changed block must land in all four.
    NOT part of this deferral: the tie sentence at `en/mk:209-211`, `hr/sr:208-210` ("Fall short —
    or even tie — and the hand is lost"), which is correct for both variants now that Story 12.7
    is deferred and Bitola keeps borrowing the Croatian tie rule.
  update_2026-08-20: PROMOTED to **Story 12.9 — Variant Rules-Reference Reconciliation**, `backlog`, ships last inside Epic 12. Registered in `epic-12-context.md` and `sprint-status.yaml` on owner instruction so it is tracked rather than remembered. D137 (the both-teams-cross-tied -> taker-wins match tiebreaker, implemented but undocumented) folds into the same story.

- source_spec: `_bmad-output/implementation-artifacts/spec-12-8-croatian-variant-enablement.md`
  summary: The viewer's OWN seat still shows six cards during Croatian round-1 bidding while every opponent now visibly shows eight — Story 12.8 fixed the opponent count and made the asymmetry conspicuous.
  reason_deferred: Whether to draw two card backs at your own seat (or a count) is a presentation decision, and it is genuinely ambiguous: pre-reveal the player does not KNOW those two cards, but does know they hold them. Story 12.8's scope was the opponent count, which is what the spec's matrix row named.
  evidence: `client/src/features/match/lib/faceDownCards.ts` merges the revealed pair into the local fan only after the round-2 reveal, so before it `myHand` is six cards. Post-12.8 every other seat renders `hand.length + faceDownCount` = 8. Raised independently by two review layers.
  update_2026-08-20: CLOSED — the viewer's own seat now draws the pair as two card BACKS, so it shows the eight cards it physically holds, matching every opponent's stack. Chosen over showing six because the Croatian deal puts all eight in the hand before bidding: eight is what is physically true, while six is only what the viewer KNOWS. `HandCards` gained a `faceDownCount` prop; the backs occupy real fan slots (spread, container width and rotation all count them) and are `pointer-events: none` with no card id, so they can never be a legal move. Gated on `myHand.length === myPlayer.hand.length`, which is the exact test for "the reveal has not happened yet" — after `event:face_down_revealed` the cards are merged face-up and after bidding the snapshot carries them, so they are never counted twice. Only a COUNT reaches the client pre-reveal; identities still never leave the server. Covered by four cases in `HandCards.test.tsx`. (superseded 2026-08-22: Croatian bidding is single-round; the reveal event, FaceDownRevealed flag and RevealFaceDownOnRound2 field were removed — see spec-fix-croatian-single-round-bidding.md)

- source_spec: `_bmad-output/implementation-artifacts/spec-12-8-croatian-variant-enablement.md`
  summary: Nothing in the shipped UI signals that the in-app rules reference describes Bitola only, so a player mid-Croatian-match can open instructions that contradict the table in front of them and read them as truth.
  reason_deferred: The full reconciliation is an owner-approved split with its own spec (see the rules-reference entry above). This entry is narrower: even before that lands, `RulesDialog` takes no variant prop and offers no caveat, which is a small in-product signal rather than a content rewrite.
  evidence: `client/src/features/rules/content/en.ts` describes the 3+2 deal, the face-up candidate flip, declarations "announced on your turn in the first trick", and "dealt 3 and 2, then trump" — all wrong for Croatian after 12.1/12.6. `client/src/features/match/components/RulesDialog.tsx` renders `getRulesContent(language)` with no variant awareness.
  update_2026-08-20: Owner assigned to **Story 12.9** — the caveat and the content reconciliation ship together rather than as an interim signal followed by a rewrite.

- source_spec: `_bmad-output/implementation-artifacts/spec-12-8-croatian-variant-enablement.md`
  summary: Four independent variant-to-label mappings and three i18n key families holding identical values now exist, and Story 12.8 had to patch each one separately.
  reason_deferred: Pre-existing duplication that 12.8 extended rather than introduced; consolidating it touches the lobby, room, create-room and match features at once, which is its own refactor. Recorded because the next variant will have to patch all four again.
  evidence: `roomLabels.ts` `variantLabel`'s if-chain, `RoomPage.tsx` `variantKeys`, `CreateRoomModal.tsx`'s PreviewCard ternary, and `MatchPage.tsx`'s `t(\`match.variants.${variant}\`)` — backed by `lobby.card.*`, `lobby.createRoomModal.*` and `match.variants.*` carrying the same strings. `client/src/features/match/lib/variantRules.ts` already declares itself the sanctioned single home for variant-keyed client facts. Also: `profile.matchHistory.variant.*` exists in all four locales, has no `croatia`, and is read by nothing — the new history chip imports the lobby helper across a feature boundary instead. And `MatchListItem.variant` is typed `string` rather than the exported `Variant` union.
  update_2026-08-20: CLOSED, together with D138/D139/D140. `client/src/features/lobby/lib/roomLabels.ts` moved to `client/src/shared/lib/roomLabels.ts` and is now THE resolver — the feature-boundary crossing is gone, since match history and the match HUD are legitimate shared-lib consumers. All four mappings now read through it: `RoomPage`'s `variantKeys`/`matchModeKeys` maps deleted, `CreateRoomModal`'s option lists and PreviewCard ternary replaced, and `MatchPage` no longer builds a `match.variants.*` key. i18n families collapsed from three to one for variants (the strings were byte-identical): `match.variants.*` and `lobby.createRoomModal.variant{Bitola,Croatia}` removed from all four locales. Modes deliberately KEEP two forms — compact "1001 pts" for chips vs spelled-out "1001 points" in the create-room form — both reached via `modeLabel` / `modeOptionLabel`, with a test asserting they stay distinct. Dead `profile.matchHistory.variant` and `profile.matchHistory.mode` removed from all four locales (confirmed read by nothing, which also closes D140 — completing a map no consumer reads was the wrong fix). D139: new `MatchMode = "1001" | "501"` union in `matchTypes.ts`, applied to `Room`, `CreateRoomRequest`, `MatchState.matchMode` and `MatchListItem.variant`/`matchMode`. D138: the `${m} pts` English fallback is now `lobby.card.matchModeGeneric` in all four locales, with a test that renders it across en/mk/hr/sr and asserts the output actually differs by locale. Covered by the new `client/src/shared/lib/roomLabels.test.ts`.

- source_spec: `_bmad-output/implementation-artifacts/spec-12-8-croatian-variant-enablement.md`
  summary: The bot's `wantsTrump` thresholds are still calibrated for a six-card bid hand while Croatian round 2 now feeds them the eight cards the bot actually holds, so bots will be somewhat keener to take trump there.
  reason_deferred: On Story 12.8's Ask First list and deliberately not taken — correcting the INPUT was the story's job, re-deriving the numeric bar is a balance change that deserves its own decision and its own before/after measurement.
  evidence: `server/internal/bot/bot.go` `wantsTrump`'s thresholds were sized against five dealt cards plus a candidate. Croatian round 1 is coincidentally also six, so only round 2 changes — strictly more information against an unchanged bar.
  update_2026-08-20: CLOSED — WONTFIX by owner decision. The intended behaviour IS the current behaviour: a bot bids round 1 on the six cards it can see and round 2 on all eight, against the SAME bar for taking trump. No re-derivation of the numeric thresholds, and no per-variant bar. Do not re-file as a balance bug.

- source_spec: `_bmad-output/implementation-artifacts/spec-12-8-croatian-variant-enablement.md`
  summary: The create-room variant hint now restates the two option labels instead of explaining what distinguishes the variants, so nothing in the product tells an owner what they are choosing at the moment of choice.
  reason_deferred: Writing a per-variant summary is product copy in four locales, and it overlaps the rules-reference reconciliation that was split out of this story. The old hint ("Only Bitola for now") had to go regardless, since it became false.
  evidence: `lobby.createRoomModal.variantHint` reads "Bitola or Croatian rules" in en, duplicating the `variantBitola` / `variantCroatia` labels rendered directly above it. With no per-variant rules content either, the difference (all eight cards dealt and free-suit taking, versus 5+3 with a face-up candidate) is stated nowhere in the UI.
  update_2026-08-20: Owner assigned to **Story 12.9**, which owns the per-variant rules copy this hint should point at.

- source_spec: `_bmad-output/implementation-artifacts/spec-12-4-card-deck-style-preference.md`
  summary: `LEGACY_INK_CLASS` in `TrumpIndicator.tsx` is a dead per-suit Tailwind class map kept alive only so four pre-existing test assertions keep matching.
  reason_deferred: Deleting the map and repointing those four assertions at the accent is cleaner than the comment defending it, but it means editing pre-existing tests, which is risk this story did not need to take.
  evidence: The component's own comment concedes the map has never carried colour (the inline `color` always wins). It also has no Croatian equivalent, so the `.text-red-500` / `.text-text-primary` assertions only hold on the French deck, by accident rather than design.

- source_spec: `_bmad-output/implementation-artifacts/spec-12-4-card-deck-style-preference.md`
  summary: Both deck importers hardcode owner-machine source paths and there is no checked-in manifest or checksum for the source art, so a re-import cannot be verified.
  reason_deferred: The art is owner-authored and lives outside the repo; committing a manifest is a separate decision about where masters live.
  evidence: `scripts/import-croatian-deck.py` defaults to `D:\Downloads\Croatian Card Deck` and `scripts/import-croatian-suits.py` to the same path plus ` (1)`, differing only by a suffix. Both accept an override argument, so they are reproducible only for someone holding identical sources. Nothing validates that the `AQ`->`AD` / `TQ`->`TD` remap still points at bells; that correctness claim lives only in prose.

- source_spec: `_bmad-output/implementation-artifacts/spec-12-4-card-deck-style-preference.md`
  summary: `cardDeckPreference` rides the self-profile DTO on both sides but the client never reads it, so store-vs-row drift never reconciles.
  reason_deferred: Language has `reconcileLanguagePreference` for exactly this; giving the deck an equivalent is a small but independent behaviour, and no known path currently produces the drift.
  evidence: Every client read goes through `useAuthStore((s) => s.user?.cardDeckPreference)`, including `CardDeckPanel` — which sits on the very page whose profile query already carries the server's fresh value. A deck changed in another tab, or a PATCH that succeeded server-side with a lost response, leaves the panel showing a stale selection with nothing to settle it.

- source_spec: `_bmad-output/implementation-artifacts/spec-12-4-card-deck-style-preference.md`
  summary: The in-match language picker fires concurrent PATCHes with no latest-wins guard, the same race being fixed for the deck picker in this story.
  reason_deferred: Pre-existing on every language surface (`LanguageSelector`, `SettingsDialog`, `reconcileLanguage`); fixing it here would have widened an already-wide story, and fixing only one of the two is what leaves the inconsistency.
  evidence: `handleLanguageChange` captures `previousPreference` before its own request and reverts to it on failure. Two fast switches whose responses land out of order leave the auth store and the row disagreeing, with no error surfaced. Story 12.4 added a sequence guard to the deck paths only, so the two pickers in the same dialog now behave differently under the same abuse.

- source_spec: `_bmad-output/implementation-artifacts/spec-12-4-card-deck-style-preference.md`
  summary: `TestDeclarationPhase_TimerExpiryOnLastSeatOpensTrick1` in `server/internal/match` is flaky and fails intermittently on a clean tree, so `make test` is not reliably green independent of any story.
  reason_deferred: Pre-existing and unrelated to the card deck; it belongs to Story 12.6's declaration phase. Surfaced incidentally because Story 12.4 ran the Go suite repeatedly.
  evidence: Proven pre-existing, not inferred: checked out baseline 89e7964 into a clean detached worktree with NONE of Story 12.4's changes and ran the test with `-count=5` -- it failed there. The assertion is `declaration_phase_test.go:134` via `:256`, failing with "-1 is not greater than or equal to 0" and the message "the resolving answer must emit event:declarations_resolved", i.e. a broadcast-index lookup returns -1 because the expected event has not been emitted yet when the assertion runs. Timing-sensitive on the timer-expiry path. Full suite runs do often pass, which is what makes it a flake rather than a hard failure: `go test ./...` was fully green on 2026-08-21 across two separate runs of the same code.
  update_2026-08-21: CLOSED - fixed at root cause on owner instruction ("all should be green even if not from this task"). It was a TEST bug, not a production one: `handleTimerExpiry` assigns `session.gameState` and unlocks BEFORE it broadcasts (`live_match.go`), so the test's `require.Eventually` on `GetStateSnapshot().Phase == PhasePlaying` could win the race against the broadcasts and read a hub that was still empty. Measured 19 failures in 30 isolated runs before the fix, 0 in 30 after. `assertRevealPrecedesPlayingState` now takes the spy plus a watermark and waits for the two events it asserts on, which also closes the same latent race in the two sibling tests that shared the shape. Verified the assertion still bites: stubbing out `broadcastDeclarationsResolvedIfTransition` fails all three loudly, so the fix did not weaken the test into one that cannot fail. Production ordering is untouched.

- source_spec: `_bmad-output/implementation-artifacts/spec-12-9-variant-rules-reference-reconciliation.md`
  summary: The any-suit "all eight cards of one suit wins the match" rule is still unimplemented in the engine, and the rules page no longer describes it — it now describes the trump-suit rule `checkInstantWin` actually enforces.
  reason_deferred: Story 12.9 is a client content story with the engine explicitly out of scope, and the owner's frozen decision was to correct the copy rather than build the rule. The open product questions from the 2026-06-14 entry (precedence against other match-end paths, the reveal event and UI, interaction with the 501 target) are unchanged and still need answering before any engine work.
  evidence: `server/internal/game/scoring.go:228-256` `checkInstantWin` counts cards of the resolved trump suit only, and is called from `bidding.go:190` and `scoring.go:219`. `DECLARATIONS_BASE`'s `bela` entry (1001 pts, tier 3) now documents exactly that. Nothing anywhere detects eight cards of a NON-trump suit; a player dealt all eight Hearts on a Spades hand simply plays the hand out.

- source_spec: `_bmad-output/implementation-artifacts/spec-12-9-variant-rules-reference-reconciliation.md`
  summary: The all-eight-trumps hand sits in `DECLARATIONS_BASE` alongside the seven real melds, so the rules page renders a match-ending hand as an eighth chip in the declarations grid.
  reason_deferred: Moving it out is a wire-shape change to the content module — it changes `DECLARATIONS_BASE`, the assembled `declarations` array every locale is measured against, and the parity test's declaration-ids assertion — which Story 12.9's frozen boundaries put on the Ask First list and explicitly forbade.
  evidence: `client/src/features/rules/content/shared.ts:44-53` lists `bela` (1001 pts, tier 3, kind `run`) as the eighth `DeclarationBase`, and `rulesContent.parity.test.ts` asserts the id/pts/tier/kind tuple across all four locales. It is not a declaration in any engine sense: `declarations.go` never detects it, it is never announced, and it never joins the declaration contest — it ends the match from `checkInstantWin`. Tier 3 exists solely to give this one entry its own "wins the match on the spot" treatment, which is the tell that the grid is the wrong home for it.

- source_spec: `_bmad-output/implementation-artifacts/spec-12-9-variant-rules-reference-reconciliation.md`
  summary: The in-match rules overlay has never rendered the `tiers` block, so the honour-tier ladder is silently missing from the whole honour chapter in-game.
  evidence: `DarkBlock` in `client/src/features/match/components/RulesDialog.tsx` has cases for `p`, `rule`, `note`, `steps`, `cards`, `melds` and no `tiers` case, so the block falls to `default: return null`. Confirmed pre-existing at baseline `fba8409` — the same case list is present there, so Story 12.9 neither caused nor widened it. The standalone page renders it correctly via `TiersBlock` in `RuleBlock.tsx`.

- source_spec: `_bmad-output/implementation-artifacts/spec-12-9-variant-rules-reference-reconciliation.md`
  summary: The all-eight-trumps entry still renders a `+1001` points chip, asserting a point award the engine never makes.
  evidence: `checkInstantWin` (`server/internal/game/scoring.go:230`) sets only `WinnerTeam` and `Phase = PhaseMatchEnd`; it never touches `TeamScores` (verified against `scoring.go:218-222` and `bidding.go:190-193`). Story 12.9 corrected the prose to stop claiming the 1001 award, but `DECLARATIONS_BASE` in `content/shared.ts` still carries `pts: 1001` and the melds grid renders `+1001`. Under the 501 match mode the figure is wrong twice over. Fixing it means changing `DECLARATIONS_BASE` and the parity test's declaration-numbers assertion, which the 12.9 spec routed to Ask First rather than allowing.

- source_spec: `_bmad-output/implementation-artifacts/spec-12-9-variant-rules-reference-reconciliation.md`
  summary: The all-eight-trumps entry is typed `kind: "run"`, so the melds grid labels a match-ending hand "Run" / "Niz" / "Низа" alongside tierce and quint.
  evidence: `DECLARATIONS_BASE` id `bela` carries `kind: "run"` in `client/src/features/rules/content/shared.ts`, and `ui.meldKinds.run` renders as the chip's category label. It is not a sequence meld at all — it is an instant match win checked at trump-set time. Same blocker as the `pts: 1001` entry above: the fix is a `DECLARATIONS_BASE` change, and both are arguments for moving this entry out of the declarations grid entirely.

- source_spec: `_bmad-output/implementation-artifacts/spec-12-9-variant-rules-reference-reconciliation.md`
  summary: Pressing Escape while a difference-marker tooltip is pinned open closes the entire rules overlay instead of just dismissing the tooltip.
  evidence: `RulesDialog` installs `useFocusTrap({ onEscape: () => onOpenChange(false) })`, and base-ui's tooltip also dismisses on Escape, so a single press does both. Nested-dismissal ordering is a general overlay concern rather than a 12.9 defect — the same pattern would affect any future popup opened from inside a `Z.UTIL` overlay.

- source_spec: `_bmad-output/implementation-artifacts/spec-12-9-variant-rules-reference-reconciliation.md`
  summary: Nothing binds the rules page's per-variant claims to `VariantRules`, which is the root cause of the drift Story 12.9 had to repair by hand.
  evidence: Every variant claim now on the page has an exact field in `RulesFor` (`server/internal/game/types.go:183-203`) — `HasTrumpCandidate`, `RevealFaceDownOnRound2`, `AllPassOutcome`, `DeclarationOverlap`, `DeclarationTiming` — but no test, golden file or generated fixture asserts the page and the struct agree. The client mirrors are prose-only by design (see `epic-12-context.md`), so the next `VariantRules` change can silently falsify the page exactly as the last two stories did. A contract test keyed on the preset fields would convert a silent content lie into a failing build. (superseded 2026-08-22: Croatian bidding is single-round; the reveal event, FaceDownRevealed flag and RevealFaceDownOnRound2 field were removed — see spec-fix-croatian-single-round-bidding.md)

- source_spec: `_bmad-output/implementation-artifacts/spec-12-9-variant-rules-reference-reconciliation.md`
  summary: `scoring.go:81`'s comment still says Bitola "must eventually use HANGING POINTS on a tie", while the rules page now states the shared tie rule as permanent.
  evidence: The comment predates the 2026-08-20 owner decision that deferred Story 12.7 indefinitely and made the Croatian tie rule permanent for both variants. Story 12.9's spec forbade server changes, so the comment was left alone; it now contradicts the player-facing copy in all four locales. Retiring or restating it is a one-line server edit that belongs with whoever next opens `scoring.go`.

- source_spec: `_bmad-output/implementation-artifacts/spec-12-9-variant-rules-reference-reconciliation.md`
  summary: The new rules copy coins mk „именува" and hr/sr "nazvati boju" for naming trump, where the shipped UI and the terminology reference use земе/земи and zvati throughout.
  evidence: `mk.ts` uses „именува" 9 times; the shipped mk strings in `shared/i18n/mk.json` never do — they use „Земи адут во која боја сакаш", „продолжи понатаму", „избере боја". The Croatian variant genuinely has free-suit-naming semantics distinct from taking a candidate card, so a new verb may be justified, but whether to coin one is an owner terminology call rather than a defect. (The clearly wrong „наддава", which implies an ascending auction this game does not have, was fixed in 12.9.)

## Deferred from: code review of 12-10-per-recipient-snapshot-projection (2026-08-22)

- source_spec: `_bmad-output/implementation-artifacts/spec-12-10-per-recipient-snapshot-projection.md`
  summary: Retire the client's `?? player.hand.length` rolling-deploy fallback in `MatchPage.tsx` once 12.10 is deployed — after that it is dead code that can mask a hand-leak regression.
  evidence: A server that regressed to sending full opponent hands (and dropped `handCount`) would render perfectly normal card backs through the fallback, so the exact leak Story 12.10 closes could return without any visible symptom. The fallback exists only for the stale-server window during rollout; removing it afterwards makes such a regression render as zero card backs — ugly, but loud.

- source_spec: `_bmad-output/implementation-artifacts/spec-12-10-per-recipient-snapshot-projection.md`
  summary: Pre-existing flake — `TestVariantRulesAndDeclarationsContract/declaration_melds.json` intermittently fails because equal-value melds are emitted in nondeterministic order, breaking the byte-equal golden comparison.
  evidence: Reproduced at Story 12.10's baseline commit `da678bbaf71c7bd00627ece512f9310ddb14083b` in a clean worktree: 10 failures in 40 runs of `go test -count=1 ./internal/game/ -run Contract`, with the diff showing two 20-point tierces (7S-8S-9S vs TH-JH-QH) swapping list positions — classic map-iteration ordering in meld detection surfacing through the golden. Not caused by 12.10 (its `internal/game` diff is `state.go` JSON tags plus new projection files; `declarations.go` untouched). Makes `make test` and CI fail roughly one run in four. Fix belongs in the engine: a deterministic sort for equal-value melds (or in the golden sample construction), then a one-time golden regen for `declaration_melds.json`.
  update_2026-08-23: CLOSED. `detectDeclarations` now walks `AllSuits` and `AllRanks` instead of the `bySuit`/`byRank` maps (`server/internal/game/declarations.go`), so meld emission order is fixed. Verified 0 failures in 30 uncached runs of `go test ./internal/game/ -run Contract -count=1` (was 7/30 immediately before the change), plus 5 clean uncached runs of the full server suite. No golden regeneration was needed — `declaration_melds.json` is untouched, confirming the pinned order already matched AllSuits/AllRanks. Fixed as a standalone commit alongside `spec-fix-croatian-simultaneous-declaration-phase.md`, at the owner's request.

- source_spec: `_bmad-output/implementation-artifacts/spec-profile-clickable-player-names.md`
  summary: The SPA has no scroll restoration on route changes — clicking a sidebar link deep in a long profile lands mid-page on the destination profile.
  evidence: No `ScrollRestoration` or scroll-to-top exists anywhere in the client; this pre-dates the clickable-names work but became far more visible now that profile-to-profile navigation is reachable from the Partner Spotlight / Rivalries panels and match-history chips. An app-level `ScrollRestoration` (React Router v7) or a scroll-to-top on pathname change would fix every route transition at once.

- source_spec: `_bmad-output/implementation-artifacts/spec-fix-croatian-single-round-bidding.md`
  summary: TestVariantRulesAndDeclarationsContract/declaration_melds.json flakes because detectDeclarations iterates the bySuit map in random order while the golden pins one order (same defect as the 12.10-era entry above).
  evidence: Reproduced during this story's review at its own baseline `26caf0b84e080f30507ecfd371792e9c68c3c261` in a clean worktree — 2 failures in 8 runs, untouched by this change (the spec forbids touching declaration detection). Root cause pinpointed at `server/internal/game/declarations.go:46`; the one-line fix is iterating `AllSuits` order instead of the map (the current golden already matches S-before-H order), then no golden regen is needed.
  update_2026-08-23: CLOSED. `detectDeclarations` now walks `AllSuits` and `AllRanks` instead of the `bySuit`/`byRank` maps (`server/internal/game/declarations.go`), so meld emission order is fixed. Verified 0 failures in 30 uncached runs of `go test ./internal/game/ -run Contract -count=1` (was 7/30 immediately before the change), plus 5 clean uncached runs of the full server suite. No golden regeneration was needed — `declaration_melds.json` is untouched, confirming the pinned order already matched AllSuits/AllRanks. Fixed as a standalone commit alongside `spec-fix-croatian-simultaneous-declaration-phase.md`, at the owner's request.

- source_spec: `_bmad-output/implementation-artifacts/spec-dialog-button-layout-standardization.md`
  summary: The action-left/status-quo-right dialog convention has no in-repo record and no mechanical enforcement — it is upheld by 15 hand-written per-component order assertions.
  evidence: Surfaced by all three review layers. There is no `DialogActions` primitive that owns the footer shape and no lint rule, so the next dialog someone writes regresses silently and only a human reviewer would notice. The convention itself is documented only in this spec, which lives outside the source tree. A shared footer component (or extending `DialogFooter` to take `action`/`statusQuo` slots) would make the rule structural instead of conventional.

- source_spec: `_bmad-output/implementation-artifacts/spec-dialog-button-layout-standardization.md`
  summary: `window.confirm` still handles the leave-a-live-match confirmation, so the one dialog players meet most often follows OS button ordering rather than the new app convention.
  evidence: `client/src/features/match/MatchPage.tsx:968` — correctly exempted from this story as unstyleable, but the stated goal was muscle memory and this is the highest-frequency confirm in the app. Replacing it with the standard dialog behind React Router's `useBlocker` would bring it into the convention.

- source_spec: `_bmad-output/implementation-artifacts/spec-dialog-button-layout-standardization.md`
  summary: CreateRoomModal's footer has no narrow-screen stacking — its two buttons sit in a bare `flex` row with no `flex-col` below `sm`.
  evidence: Pre-existing; the footer was `justify-between` with no responsive direction before this story and is `justify-end` after, neither of which stacks. Every other multi-button footer in the app stacks below `sm` (shared `DialogFooter`, RoomEjectionModal). The `size="cta"` submit plus the cancel can overflow horizontally on narrow phones.

- source_spec: `_bmad-output/implementation-artifacts/spec-room-last-match-stats.md`
  summary: `.parchment-inset` restates 15 literal copies of the `:root` parchment palette, so retuning that palette silently desyncs the inset.
  evidence: `client/src/index.css` — every value in the new block (`#f5f2e8`, `#fbf9f2`, `#ffffff`, `#ebe6d6`, `#d9d1ba`, `#c9a876`, the four inks, the five accents) is byte-for-byte identical to `:root` in the same file, and nothing (lint, test, or comment cross-reference) ties them together. The felt scopes at least redefine *different* values; this block exists only to restate identical ones. The real fix is a single source — `--parchment-*` tokens at `:root` referenced by both — which means editing the global palette and is beyond this story's blast radius.

- source_spec: `_bmad-output/implementation-artifacts/spec-room-last-match-stats.md`
  summary: Boot-reconcile placeholder rows surface as a room's "last match", showing 0–0 with no hand details after a server crash.
  evidence: `server/internal/match/reconcile.go:124-131` writes `TeamAScore: 0, WinnerTeam: 0, CompletedAt: time.Now(), Status: "abandoned"` with no hands for every room stuck in `playing` at boot. `GetLastMatchForRoomAndUser` accepts `abandoned`, so that placeholder becomes the room's last match. The surface degrades honestly (the card's `match-history-no-hands` empty state renders) rather than lying, and it only occurs post-crash — but a crash-recovery artifact presented as a played match is still misleading. Either exclude hand-less placeholder rows from the query or mark them distinctly in the DTO.
  resolved: 2026-08-23 — `GetLastMatchForRoomAndUser` now requires `EXISTS (SELECT 1 FROM hand_results ...)`, so a row with no scored hand is never the room's last match (this also covers a real abandonment inside hand 1, which has the same empty shape). Covered by two real-DB cases in `gorm_repo_test.go`: the placeholder is skipped, and it does not shadow an earlier real match. Confirmed live against a hand-less row dated an hour after a real match — the endpoint returned the real one.

- source_spec: `_bmad-output/implementation-artifacts/spec-room-last-match-stats.md`
  summary: Five near-identical `MatchListItem` fixtures were added across test files instead of one factory in `test-utils.tsx`.
  evidence: `makeLastMatch` (MatchResult.test), `makeMatch` (LastMatchDialog.test), `makeMatch` (MatchPlayerActions.test), `makeMatch` (MatchStatsCard.test) and the inline `lastMatch` literal in `RoomPage.test.tsx` are the same ~35-line DTO. The repo convention centralizes fixtures — `client/src/test-utils.tsx` already hosts `makeUser`, `makeRoom`, `makeRoomPlayer`, `makeRoomDetail`, `makeRoomInvite`. Adding a `MatchListItem` field now means five edits.

- source_spec: `_bmad-output/implementation-artifacts/spec-whisper-unread-chip.md`
  summary: A third copy of the same `WhisperPayload` fixture was added instead of one factory in `test-utils.tsx`.
  evidence: Identical factory bodies now sit in `client/src/shared/stores/chatStore.test.ts`, `client/src/features/chat/ChatDock.test.tsx` and (new this story) `client/src/features/match/components/MatchChatDock.test.tsx`. The repo convention centralizes fixtures in `client/src/test-utils.tsx`, which already hosts `makeUser`, `makeRoom`, `makeRoomDetail`. Adding a `WhisperPayload` field now means three edits. The duplication predates this story; this change made it a triple.

- source_spec: `_bmad-output/implementation-artifacts/spec-whisper-unread-chip.md`
  summary: `i18n.parity.test.ts` structurally forbids i18next plural categories, so every count string in the app is stuck with a `Label: {{count}}` form.
  evidence: The parity test asserts identical flattened key sets across `en/mk/hr/sr`. i18next plural suffixes are per-locale — en needs `_one`/`_other`, hr/sr need `_one`/`_few`/`_other` — so any correctly pluralized key fails parity by construction. `whisper.unreadCount` and `chat.unreadCount` therefore use a labelled-value phrasing that reads acceptably at 1 rather than a grammatically inflected one. Fixing this properly means teaching the parity test to group plural suffix families, which affects every future count string, not just these two.

- source_spec: `_bmad-output/implementation-artifacts/spec-whisper-unread-chip.md`
  summary: A whisper's *arrival* still has no cue — only a static number that changes on an already-visible chip.
  evidence: Public messages get a 2-second peek bubble (`ChatDock.tsx` `peekPayload`); a whisper deliberately gets none, because previewing private text on a closed FAB is forbidden. But that leaves no arrival signal at all: no pulse, no animation, and nothing announced (the count only reaches a screen reader when the FAB is focused). The codebase already has `animate-[card-in_.2s_ease_both]` and a `pulse-dot` keyframe, and an `aria-live="polite"` node outside the button would announce it. Deferred because a one-shot animation plus a live region is a design decision, and the spec's "Ask First" fences off notification behaviour.

- source_spec: `_bmad-output/implementation-artifacts/spec-whisper-unread-chip.md`
  summary: `RoomChatDock` has no test file, so one of the three dock variants this change alters is entirely uncovered.
  evidence: `client/src/features/room/components/RoomChatDock.tsx` exists with no sibling `.test.tsx`; `ChatDock.test.tsx` covers the lobby variant and `MatchChatDock.test.tsx` the match variant. The room variant is the one whose `openLabel` differs in every locale, and the new accessible-name composition is variant-keyed. Pre-existing gap, surfaced by a change that claims identical behaviour across all three variants.

- source_spec: `_bmad-output/implementation-artifacts/spec-room-declarations-toggle.md`
  summary: A match played without declarations is indistinguishable in match history, and its per-hand "declarations" column is a permanent all-zero.
  evidence: `match.Match` (server/internal/match/model.go) persists Variant, MatchMode and CoinBuyIn but no DeclarationsEnabled, so nothing downstream can know the match was bez zvanja. `MatchStatsCard.tsx` renders the per-hand declarations column unconditionally. Explicitly out of scope for the toggle spec (its "Ask First" names persisting the flag on `matches` and surfacing it in history), so it was left for an owner decision rather than inferred.

- source_spec: `_bmad-output/implementation-artifacts/spec-room-declarations-toggle.md`
  summary: The in-match rules reference still teaches melds and Belote at a table that plays without them.
  evidence: `RulesDialog` (client/src/features/match/components/RulesDialog.tsx) takes only `variant` and renders the full meld/Belote chapters from the static rules content; `matchState.declarationsEnabled` is now on the wire and unused there. Its own prop doc says the variant is passed precisely so the overlay never contradicts the table in front of the player, so the same argument now applies to declarations. Out of scope by the toggle spec's explicit "Never" (the rules reference belongs to Story 12.9), which already owns reconciling that content against real engine behaviour.
