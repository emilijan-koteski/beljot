# Google SSO — the OAuth client ID (the *.apps.googleusercontent.com value,
# NOT the client secret). Rendered into the GIS button; leave empty to hide
# "Continue with Google" entirely. Must match the server's
# BELJOT_GOOGLE_CLIENT_ID (the token audience).
VITE_GOOGLE_CLIENT_ID=your-client-id.apps.googleusercontent.com
